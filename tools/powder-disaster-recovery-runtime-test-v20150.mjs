import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';
const root=path.resolve(process.argv[2]||process.cwd()),read=r=>fs.readFileSync(path.join(root,r),'utf8'),o={version:'20.15.0',checks:{}},ok=(k,v)=>o.checks[k]=!!v;
const r=spawnSync(process.execPath,[path.join(root,'tools/powder-disaster-recovery-runner-v20150.mjs'),'--self-test'],{encoding:'utf8'});let d={};try{d=JSON.parse(r.stdout)}catch{};ok('runnerSelfTest',r.status===0&&d.pass===true);ok('requiredTables12',d.checks?.requiredTables===true);ok('differentDatabaseGuard',d.checks?.sourceRestoreDifferent===true);ok('digestRoundTrip',d.checks?.allDigests===true);ok('postRestoreSmoke',d.checks?.allSmoke===true);ok('failClosedRecovery',d.checks?.failClosed===true);ok('rollbackEvidence',d.checks?.rollback===true);const rt=read('js/disaster-recovery-runtime-v20150.js');ok('playerPassive',rt.includes('passive:true')&&rt.includes('automaticRestore:false')&&rt.includes('productionWrites:false'));const admin=read('js/admin-disaster-recovery-v20150.js');ok('adminNoManualPass',admin.includes('không cho tự nhập PASS')&&!admin.includes('Record Real Restore Evidence'));o.pass=Object.values(o.checks).every(Boolean);console.log(JSON.stringify(o,null,2));if(!o.pass)process.exit(1);
