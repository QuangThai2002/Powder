import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';
const root=path.resolve(process.argv[2]||'.'),write=process.argv.includes('--write'),bootPath=path.join(root,'js/boot-loader-v21004.js'),original=fs.readFileSync(bootPath,'utf8');
const runtime='js/learning-academic-content-v2113.js',engine='js/learning-question-engine-v2112.js',app='js/app.js',MANAGED=[runtime,engine];
const mm=original.match(/const MANIFEST=(\[[\s\S]*?\]);\nconst SCRIPT_ORDER=/),om=original.match(/const SCRIPT_ORDER=(\[[\s\S]*?\]);\nconst TOTAL_BYTES=/);if(!mm||!om)throw new Error('Boot manifest/script order not found');let manifest=JSON.parse(mm[1]),order=JSON.parse(om[1]);
function meta(u){const b=fs.readFileSync(path.join(root,u));return{u,s:b.length,r:crypto.createHash('sha256').update(b).digest('hex').slice(0,12),k:u.endsWith('.css')?'style':'script'}}
for(const u of MANAGED){const m=meta(u),mi=manifest.findIndex(x=>x.u===u);if(mi>=0)manifest[mi]={...manifest[mi],...m};else manifest.push(m)}
order=order.filter(x=>x!==runtime);const ei=order.indexOf(engine);if(ei<0)throw new Error('Learning engine missing from SCRIPT_ORDER');order.splice(ei,0,runtime);
const manifestJson=JSON.stringify(manifest),orderJson=JSON.stringify(order),hash=crypto.createHash('sha256').update(manifestJson).digest('hex').slice(0,16);let next=original.replace(/MANIFEST_HASH="[0-9a-f]+"/,`MANIFEST_HASH="${hash}"`).replace(/const MANIFEST=\[[\s\S]*?\];\nconst SCRIPT_ORDER=/,`const MANIFEST=${manifestJson};\nconst SCRIPT_ORDER=`).replace(/const SCRIPT_ORDER=\[[\s\S]*?\];\nconst TOTAL_BYTES=/,`const SCRIPT_ORDER=${orderJson};\nconst TOTAL_BYTES=`);
const runtimeIndex=order.indexOf(runtime),engineIndex=order.indexOf(engine),appIndex=order.indexOf(app),details={version:'21.1.3',manifestEntries:manifest.length,scriptOrder:order.length,manifestHash:hash,managed:MANAGED.map(meta),runtimeIndex,engineIndex,appIndex,changed:next!==original};
if(manifest.length!==1295||order.length!==133||runtimeIndex!==engineIndex-1||engineIndex!==appIndex-1)throw new Error(`Unexpected 21.1.3 boot shape: ${JSON.stringify(details)}`);if(write&&next!==original)fs.writeFileSync(bootPath,next);console.log(JSON.stringify(details,null,2));if(!write&&next!==original)process.exit(1);
