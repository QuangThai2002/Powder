import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';
const root=path.resolve(process.argv[2]||process.cwd()),read=r=>fs.readFileSync(path.join(root,r),'utf8'),o={version:'20.14.0',checks:{}},ok=(k,v)=>o.checks[k]=!!v;
const r=spawnSync(process.execPath,[path.join(root,'tools/powder-load-soak-runner-v20140.mjs'),'self-test'],{encoding:'utf8'});let d={};try{d=JSON.parse(r.stdout)}catch{};ok('runnerSelfTest',r.status===0&&d.pass===true);ok('sixScenarios',Object.keys(d.sample?.scenarioCounts||{}).length===6);ok('safeReadTraffic',d.checks?.safeReads===true);ok('integrityDeltaZero',d.checks?.integrityZero===true);ok('evidenceSha256',d.checks?.evidenceHash===true);const rt=read('js/load-soak-runtime-v20140.js');ok('playerPassiveOnly',rt.includes('passiveOnly:true')&&rt.includes('networkLoadGenerator:false'));const mig=read('server/supabase/migrations/20260823_20140_production_load_soak_testing.sql');ok('serverOwnsPass',mig.includes('powder_load_soak_validate_report_v20140')&&!mig.includes("p_report->>'pass'"));o.pass=Object.values(o.checks).every(Boolean);console.log(JSON.stringify(o,null,2));if(!o.pass)process.exit(1);
