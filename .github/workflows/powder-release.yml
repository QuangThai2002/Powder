name: Powder Release

on:
  push:
    tags:
      - 'v*.*.*'

concurrency:
  group: powder-release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout exact tag
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '24'
      - name: Validate tag and runtime metadata
        shell: bash
        run: |
          VERSION="$(node -p "JSON.parse(require('fs').readFileSync('release.json','utf8')).version")"
          EXPECTED="v${VERSION}"
          test "${GITHUB_REF_NAME}" = "${EXPECTED}" || { echo "Tag ${GITHUB_REF_NAME} != ${EXPECTED}"; exit 1; }
          test "$(node -p "JSON.parse(require('fs').readFileSync('release.json','utf8')).official")" = "false" || { echo "Automation never silently flips official=true"; exit 1; }
      - name: Repository automation policy
        run: node tools/powder-ci-policy-gate-v21005.mjs .
      - name: Supply-chain security policy
        run: node tools/powder-supply-chain-gate-v21006.mjs .
      - name: Run current runtime final gate
        run: node tools/powder-final-gate-v21004.mjs . | tee FINAL-GATE-RELEASE.json
      - name: Browser release smoke
        run: node tools/powder-browser-e2e-v21007.mjs .
      - name: Visual and performance release gate
        run: node tools/powder-visual-performance-gate-v21008.mjs .
      - name: Package release artifact
        shell: bash
        run: |
          VERSION="$(node -p "JSON.parse(require('fs').readFileSync('release.json','utf8')).version")"
          BUILD="$(node -p "JSON.parse(require('fs').readFileSync('release.json','utf8')).buildId")"
          NAME="Powder-${VERSION}"
          zip -qr "${NAME}.zip" . -x '.git/*' '.github/*' 'artifacts/*' '*.zip' '*.sha256' 'CI-FINAL-GATE*' 'FINAL-GATE-RELEASE*' '*-PROVENANCE.json' '*-PROVENANCE-VERIFY.json' '*-RECOVERY.json'
          sha256sum "${NAME}.zip" > "${NAME}.zip.sha256"
          cp FINAL-GATE-RELEASE.json "${NAME}-FINAL-GATE.json"
          printf '%s\n' "buildId=${BUILD}" > "${NAME}-BUILD.txt"
      - name: Generate and verify provenance and recovery evidence
        shell: bash
        run: |
          VERSION="$(node -p "JSON.parse(require('fs').readFileSync('release.json','utf8')).version")"
          NAME="Powder-${VERSION}"
          node tools/powder-release-provenance-v21009.mjs generate . "${NAME}.zip" "${NAME}-FINAL-GATE.json" "${NAME}" --require-tag
          node tools/powder-release-provenance-v21009.mjs verify . "${NAME}.zip" "${NAME}-FINAL-GATE.json" "${NAME}" --require-tag
      - name: Publish GitHub Release assets
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          VERSION="$(node -p "JSON.parse(require('fs').readFileSync('release.json','utf8')).version")"
          NAME="Powder-${VERSION}"
          gh release create "${GITHUB_REF_NAME}" \
            "${NAME}.zip" \
            "${NAME}.zip.sha256" \
            "${NAME}-FINAL-GATE.json" \
            "${NAME}-BUILD.txt" \
            "${NAME}-PROVENANCE.json" \
            "${NAME}-PROVENANCE.json.sha256" \
            "${NAME}-PROVENANCE-VERIFY.json" \
            "${NAME}-RECOVERY.json" \
            --verify-tag \
            --title "Powder ${VERSION}" \
            --generate-notes
