{
  "name": "powder",
  "private": true,
  "version": "22.2.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets:audit": "node tools/audit-pow-assets.mjs",
    "check": "npm run typecheck && npm run build && npm run assets:audit"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}