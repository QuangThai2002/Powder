(()=>{'use strict';if(window.POWDER_ADMIN_DOMAIN_TELEMETRY_V2176)return;const VERSION='21.7.6',URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151',CARD='cl2176DomainTelemetry';const $=(s,r=document)=>r.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const st={mounted:false,loads:0,lastError:'',lastAt:0,packet:null};let mountTimer=0;
function session(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}async function api(){const s=session();if(!s?.access_token)throw new Error('Cần đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-domain-v2176',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action:'summary',limit:40})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function pct(v){return`${Math.round((Number(v)||0)*100)}%`}function ms(v){return`${Math.round(Number(v)||0)} ms`}function row(r){const a=r.players?.a||{},b=r.players?.b||{},alert=(r.alerts||[]).length?`<em>${esc((r.alerts||[]).join(' · '))}</em>`:'<i>OK</i>';return `<div class="cl2176-row"><div><b>${esc(a.name)} ${a.score?.correct||0}/${a.score?.total||0}</b><small>${esc(r.domains?.a||'—')}</small></div><strong>VS</strong><div><b>${esc(b.name)} ${b.score?.correct||0}/${b.score?.total||0}</b><small>${esc(r.domains?.b||'—')}</small></div><span>Δ${Number(r.correctMargin)||0} · T${Number(r.supremacy?.tier)||0}</span>${alert}</div>`}
function render(){const root=$('#'+CARD),p=st.packet;if(!root)return;if(!p){root.querySelector('.cl2176-body').innerHTML='<p class="muted">Chưa tải telemetry.</p>';return}const m=p.metrics||{};root.querySelector('.cl2176-metrics').innerHTML=`<span><small>CLASH</small><b>${m.total||0}</b></span><span><small>RESOLVED</small><b>${m.resolved||0}</b></span><span><small>ACTIVE</small><b>${m.active||0}</b></span><span><small>TIMEOUT</small><b>${pct(m.timeoutRate)}</b></span><span><small>AVG Δ ĐÚNG</small><b>${m.avgCorrectMargin||0}</b></span><span><small>AVG ĐÚNG</small><b>${ms(m.avgCorrectResponseMs)}</b></span><span><small>HỦY LĐ</small><b>${m.domainDestroyed||0}</b></span><span><small>CẢNH BÁO</small><b>${m.alerts||0}</b></span>`;root.querySelector('.cl2176-body').innerHTML=(p.rows||[]).map(row).join('')||'<p class="muted">Chưa có Clash server-authoritative.</p>';root.querySelector('.cl2176-status').textContent=`✓ SERVER · READ ONLY · ${new Date(p.serverNow||Date.now()).toLocaleTimeString('vi-VN')}`}
async function load(){const root=$('#'+CARD);if(!root)return;st.loads++;root.querySelector('.cl2176-status').textContent='Đang đọc Domain authority…';try{st.packet=await api();st.lastError='';st.lastAt=Date.now();render()}catch(e){st.lastError=String(e?.message||e);root.querySelector('.cl2176-status').textContent=`Lỗi: ${st.lastError}`}}
function card(){const e=document.createElement('section');e.id=CARD;e.className='admin-card cl2176-card';e.innerHTML='<div class="cl2176-head"><div><p class="eyebrow">ADMIN ONLY · 21.7.6</p><h3>Domain Authority Telemetry</h3><p>Chỉ đọc kết quả Đấu Lãnh Địa PvP thật. Không hiển thị answer key/câu hỏi/đáp án và không có quyền sửa điểm hay người thắng.</p></div><button class="admin-btn primary" data-refresh>↻ Làm mới</button></div><div class="cl2176-status">Sẵn sàng.</div><div class="cl2176-metrics"></div><div class="cl2176-body"><p class="muted">Chưa tải telemetry.</p></div>';e.querySelector('[data-refresh]').onclick=load;return e}
function mount(reason='boot'){clearTimeout(mountTimer);if(document.getElementById(CARD)){st.mounted=true;return true}const root=$('.combat2139'),anchor=$('#cl2175DomainPipelineCard')||$('.cl21610-card')||$('.cl2139-qa');if(!root||!anchor){mountTimer=setTimeout(()=>mount('settle'),260);return false}const e=card();anchor.insertAdjacentElement('afterend',e);st.mounted=true;if(reason!=='boot')load();return true}
function snapshot(){return{version:VERSION,...st,privacy:{readOnly:true,answerKey:false,questionPayload:false,choice:false},polling:false,network:'manual refresh / Combat Lab open only'}}window.POWDER_ADMIN_DOMAIN_TELEMETRY_V2176=Object.freeze({version:VERSION,mount:()=>mount('manual'),load,snapshot,report:()=>st.packet});document.addEventListener('click',e=>{if(e.target?.closest?.('[data-page="combatlab"]')){mount('nav');setTimeout(load,250)}},true);window.addEventListener('pagehide',()=>clearTimeout(mountTimer),{once:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>mount(),{once:true});else mount();})();