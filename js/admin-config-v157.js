(()=>{'use strict';
const URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));let data=null;
function session(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,payload={}){const s=session();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-config',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...payload})});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={error:t}}if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function toast(t){const e=$('#adminToast');if(!e)return;e.textContent=t;e.hidden=false;clearTimeout(e._t);e._t=setTimeout(()=>e.hidden=true,2800)}
function pretty(v){return JSON.stringify(v??{},null,2)}
function renderConfigs(){const host=$('#balanceConfigGrid');if(!host)return;host.innerHTML=(data?.configs||[]).map(x=>`<section class="admin-card config-card-v157"><div class="card-head"><div><small>REMOTE CONFIG</small><h2>${esc(x.key)}</h2></div><span class="config-badge-v157">rev ${Number(x.revision||0)}</span></div><div class="config-meta"><span>${esc(x.updated_by||'system')}</span><span>${x.updated_at?new Date(x.updated_at).toLocaleString('vi-VN'):'—'}</span></div><textarea data-balance-json="${esc(x.key)}">${esc(pretty(x.value))}</textarea><div class="liveops-admin-actions"><button class="admin-btn primary" data-save-balance="${esc(x.key)}">Lưu cấu hình</button></div></section>`).join('');$$('[data-save-balance]').forEach(b=>b.onclick=async()=>{const key=b.dataset.saveBalance,ta=$(`[data-balance-json="${CSS.escape(key)}"]`);try{const value=JSON.parse(ta.value);b.disabled=true;await api('balance_update',{key,value});toast('Đã lưu cấu hình '+key);await load()}catch(e){toast('Không lưu: '+e.message)}finally{b.disabled=false}})}
function renderPowballs(){const tb=$('#powballBalanceRows');if(!tb)return;tb.innerHTML=(data?.powballs||[]).map(x=>`<tr data-pb="${esc(x.ball_id)}"><td><b>${esc(x.rarity)}</b><small>${esc(x.ball_id)}</small></td><td><input data-f="cost" type="number" value="${Number(x.coin_cost||0)}"></td><td><input data-f="pity" type="number" value="${Number(x.pity_threshold||0)}"></td><td><input data-f="rank" type="number" min="0" max="6" value="${Number(x.requirement_rank||0)}"></td><td><textarea data-f="rates">${esc(pretty(x.drop_rates))}</textarea></td><td><button class="admin-btn primary" data-save-pb="${esc(x.ball_id)}">Lưu</button></td></tr>`).join('');$$('[data-save-pb]').forEach(b=>b.onclick=async()=>{const tr=b.closest('tr'),id=b.dataset.savePb;try{await api('powball_update',{ballId:id,coinCost:Number(tr.querySelector('[data-f=cost]').value),pityThreshold:Number(tr.querySelector('[data-f=pity]').value),requirementRank:Number(tr.querySelector('[data-f=rank]').value),dropRates:JSON.parse(tr.querySelector('[data-f=rates]').value)});toast('Đã cập nhật '+id);await load()}catch(e){toast(e.message)}})}
function renderShop(){const tb=$('#shopRankRows');if(!tb)return;const names=['Nhập môn','Sơ cấp','Trung cấp','Cao cấp','Chuyên nghiệp','Cao thủ','Thách đấu'];tb.innerHTML=(data?.shopRanks||[]).map(x=>`<tr data-rank="${x.rank}"><td><b>${names[x.rank]||x.rank}</b></td><td><input data-f="refresh" type="number" min="1" max="8" value="${x.refresh_limit}"></td><td><input data-f="limit" type="number" min="1" max="4" value="${x.exchange_limit}"></td><td><input data-f="cost" type="number" value="${x.exchange_coin_cost}"></td><td><input data-f="gain" type="number" value="${x.exchange_currency_gain}"></td><td><button class="admin-btn primary" data-save-rank="${x.rank}">Lưu</button></td></tr>`).join('');$$('[data-save-rank]').forEach(b=>b.onclick=async()=>{const tr=b.closest('tr');try{await api('shop_rank_update',{rank:Number(b.dataset.saveRank),refreshLimit:Number(tr.querySelector('[data-f=refresh]').value),exchangeLimit:Number(tr.querySelector('[data-f=limit]').value),exchangeCoinCost:Number(tr.querySelector('[data-f=cost]').value),exchangeCurrencyGain:Number(tr.querySelector('[data-f=gain]').value)});toast('Đã cập nhật quyền lợi Rank');await load()}catch(e){toast(e.message)}})}
function renderEquipment(){const q=String($('#catalogSearch')?.value||'').toLowerCase(),rows=(data?.equipment||[]).filter(x=>!q||`${x.name} ${x.equipment_id} ${x.rarity} ${x.set_id}`.toLowerCase().includes(q)).slice(0,120);$('#equipmentCatalogRows').innerHTML=rows.map(x=>`<tr><td><b>${esc(x.name)}</b><small>${esc(x.equipment_id)} · ${esc(x.rarity)} · ${esc(x.slot)}</small></td><td><input data-eq-price="${esc(x.equipment_id)}" type="number" value="${Number(x.price||0)}"></td><td><input data-eq-rank="${esc(x.equipment_id)}" type="number" min="0" max="6" value="${Number(x.min_rank||0)}"></td><td><button class="admin-btn primary" data-save-eq="${esc(x.equipment_id)}">Lưu</button></td></tr>`).join('')||'<tr><td colspan="4">Không có kết quả.</td></tr>';$$('[data-save-eq]').forEach(b=>b.onclick=async()=>{const id=b.dataset.saveEq;try{await api('equipment_update',{equipmentId:id,price:Number($(`[data-eq-price="${CSS.escape(id)}"]`).value),minRank:Number($(`[data-eq-rank="${CSS.escape(id)}"]`).value)});toast('Đã cập nhật trang bị');await load()}catch(e){toast(e.message)}})}
function render(){renderConfigs();renderPowballs();renderShop();renderEquipment()}
async function load(){try{$('#configLoadState').textContent='Đang tải cấu hình...';data=await api('state');render();$('#configLoadState').textContent='Đã đồng bộ từ server'}catch(e){$('#configLoadState').textContent=e.message;toast(e.message)}}
function bind(){const btn=$('#adminNav [data-page="config"]');btn?.addEventListener('click',()=>setTimeout(()=>{$('#adminPageTitle').textContent='Cân bằng & nội dung game';load()},0));$('#configRefresh')?.addEventListener('click',load);$('#catalogSearch')?.addEventListener('input',renderEquipment)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
