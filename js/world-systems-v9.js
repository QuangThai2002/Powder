(()=>{'use strict';
const W=window.POWDER_WORLD_DATA_V9,D=window.POWDER_DATA,C=window.POWDER_CONFIG;if(!W||!D)return;
const KEY='powder_world_systems_v9',OLD='powder_world_systems_v8';
const rarityLabel={common:'Thường',rare:'Hiếm',super_rare:'Siêu hiếm',epic:'Sử thi',legendary:'Huyền thoại',mythic:'Thần thoại',ancient:'Thượng cổ'};
const rarityOrder={common:0,rare:1,super_rare:2,epic:3,legendary:4,mythic:5,ancient:6};const rarityMaxStars={common:1,rare:2,super_rare:3,epic:4,legendary:5,mythic:6,ancient:7};
const ART_UPGRADE_CFG=W.economy?.artifactUpgrade||{},ART_UPGRADE_RATE={common:.01,rare:.02,super_rare:.05,epic:.08,legendary:.10,mythic:.20,ancient:.30,...(ART_UPGRADE_CFG.rarityRates||{})},ART_DUP_COST={1:1,2:1,3:2,4:2,5:3,6:3,7:4,8:4,9:5,10:5,...(ART_UPGRADE_CFG.duplicateCostTable||{})},ART_MAX_UPGRADE=Math.max(1,Number(ART_UPGRADE_CFG.maxUpgrade||10));
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const powThumb=asset=>window.POWDER_THUMBNAILS?.[asset]||asset;
const itemThumb=asset=>window.POWDER_ITEM_THUMBNAILS?.[asset]||asset;
const currencyIcon=(kind,cls='currency-inline-icon')=>`<img class="${cls}" src="assets/items/ui-compact/${kind==='element'?'element-coin':'military-coin'}.webp" alt="" aria-hidden="true">`;const candyMeta={common:{name:'Kẹo Thường',asset:'assets/items/ui-compact/candy-common.webp',xp:60},rare:{name:'Kẹo Hiếm',asset:'assets/items/ui-compact/candy-rare.webp',xp:300},legendary:{name:'Kẹo Huyền thoại',asset:'assets/items/ui-compact/candy-legendary.webp',xp:1200}};const candyWallet=a=>{const q=a?.powCandies&&typeof a.powCandies==='object'?a.powCandies:{};return{common:Math.max(0,Number(q.common??a?.powCandy)||0),rare:Math.max(0,Number(q.rare)||0),legendary:Math.max(0,Number(q.legendary)||0)}};
const powerFromStats=s=>s?Math.round((s.hp||0)*.16+(s.atk||0)*2.05+(s.def||0)*1.75+(s.ap||0)*1.85+(s.speed||0)*1.35+(s.critRate||0)*5+(s.critDamage||0)*.3):0;
function defaults(){return{version:9,saveMeta:{schema:9,lastSavedAt:0,lastReason:'',recoveryCount:0,recoverySource:''},journal:{artifactSummon:null,currencyExchange:null},equipmentOwned:{},equipmentEnhance:{},artifactOwned:{},artifactInstances:{},artifactInstanceSeq:0,powEquipment:{},powArtifact:{},selectedPow:null,elementCoin:W.economy.startingBalance,artifactTickets:10,ticketStats:{earned:10,spent:0},ticketClaims:{},shop:{lastRefresh:0,nextRefresh:0,offers:[],manualRefreshes:0,activeTab:'equipment',dailyKey:'',dailyRefreshes:0,dailyExchanges:0,purchaseHistory:[]},artifactSummon:{totalPulls:0,since:{super_rare:0,epic:0,legendary:0,mythic:0,ancient:0},history:[],lastResults:[]},stats:{purchases:0,spent:0,earned:0,artifactPulls:0,artifactUpgrades:0},ui:{tab:'overview',equipSlot:'all',rarity:'all',artifactCollectionOpen:false}}}
function artifactMaster(id){return W.artifacts.find(x=>x.id===id||x.artifactId===id)||null}
function artifactInstanceId(state){state.artifactInstanceSeq=Math.max(0,Number(state.artifactInstanceSeq||0))+1;return `ARTI-${Date.now().toString(36)}-${state.artifactInstanceSeq.toString(36)}`}
function createArtifactInstanceForState(state,artifactId,opts={}){const raw=artifactMaster(artifactId);if(!raw)return null;state.artifactInstances=state.artifactInstances&&typeof state.artifactInstances==='object'?state.artifactInstances:{};const instanceId=opts.instanceId||artifactInstanceId(state),rate=Number(raw.upgradeRate??ART_UPGRADE_RATE[raw.rarity]??0),inst={instanceId,artifactId:raw.id,rarity:raw.rarity,upgradeLevel:Math.max(0,Math.min(Number(raw.maxUpgrade||ART_MAX_UPGRADE),Math.floor(Number(opts.upgradeLevel)||0))),baseStats:{...(opts.baseStats||raw.baseStats||raw.statBonusPct||{})},upgradeRate:rate,isLocked:!!opts.isLocked,isFavorite:!!opts.isFavorite,equippedBy:opts.equippedBy||null,acquiredAt:Number(opts.acquiredAt||Date.now())};state.artifactInstances[instanceId]=inst;return inst}
function rebuildArtifactOwned(state){const counts={};for(const inst of Object.values(state.artifactInstances||{})){if(!artifactMaster(inst.artifactId))continue;counts[inst.artifactId]=(counts[inst.artifactId]||0)+1}state.artifactOwned=counts;return counts}
function normalizeArtifactInventory(state){state.artifactInstances=state.artifactInstances&&typeof state.artifactInstances==='object'?state.artifactInstances:{};state.artifactInstanceSeq=Math.max(0,Number(state.artifactInstanceSeq||0));const hasInstances=Object.keys(state.artifactInstances).length>0;if(!hasInstances){for(const [artifactId,count] of Object.entries(state.artifactOwned||{})){for(let i=0;i<Math.max(0,Math.floor(Number(count)||0));i++)createArtifactInstanceForState(state,artifactId,{acquiredAt:Date.now()-i})}}
for(const [iid,inst] of Object.entries(state.artifactInstances)){const raw=artifactMaster(inst.artifactId);if(!raw){delete state.artifactInstances[iid];continue}inst.instanceId=iid;inst.rarity=raw.rarity;inst.upgradeLevel=Math.max(0,Math.min(Number(raw.maxUpgrade||ART_MAX_UPGRADE),Math.floor(Number(inst.upgradeLevel)||0)));inst.baseStats={...(inst.baseStats||raw.baseStats||raw.statBonusPct||{})};inst.upgradeRate=Number(raw.upgradeRate??ART_UPGRADE_RATE[raw.rarity]??0);inst.isLocked=!!inst.isLocked;inst.isFavorite=!!inst.isFavorite;inst.equippedBy=null;inst.acquiredAt=Number(inst.acquiredAt||Date.now())}
const migrated={};for(const [pid,ref] of Object.entries(state.powArtifact||{})){let inst=state.artifactInstances?.[ref]||null;if(!inst&&artifactMaster(ref)){inst=Object.values(state.artifactInstances).find(x=>x.artifactId===ref&&!Object.values(migrated).includes(x.instanceId))||createArtifactInstanceForState(state,ref)}if(inst){inst.equippedBy=pid;migrated[pid]=inst.instanceId}}state.powArtifact=migrated;rebuildArtifactOwned(state);state.saveMeta={schema:9,lastSavedAt:0,lastReason:'',recoveryCount:0,recoverySource:'',...(state.saveMeta||{})};state.saveMeta.schema=9;state.journal=state.journal&&typeof state.journal==='object'?state.journal:{artifactSummon:null,currencyExchange:null};state.journal.artifactSummon=state.journal.artifactSummon||null;state.journal.currencyExchange=state.journal.currencyExchange||null;state.version=9;return state}
function worldStorageWrite(key,value){const text=String(value);try{localStorage.setItem(key,text);return localStorage.getItem(key)===text}catch(_){return false}}
function worldAtomicSave(reason='auto'){S.saveMeta={schema:9,lastSavedAt:Date.now(),lastReason:String(reason||'auto'),recoveryCount:0,recoverySource:'',...(S.saveMeta||{})};S.saveMeta.schema=9;S.saveMeta.lastSavedAt=Date.now();S.saveMeta.lastReason=String(reason||'auto');const text=JSON.stringify(S),pending=`${KEY}_pending`,recovery=`${KEY}_recovery`,previous=localStorage.getItem(KEY);try{if(previous){try{JSON.parse(previous);worldStorageWrite(recovery,previous)}catch(_){}}if(!worldStorageWrite(pending,text))throw new Error('world pending write failed');JSON.parse(localStorage.getItem(pending)||'');if(!worldStorageWrite(KEY,text))throw new Error('world commit failed');JSON.parse(localStorage.getItem(KEY)||'');localStorage.removeItem(pending);return true}catch(err){console.error('[Powder world safe save]',err);if(previous)worldStorageWrite(KEY,previous);return false}}
function worldReadRaw(){const priority={pending:4,primary:3,recovery:2,backup:1},rows=[];for(const [key,label] of [[KEY,'primary'],[`${KEY}_pending`,'pending'],[`${KEY}_recovery`,'recovery'],[`${KEY}_backup_latest`,'backup'],[`${KEY}_backup_previous`,'backup']]){try{const raw=localStorage.getItem(key);if(!raw)continue;let parsed=JSON.parse(raw);if(label==='backup'&&parsed?.state)parsed=parsed.state;if(parsed&&typeof parsed==='object')rows.push({key,label,raw,parsed,stamp:Number(parsed.saveMeta?.lastSavedAt||0),priority:priority[label]||0})}catch(_){}}if(!rows.length)return null;rows.sort((a,b)=>b.stamp-a.stamp||b.priority-a.priority);const best=rows[0],parsed=best.parsed;if(best.label!=='primary'){parsed.saveMeta={...(parsed.saveMeta||{}),recoveryCount:Number(parsed.saveMeta?.recoveryCount||0)+1,recoverySource:best.label};worldStorageWrite(KEY,JSON.stringify(parsed));if(best.label==='pending')localStorage.removeItem(best.key)}return parsed}
function recoverPendingWorldJournal(state){const j=state?.journal?.artifactSummon;if(!j||!Number(j.count))return state;const n=Math.max(0,Math.floor(Number(j.count)||0));state.artifactTickets=Math.max(0,Math.floor(Number(state.artifactTickets)||0)+n);state.ticketStats=state.ticketStats||{earned:0,spent:0};state.ticketStats.spent=Math.max(0,Number(state.ticketStats.spent||0)-n);state.journal.artifactSummon=null;state.saveMeta={...(state.saveMeta||{}),schema:9,recoveryCount:Number(state.saveMeta?.recoveryCount||0)+1,recoverySource:'artifact-summon-journal'};return state}
function load(){try{const d=defaults(),cur=worldReadRaw();if(cur){const n={...d,...cur,version:9,saveMeta:{...d.saveMeta,...(cur.saveMeta||{})},journal:{...d.journal,...(cur.journal||{})},artifactTickets:Math.max(0,Number(cur.artifactTickets??d.artifactTickets)||0),ticketStats:{...d.ticketStats,...(cur.ticketStats||{})},ticketClaims:{...d.ticketClaims,...(cur.ticketClaims||{})},shop:{...d.shop,...(cur.shop||{}),purchaseHistory:Array.isArray(cur.shop?.purchaseHistory)?cur.shop.purchaseHistory.slice(0,12):[]},artifactSummon:{...d.artifactSummon,...(cur.artifactSummon||{}),since:{...d.artifactSummon.since,...(cur.artifactSummon?.since||{})}},stats:{...d.stats,...(cur.stats||{})},ui:{...d.ui,...(cur.ui||{})}};return recoverPendingWorldJournal(normalizeArtifactInventory(n))}const old=JSON.parse(localStorage.getItem(OLD)||'null');if(old){const n=recoverPendingWorldJournal(normalizeArtifactInventory({...d,...old,version:9,shop:{...d.shop,...(old.shop||{}),purchaseHistory:[]},artifactSummon:d.artifactSummon,stats:{...d.stats,...(old.stats||{})},ui:d.ui}));worldStorageWrite(KEY,JSON.stringify(n));return n}}catch(e){console.warn('[Artifact inventory migration]',e)}return normalizeArtifactInventory(defaults())}
let S=load(),transactionBusy=false;
function app(){return window.POWDER_APP||null}
function appSave(){return app()?.getSave?.()||{owned:{},team:[],settings:{}}}
function save(reason='auto'){const ok=worldAtomicSave(reason);updateWallet();return ok}
function artifactTicketBalance(){return Math.max(0,Math.floor(Number(S.artifactTickets)||0))}
function grantArtifactTickets(count=1,source='reward',claimId=''){count=Math.max(0,Math.floor(Number(count)||0));if(!count)return 0;const claim=String(claimId||'');S.ticketClaims=S.ticketClaims&&typeof S.ticketClaims==='object'?S.ticketClaims:{};if(claim&&S.ticketClaims[claim])return 0;const before=artifactTicketBalance(),earnedBefore=Number(S.ticketStats?.earned||0);S.artifactTickets=before+count;S.ticketStats=S.ticketStats||{earned:0,spent:0};S.ticketStats.earned=earnedBefore+count;if(claim){S.ticketClaims[claim]={at:Date.now(),count,source:String(source||'reward')};const rows=Object.entries(S.ticketClaims).sort((a,b)=>Number(b[1]?.at||0)-Number(a[1]?.at||0));if(rows.length>180)S.ticketClaims=Object.fromEntries(rows.slice(0,180))}if(save(`ticket-grant:${source}`))return count;S.artifactTickets=before;S.ticketStats.earned=earnedBefore;if(claim)delete S.ticketClaims[claim];return 0}
function spendArtifactTickets(count=1){count=Math.max(0,Math.floor(Number(count)||0));if(!count)return true;if(artifactTicketBalance()<count)return false;const before=artifactTicketBalance(),spentBefore=Number(S.ticketStats?.spent||0);S.artifactTickets=before-count;S.ticketStats=S.ticketStats||{earned:0,spent:0};S.ticketStats.spent=spentBefore+count;if(!save('ticket-spend')){S.artifactTickets=before;S.ticketStats.spent=spentBefore;return false}return true}function reserveArtifactTickets(count=1){count=Math.max(0,Math.floor(Number(count)||0));if(!count)return true;S.journal=S.journal||{artifactSummon:null};if(S.journal.artifactSummon)return false;if(artifactTicketBalance()<count)return false;const before=artifactTicketBalance();S.artifactTickets=before-count;S.ticketStats=S.ticketStats||{earned:0,spent:0};S.ticketStats.spent=Number(S.ticketStats.spent||0)+count;S.journal.artifactSummon={id:`AS-${Date.now().toString(36)}`,count,at:Date.now()};if(save('artifact-summon-reserve'))return true;S.artifactTickets=before;S.ticketStats.spent=Math.max(0,Number(S.ticketStats.spent||0)-count);S.journal.artifactSummon=null;return false}function refundArtifactReservation(count=0){const j=S.journal?.artifactSummon,n=Math.max(0,Math.floor(Number(j?.count||count)||0));if(n){S.artifactTickets=artifactTicketBalance()+n;S.ticketStats=S.ticketStats||{earned:0,spent:0};S.ticketStats.spent=Math.max(0,Number(S.ticketStats.spent||0)-n)}if(S.journal)S.journal.artifactSummon=null;return save('artifact-summon-refund')}function hasTicketClaim(claimId=''){return !!(claimId&&S.ticketClaims?.[String(claimId)])}function recoverCurrencyExchangeJournal(attempt=0){const j=S.journal?.currencyExchange;if(!j)return true;const a=app();if(!a?.getCoinBalance||!a?.creditCoins){if(attempt<30)setTimeout(()=>recoverCurrencyExchangeJournal(attempt+1),120);return false}const current=Math.max(0,Number(a.getCoinBalance())||0),spentThreshold=Math.max(0,Number(j.beforeCoins||0)-Number(j.coinCost||0)),spent=String(j.stage||'prepared')!=='prepared'||current<=spentThreshold;if(!spent){S.journal.currencyExchange=null;save('currency-exchange-recover-clear');return true}if(j.stage!=='applied'){S.elementCoin=Math.max(0,Number(S.elementCoin||0)+Number(j.gain||0));S.stats.earned=Number(S.stats.earned||0)+Number(j.gain||0);if(S.shop.dailyKey===j.dailyKey)S.shop.dailyExchanges=Math.max(Number(S.shop.dailyExchanges||0),Number(j.dailyAfter||0));j.stage='applied';if(!save('currency-exchange-recover-apply'))return false}S.journal.currencyExchange=null;save('currency-exchange-recover-finish');renderShop();a.renderAll?.();toast('Đã hoàn tất giao dịch đổi Tinh Tệ bị gián đoạn.');return true}
function powOf(id){return D.pows.find(p=>p.id===id)||null}function displayPow(id){const p=powOf(id);if(!p)return null;const o=appSave().owned?.[id]||{},f=window.POWDER_FORM_RESOLVER&&p.evolutionForms?.length?window.POWDER_FORM_RESOLVER.formAt(p,o.stars):null;return f?{...p,name:f.name||p.name,asset:f.asset||p.asset,rarity:f.rarity||p.rarity,displayTitle:f.title||p.displayTitle}:p}
function powName(id){return displayPow(id)?.name||id}
function updateWallet(){const el=document.querySelector('#shopCurrencyLabel');if(el)el.textContent=Math.floor(S.elementCoin||0).toLocaleString('vi-VN');document.querySelectorAll('[data-artifact-ticket-balance]').forEach(x=>x.textContent=`${artifactTicketBalance()} vé`)}
function teamPows(){const a=appSave();const ids=(a.team||[]).filter(id=>a.owned?.[id]&&powOf(id));return ids.map(displayPow).filter(Boolean)}
function teamSynergyProfile(){const a=appSave(),pows=(a.team||[]).filter(id=>a.owned?.[id]&&powOf(id)).map(id=>powOf(id));return window.POWDER_TEAM_SYNERGY?.calculate?.(pows)||{active:false,roles:[]}}
function applyTeamSynergyStats(pid,stats){const a=appSave();if(!(a.team||[]).includes(pid))return{...stats};return window.POWDER_TEAM_SYNERGY?.applyStats?.(stats,teamSynergyProfile())||{...stats}}
function teamSynergyMarkup(){const p=teamSynergyProfile();if(!p.active)return'';const rows=[];if(p.element)rows.push(`<div class="ph-synergy-chip element"><b>${esc(p.element.icon)} ${esc(p.element.name)} ×${p.element.count}</b><span>HP · ATK · DEF · AP +${Math.round(p.element.corePct*100)}% · SPEED +${Math.round(p.element.speedPct*100)}%</span></div>`);for(const r of p.roles)rows.push(`<div class="ph-synergy-chip role"><b>${esc(r.label)} ×${r.count}</b><span>${esc(r.text)}</span></div>`);return `<div class="ph-team-synergy"><small>CỘNG HƯỞNG ĐỘI HÌNH</small><div>${rows.join('')}</div></div>`}
function ensureSelected(){const team=teamPows();if(!team.length){S.selectedPow=null;return}if(!team.some(p=>p.id===S.selectedPow))S.selectedPow=team[0].id}
function qty(map,id){return Number(map?.[id]||0)}
function masteryStar(map,id,rarity){return Math.max(0,Math.min(5,Number(S.equipmentEnhance?.[id]||0)))}
function equipmentEnhanceLevel(id){return Math.max(0,Math.min(5,Number(S.equipmentEnhance?.[id]||0)))}
function equipmentEnhanceNeed(level){return [2,3,4,5,6][Math.max(0,Math.min(4,Number(level)||0))]||0}
function equipmentOf(id){const raw=W.equipment.find(x=>x.id===id)||null;if(!raw)return null;const star=equipmentEnhanceLevel(id),base=Number(raw.mainStat?.v8BaseValue??raw.mainStat?.value??0),value=Math.max(1,Math.round(base*(1+star*.08)));return{...raw,v8Stars:star,v8MaxStars:5,enhanceLevel:star,enhanceBonusPct:star*.08,mainStat:raw.mainStat?{...raw.mainStat,value}:raw.mainStat}}
function artifactInstance(ref){return S.artifactInstances?.[ref]||null}
function artifactInstancesById(artifactId){return Object.values(S.artifactInstances||{}).filter(x=>x.artifactId===artifactId)}
function artifactQty(artifactId){return artifactInstancesById(artifactId).length}
function artifactFreeInstances(artifactId,powId=null){return artifactInstancesById(artifactId).filter(x=>!x.equippedBy||x.equippedBy===powId)}
function bestArtifactInstance(artifactId,powId=null){return artifactFreeInstances(artifactId,powId).sort((a,b)=>Number(b.upgradeLevel||0)-Number(a.upgradeLevel||0)||Number(a.acquiredAt||0)-Number(b.acquiredAt||0))[0]||null}
function artifactOf(ref){let inst=artifactInstance(ref),raw=null;if(inst)raw=artifactMaster(inst.artifactId);else{raw=artifactMaster(ref);if(raw)inst=artifactInstancesById(raw.id).sort((a,b)=>Number(b.upgradeLevel||0)-Number(a.upgradeLevel||0))[0]||null}if(!raw)return null;const level=Math.max(0,Math.min(Number(raw.maxUpgrade||ART_MAX_UPGRADE),Number(inst?.upgradeLevel||0))),rate=Number(inst?.upgradeRate??raw.upgradeRate??ART_UPGRADE_RATE[raw.rarity]??0),factor=1+level*rate,base={...(inst?.baseStats||raw.baseStats||raw.statBonusPct||{})},statBonusPct=Object.fromEntries(Object.entries(base).map(([k,v])=>[k,Number(v||0)*factor]));return{...raw,artifactId:raw.id,instanceId:inst?.instanceId||null,upgradeLevel:level,upgradeRate:rate,maxUpgrade:Number(raw.maxUpgrade||ART_MAX_UPGRADE),upgradeBonusPercent:level*rate,baseStats:base,isLocked:!!inst?.isLocked,isFavorite:!!inst?.isFavorite,equippedBy:inst?.equippedBy||null,acquiredAt:inst?.acquiredAt||null,v8Stars:level,v8MaxStars:Number(raw.maxUpgrade||ART_MAX_UPGRADE),effect:raw.effect?{...raw.effect}:raw.effect,statBonusPct}}
function syncArtifactOwned(){rebuildArtifactOwned(S)}
function createArtifactInstance(artifactId,opts={},persistNow=true){const inst=createArtifactInstanceForState(S,artifactId,opts);if(!inst)return null;syncArtifactOwned();if(persistNow)save();return inst}
function artifactUpgradeCost(level){const target=Math.max(1,Math.floor(Number(level)||1));return Math.max(1,Math.floor(Number(ART_DUP_COST[target]??ART_DUP_COST[String(target)]??Math.ceil(target/2))))}
function artifactMaterialCandidates(instanceId){const main=artifactInstance(instanceId);if(!main)return[];return artifactInstancesById(main.artifactId).filter(x=>x.instanceId!==instanceId&&Number(x.upgradeLevel||0)===0&&!x.isLocked&&!x.isFavorite&&!x.equippedBy).sort((a,b)=>Number(a.acquiredAt||0)-Number(b.acquiredAt||0))}
function artifactUpgradePreview(instanceId){const main=artifactInstance(instanceId);if(!main)return null;const raw=artifactMaster(main.artifactId),current=Math.max(0,Number(main.upgradeLevel||0)),max=Number(raw?.maxUpgrade||ART_MAX_UPGRADE),next=Math.min(max,current+1),need=current>=max?0:artifactUpgradeCost(next),materials=artifactMaterialCandidates(instanceId),rate=Number(main.upgradeRate??raw?.upgradeRate??ART_UPGRADE_RATE[raw?.rarity]??0),base={...(main.baseStats||raw?.baseStats||raw?.statBonusPct||{})};const statRows=Object.entries(base).filter(([,v])=>Number.isFinite(Number(v))&&Number(v)!==0).map(([stat,v])=>{const b=Number(v),now=b*(1+current*rate),after=b*(1+next*rate);return{stat,base:b,current:now,next:after,bonusNow:now-b,bonusNext:after-b}});return{instanceId,artifactId:main.artifactId,name:raw?.name||main.artifactId,rarity:raw?.rarity,current,next,max,rate,need,available:materials.length,canUpgrade:current<max&&materials.length>=need,materials:materials.slice(0,need).map(x=>({...x})),statRows}}
function upgradeArtifactInstance(instanceId){return transaction(()=>{const preview=artifactUpgradePreview(instanceId);if(!preview)return false;if(preview.current>=preview.max){toast('Cổ vật đã đạt giới hạn nâng cấp hiện tại.');return false}if(!preview.canUpgrade){toast(`Thiếu ${Math.max(0,preview.need-preview.available)} bản +0 cùng Cổ vật.`);return false}worldBackup('artifact-upgrade');for(const mat of preview.materials)delete S.artifactInstances[mat.instanceId];const main=artifactInstance(instanceId);main.upgradeLevel=preview.next;S.stats.artifactUpgrades=Number(S.stats.artifactUpgrades||0)+1;syncArtifactOwned();if(!save('artifact-upgrade'))return false;renderGear();renderShop();toast(`${preview.name} đã nâng lên +${preview.next}.`);return true})}
function setArtifactProtection(instanceId,field,value){return transaction(()=>{const inst=artifactInstance(instanceId);if(!inst||!['isLocked','isFavorite'].includes(field))return false;inst[field]=value==null?!inst[field]:!!value;if(!save('artifact-protection'))return false;renderGear();renderShop();return true})}
function equippedCount(id,exceptPow=null){let n=0;for(const [pid,slots] of Object.entries(S.powEquipment||{})){if(pid===exceptPow)continue;for(const v of Object.values(slots||{}))if(v===id)n++}return n}
function artifactEquippedCount(ref,exceptPow=null){const target=artifactOf(ref);if(!target)return 0;let n=0;for(const [pid,iid] of Object.entries(S.powArtifact||{})){if(pid===exceptPow)continue;const it=artifactOf(iid);if(it&&(target.instanceId?iid===target.instanceId:it.artifactId===target.artifactId))n++}return n}
function availableEq(id,powId=null){return Math.max(0,qty(S.equipmentOwned,id)-equippedCount(id,powId))}
function availableArt(ref,powId=null){const inst=artifactInstance(ref);if(inst)return(!inst.equippedBy||inst.equippedBy===powId)?1:0;const raw=artifactMaster(ref);return raw?artifactFreeInstances(raw.id,powId).length:0}
function equipmentOwners(id,exceptPow=null){const out=[];for(const [pid,slots] of Object.entries(S.powEquipment||{})){if(pid===exceptPow)continue;if(Object.values(slots||{}).includes(id))out.push(powName(pid))}return out}
function artifactOwners(ref,exceptPow=null){const target=artifactOf(ref),out=[];if(!target)return out;for(const [pid,iid] of Object.entries(S.powArtifact||{})){if(pid===exceptPow)continue;const it=artifactOf(iid);if(it&&(target.instanceId?iid===target.instanceId:it.artifactId===target.artifactId))out.push(powName(pid))}return out}
function rarityChip(r){return `<span class="w8-rarity r-${esc(r)}">${esc(rarityLabel[r]||r)}</span>`}
function toast(t){const e=document.querySelector('#toast');if(!e)return;e.textContent=t;e.hidden=false;clearTimeout(toast._t);toast._t=setTimeout(()=>e.hidden=true,2400)}
function getStats(pid=S.selectedPow){return app()?.getStats?.(pid)||null}
function roleKeyOf(pid){return W.powRoleById?.[pid]?.key||powOf(pid)?.combatRole||powOf(pid)?.roleTags?.[0]||''}
function primaryStatFor(pid){return ['mage','enchanter','healer','musician'].includes(roleKeyOf(pid))?'ap':'atk'}
function applyMainStat(out,it,pid,sign=1){if(!out||!it?.mainStat)return out;let stat=it.mainStat.stat;if(stat==='primary')stat=primaryStatFor(pid);out[stat]=Math.max(1,Math.round((out[stat]||0)+Number(it.mainStat.value||0)*sign));return out}
function rawBaseStats(pid=S.selectedPow){const p=powOf(pid),o=appSave().owned?.[pid]||{};const base=window.POWDER_ENGINE?.getBaseStatsV9;if(base&&p)return{...base(p,o)};const current={...(getStats(pid)||{})};return current}
function setCountsForSlots(slotsMap={}){const c={};for(const id of Object.values(slotsMap||{})){const it=equipmentOf(id);if(it)c[it.setId]=(c[it.setId]||0)+1}return c}
function setStatPctForSlots(slotsMap={}){const counts=setCountsForSlots(slotsMap);let pct=0;for(const [sid,n] of Object.entries(counts)){const set=W.equipmentSets.find(x=>x.id===sid);if(!set)continue;if(n>=4)pct+=Number(set.statBonus4Pct||0);else if(n>=2)pct+=Number(set.statBonus2Pct||0)}return pct}
function artifactStatProfile(artifactId){const it=artifactOf(artifactId);return it?.statBonusPct||null}
function applyPctProfile(out,profile){if(!out||!profile)return out;for(const k of ['hp','atk','def','ap','speed']){const pct=Number(profile[k]||0);if(pct&&out[k]!=null)out[k]=Math.max(1,Math.round(out[k]*(1+pct)))}return out}
function applySetPct(out,pct){if(!out||!pct)return out;for(const k of ['hp','atk','def','ap'])if(out[k]!=null)out[k]=Math.max(1,Math.round(out[k]*(1+pct)));return out}
function flatGearStats(pid,slotsMap={}){const out=rawBaseStats(pid);for(const id of Object.values(slotsMap||{})){const it=equipmentOf(id);if(it)applyMainStat(out,it,pid,1)}return out}
function statsForLoadout(pid,slotsMap={},artifactId=null){const out=flatGearStats(pid,slotsMap),setPct=setStatPctForSlots(slotsMap);applySetPct(out,setPct);applyPctProfile(out,artifactStatProfile(artifactId));return out}
function baseStatsWithoutGear(pid=S.selectedPow){return rawBaseStats(pid)}
function setEffectRating(pid,slotsMap,statPower){const counts=setCountsForSlots(slotsMap),rating={common:{two:.02,four:.035},rare:{two:.025,four:.045},super_rare:{two:.035,four:.06},epic:{two:.045,four:.08},legendary:{two:.07,four:.13},mythic:{two:.10,four:.18},ancient:{two:.14,four:.25}};let total=0;for(const [sid,n] of Object.entries(counts)){const set=W.equipmentSets.find(x=>x.id===sid);if(!set)continue;const q=rating[set.rarity]||rating.common;if(n>=4)total+=statPower*q.four;else if(n>=2)total+=statPower*q.two}return Math.max(0,Math.round(total))}
function artifactEffectRating(pid,artifactId,statPower){const it=artifactOf(artifactId);if(!it)return 0;const baseFactor={common:.015,rare:.025,super_rare:.04,epic:.06,legendary:.10,mythic:.14,ancient:.20}[it.rarity]||.015;return Math.max(1,Math.round(statPower*baseFactor))}
function buildPower(pid=S.selectedPow,slotsMap=S.powEquipment[pid]||{},artifactId=S.powArtifact[pid]||null){const baseStats=rawBaseStats(pid),basePower=powerFromStats(baseStats),flatStats=flatGearStats(pid,slotsMap),flatPower=powerFromStats(flatStats),setStats={...flatStats};applySetPct(setStats,setStatPctForSlots(slotsMap));const setStatPower=Math.max(0,powerFromStats(setStats)-flatPower),artStats={...setStats};applyPctProfile(artStats,artifactStatProfile(artifactId));const artStatPower=Math.max(0,powerFromStats(artStats)-powerFromStats(setStats)),preSynergyPower=powerFromStats(artStats),finalStats=applyTeamSynergyStats(pid,artStats),synergyStatPower=Math.max(0,powerFromStats(finalStats)-preSynergyPower),statPower=powerFromStats(finalStats),setPower=setEffectRating(pid,slotsMap,statPower),artifactEffectPower=artifactEffectRating(pid,artifactId,statPower),artifactPower=artStatPower+artifactEffectPower;return{stats:finalStats,baseStats,basePower,flatPower,gearFlatPower:Math.max(0,flatPower-basePower),setStatPower,artifactStatPower:artStatPower,synergyStatPower,statPower,setPower,artifactEffectPower,artifactPower,total:statPower+setPower+artifactEffectPower}}
function getPowerBreakdown(pid=S.selectedPow){const build=buildPower(pid);return{baseStats:build.baseStats,basePower:build.basePower,gearStatPower:build.gearFlatPower,setStatPower:build.setStatPower,setEffectPower:build.setPower,artifactStatPower:build.artifactStatPower,synergyStatPower:build.synergyStatPower||0,artifactEffectPower:build.artifactEffectPower,artifactPower:build.artifactPower,total:build.total,stats:build.stats}}
function getPower(pid=S.selectedPow){return getPowerBreakdown(pid).total}
function previewEquipmentDelta(it,pid=S.selectedPow){if(!it||!pid)return 0;const before=getPower(pid),slots={...(S.powEquipment[pid]||{})};slots[it.slot]=it.id;return buildPower(pid,slots,S.powArtifact[pid]||null).total-before}
function previewArtifactDelta(it,pid=S.selectedPow){if(!it||!pid)return 0;return buildPower(pid,S.powEquipment[pid]||{},it.instanceId||it.id).total-getPower(pid)}
function artifactStatText(it){const x=artifactOf(it?.instanceId||it?.id||it),p=x?.statBonusPct,b=x?.baseStats;if(!p||!b)return'Không cộng % chỉ số';const fmt=v=>(Number(v||0)*100).toLocaleString('vi-VN',{maximumFractionDigits:2})+'%',core=fmt(p.hp||p.atk||p.def||p.ap||0),baseCore=fmt(b.hp||b.atk||b.def||b.ap||0),spd=Number(p.speed||0),lvl=Number(x.upgradeLevel||0);return `+${lvl} · Base ${baseCore} → ${core} HP/ATK/DEF/AP${spd?` · SPEED ${fmt(spd)}`:''}`}
function roleBuildHint(pid=S.selectedPow){const key=roleKeyOf(pid),map={marksman:'Ưu tiên ATK · CRIT · kết liễu mục tiêu',mage:'Ưu tiên AP · hiệu lực kỹ năng · Mana',tank:'Ưu tiên HP · DEF · Khiên/Bảo hộ',fighter:'Ưu tiên ATK · HP · sức mạnh kéo dài',knight:'Ưu tiên DEF · ATK/AP · bảo vệ tuyến',enchanter:'Ưu tiên AP · SPEED · hiệu lực buff/debuff',healer:'Ưu tiên AP · HP · hồi máu/Khiên',musician:'Ưu tiên SPEED · AP · nhịp lượt/Mana',assassin:'Ưu tiên ATK · CRIT · burst/Xuyên bảo hộ'};return map[key]||'Ưu tiên chỉ số phù hợp bộ kỹ năng của Pow'}
function worldBackup(reason='transaction'){try{const latest=`${KEY}_backup_latest`,previous=`${KEY}_backup_previous`,cur=localStorage.getItem(latest);if(cur)localStorage.setItem(previous,cur);localStorage.setItem(latest,JSON.stringify({at:Date.now(),reason,state:S}))}catch(_){}}
function transaction(fn){if(transactionBusy){toast('Đang xử lý thao tác trước đó...');return false}transactionBusy=true;const snapshot=JSON.stringify(S);try{const result=fn();if(result===false){S=normalizeArtifactInventory(JSON.parse(snapshot));save('transaction-rollback')}return result}catch(err){console.error('[Powder transaction]',err);S=normalizeArtifactInventory(JSON.parse(snapshot));save('transaction-error-rollback');toast('Thao tác gặp lỗi, trạng thái trước đó đã được khôi phục.');return false}finally{transactionBusy=false}}
function equipItem(id){return transaction(()=>{ensureSelected();const item=equipmentOf(id);if(!item)return false;const before=getPower();if(availableEq(id,S.selectedPow)<1){toast('Bản sao này đang được Pow khác sử dụng. Mỗi bản mua chỉ trang bị cho 1 Pow.');return false}S.powEquipment[S.selectedPow]=S.powEquipment[S.selectedPow]||{};S.powEquipment[S.selectedPow][item.slot]=id;if(!save('equipment-equip'))return false;const gain=getPower()-before;renderGear();toast(`Đã trang bị ${item.name}${gain>0?` · +${gain.toLocaleString('vi-VN')} lực chiến`:''}.`);return true})}
function enhanceEquipment(id){return transaction(()=>{const item=equipmentOf(id);if(!item)return false;const level=equipmentEnhanceLevel(id);if(level>=5){toast('Trang bị này đã đạt +5.');return false}const need=equipmentEnhanceNeed(level),keep=Math.max(1,equippedCount(id));const copies=qty(S.equipmentOwned,id);if(copies<keep+need){toast(`Cần ${need} bản sao dư để lên +${level+1}. Hiện có ${copies} bản và phải giữ ${keep} bản đang dùng.`);return false}S.equipmentOwned[id]=copies-need;S.equipmentEnhance=S.equipmentEnhance||{};S.equipmentEnhance[id]=level+1;if(!save('equipment-enhance'))return false;renderGear();renderShop();toast(`${item.name} đã lên +${level+1} · chỉ số chính +${(level+1)*8}%.`);return true})}
function equipArtifact(ref){return transaction(()=>{ensureSelected();let inst=artifactInstance(ref);if(!inst){const raw=artifactMaster(ref);if(raw)inst=bestArtifactInstance(raw.id,S.selectedPow)}if(!inst){toast('Không có bản Cổ vật khả dụng.');return false}if(inst.equippedBy&&inst.equippedBy!==S.selectedPow){toast(`Bản Cổ vật này đang được ${powName(inst.equippedBy)} sử dụng.`);return false}const it=artifactOf(inst.instanceId),before=getPower(),oldRef=S.powArtifact[S.selectedPow];if(oldRef&&S.artifactInstances[oldRef])S.artifactInstances[oldRef].equippedBy=null;inst.equippedBy=S.selectedPow;S.powArtifact[S.selectedPow]=inst.instanceId;if(!save('artifact-equip'))return false;const gain=getPower()-before;renderGear();toast(`Đã trang bị ${it.name} +${it.upgradeLevel}${gain>0?` · +${gain.toLocaleString('vi-VN')} lực chiến`:''}.`);return true})}
function unequip(slot){return transaction(()=>{ensureSelected();if(S.powEquipment[S.selectedPow])delete S.powEquipment[S.selectedPow][slot];if(!save('equipment-unequip'))return false;renderGear();toast('Đã tháo trang bị.');return true})}
function unequipArtifact(){return transaction(()=>{ensureSelected();const ref=S.powArtifact[S.selectedPow];if(ref&&S.artifactInstances[ref])S.artifactInstances[ref].equippedBy=null;delete S.powArtifact[S.selectedPow];if(!save('artifact-unequip'))return false;renderGear();toast('Đã tháo Cổ vật.');return true})}
function priceOf(item){return Math.round(W.economy.rarityPrices[item.rarity]||60)}
function grantCurrency(amount,reason=''){amount=Math.max(0,Math.floor(Number(amount)||0));if(!amount)return 0;const before=Math.max(0,Number(S.elementCoin)||0),earnedBefore=Number(S.stats?.earned||0);S.elementCoin=before+amount;S.stats.earned=earnedBefore+amount;if(!save(`currency-grant:${reason||'reward'}`)){S.elementCoin=before;S.stats.earned=earnedBefore;return 0}if(reason)toast(`+${amount} ${W.economy.currencyName} · ${reason}`);return amount}
function spendCurrency(amount){amount=Math.max(0,Math.floor(Number(amount)||0));if(S.elementCoin<amount)return false;const before=Math.max(0,Number(S.elementCoin)||0),spentBefore=Number(S.stats?.spent||0);S.elementCoin=before-amount;S.stats.spent=spentBefore+amount;if(!save('currency-spend')){S.elementCoin=before;S.stats.spent=spentBefore;return false}return true}
function seedRand(seed){let x=(seed|0)||123456789;return()=>{x^=x<<13;x^=x>>>17;x^=x<<5;return((x>>>0)%1000000)/1000000}}
function random01(){try{if(globalThis.crypto?.getRandomValues){const a=new Uint32Array(1);globalThis.crypto.getRandomValues(a);return a[0]/4294967296}}catch(_){}return Math.random()}
function dayKey(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function syncDailyShop(persistNow=true){const key=dayKey();if(S.shop.dailyKey!==key){S.shop.dailyKey=key;S.shop.dailyRefreshes=0;S.shop.dailyExchanges=0;if(persistNow)save('shop-day-reset')}}
function rankCapRarity(){const rank=Math.max(0,Math.min(6,Number(appSave().rank)||0));return W.economy.rankRarityCap?.[String(rank)]||'common'}
function requiredRankForRarity(r){for(let i=0;i<=6;i++)if(W.economy.rankRarityCap?.[String(i)]===r)return i;return 6}
function rankNameById(id){return D.ranks?.find(r=>Number(r.id)===Number(id))?.name||`Rank ${id}`}
function currentTamerRank(){return Math.max(0,Math.min(6,Number(appSave().rank)||0))}function shopRankBenefits(rank=currentTamerRank()){const srv=S.shop?.serverBenefits;if(srv&&Number(srv.rank)===Number(rank))return{rank:Number(srv.rank),refreshLimit:Number(srv.refresh_limit),exchangeLimit:Number(srv.exchange_limit),coinCost:Number(srv.exchange_coin_cost),currencyGain:Number(srv.exchange_currency_gain)};const rows=[{refreshLimit:2,exchangeLimit:2,coinCost:1500,currencyGain:20},{refreshLimit:2,exchangeLimit:2,coinCost:2500,currencyGain:36},{refreshLimit:3,exchangeLimit:3,coinCost:4000,currencyGain:65},{refreshLimit:3,exchangeLimit:3,coinCost:6000,currencyGain:100},{refreshLimit:4,exchangeLimit:4,coinCost:9000,currencyGain:155},{refreshLimit:4,exchangeLimit:4,coinCost:13000,currencyGain:230},{refreshLimit:5,exchangeLimit:4,coinCost:18000,currencyGain:320}];return{rank,...rows[Math.max(0,Math.min(6,Number(rank)||0))]}}
function artifactRatesForRank(rank=currentTamerRank()){const cfg=W.economy.artifactSummon||{},by=cfg.ratesByTamerRank||{};return by[String(Math.max(0,Math.min(6,Number(rank)||0)))]||cfg.rates||{}}
function formatRate(v){v=Number(v)||0;if(v===0)return '0%';if(v<.1)return v.toFixed(2).replace(/0+$/,'').replace(/\.$/,'')+'%';if(v<1)return v.toFixed(2).replace(/0+$/,'').replace(/\.$/,'')+'%';return (Number.isInteger(v)?v.toFixed(0):v.toFixed(1).replace(/\.0$/,''))+'%'}
function artifactPityUnlocked(r,rank=currentTamerRank()){const cfg=W.economy.artifactSummon||{},need=Number(cfg.pityUnlockRank?.[r]??0),rate=Number(artifactRatesForRank(rank)?.[r]||0);return rank>=need&&rate>0}
function artifactPityUnlockName(r){const cfg=W.economy.artifactSummon||{},need=Number(cfg.pityUnlockRank?.[r]??0),d=D.ranks?.[need];return d?.name||`Rank ${need}`}
function weightedPick(list,rng){const weights=W.economy.equipmentWeights||{};let total=list.reduce((sum,x)=>sum+Number(weights[x.rarity]||1),0),roll=rng()*total;for(const x of list){roll-=Number(weights[x.rarity]||1);if(roll<=0)return x}return list[list.length-1]}
function validShopOffers(){return Array.isArray(S.shop.offers)&&S.shop.offers.length===Number(W.economy.shopOfferCount||8)&&S.shop.offers.every(o=>o.kind==='equipment'&&equipmentOf(o.id))}
function refreshShop(force=false,persistNow=true){syncDailyShop(false);const now=Date.now();if(!force&&validShopOffers()&&now<S.shop.nextRefresh)return false;const period=Number(W.economy.refreshHours||12)*3600000,seed=Math.floor(now/period)+(S.shop.manualRefreshes||0)*7919+(Number(appSave().rank)||0)*104729,rng=seedRand(seed),pool=W.equipment.map(x=>({...x,kind:'equipment'})),picked=[];let guard=0;while(picked.length<Number(W.economy.shopOfferCount||8)&&guard++<1000){const x=weightedPick(pool,rng);if(x&&!picked.some(o=>o.id===x.id))picked.push(x)}const cap=rarityOrder[rankCapRarity()]||0;S.shop.offers=picked.map(x=>({id:x.id,kind:'equipment',stock:1,locked:rarityOrder[x.rarity]>cap}));S.shop.lastRefresh=now;S.shop.nextRefresh=now+period;if(persistNow)save('shop-refresh');return true}
function currentManualRefreshCost(){syncDailyShop(false);return Number(W.economy.manualRefreshCost||60)+Math.max(0,Number(S.shop.dailyRefreshes||0))*40}
function manualRefresh(){return transaction(()=>{syncDailyShop(false);const limit=shopRankBenefits().refreshLimit;if(S.shop.dailyRefreshes>=limit){toast(`Hôm nay đã dùng hết ${limit} lượt làm mới thủ công.`);return false}const cost=currentManualRefreshCost();if(S.elementCoin<cost){toast(`Cần ${cost} ${W.economy.currencyName}.`);return false}worldBackup('manual-refresh');S.elementCoin-=cost;S.stats.spent=Number(S.stats.spent||0)+cost;S.shop.dailyRefreshes++;S.shop.manualRefreshes=(S.shop.manualRefreshes||0)+1;const changed=refreshShop(true,false);if(!changed)return false;if(!save('manual-refresh'))return false;renderShop();toast('Chợ Trang Bị đã đổi sang danh sách mới.');return true})}
function offerItem(o){const x=o?.kind==='equipment'?equipmentOf(o.id):null;return x?{...x,kind:'equipment'}:null}
function buyOffer(idx){return transaction(()=>{refreshShop();const o=S.shop.offers[idx],x=o&&offerItem(o);if(!o||!x||o.stock<=0)return false;const locked=rarityOrder[x.rarity]>rarityOrder[rankCapRarity()];if(locked){const req=requiredRankForRarity(x.rarity);toast(`Cần Tamer ${rankNameById(req)} để mua trang bị ${rarityLabel[x.rarity]}.`);return false}const price=priceOf(x);if(S.elementCoin<price){toast(`Không đủ ${W.economy.currencyName}. Cần ${price} Tinh Tệ.`);return false}worldBackup('equipment-purchase');S.elementCoin-=price;S.stats.spent=Number(S.stats.spent||0)+price;S.equipmentOwned[o.id]=qty(S.equipmentOwned,o.id)+1;o.stock--;S.stats.purchases++;S.shop.purchaseHistory=[{id:o.id,price,at:Date.now()},...(S.shop.purchaseHistory||[])].slice(0,12);if(!save('equipment-purchase'))return false;renderShop();toast(`Đã mua ${x.name}. Bản sao này chỉ có thể gán cho 1 Pow.`);return true})}
function exchangeCoins(){return transaction(()=>{syncDailyShop();const b=shopRankBenefits(),limit=b.exchangeLimit,coinCost=b.coinCost,gain=b.currencyGain;if(S.shop.dailyExchanges>=limit){toast(`Đã đạt giới hạn đổi Tinh Tệ hôm nay (${limit}/${limit}).`);return false}const a=app(),beforeCoins=Math.max(0,Number(a?.getCoinBalance?.()??appSave().coins)||0);if(beforeCoins<coinCost){toast(`Cần ${coinCost.toLocaleString('vi-VN')} Military Coin.`);return false}worldBackup('currency-exchange');S.journal=S.journal||{};S.journal.currencyExchange={id:`CE-${Date.now().toString(36)}`,stage:'prepared',at:Date.now(),beforeCoins,coinCost,gain,dailyKey:S.shop.dailyKey,dailyAfter:Number(S.shop.dailyExchanges||0)+1};if(!save('currency-exchange-prepare'))return false;if(!a?.spendCoins?.(coinCost)){S.journal.currencyExchange=null;save('currency-exchange-cancel');toast('Không thể trừ Military Coin. Hãy thử lại.');return false}S.journal.currencyExchange.stage='coins-spent';if(!save('currency-exchange-coins-spent')){a.creditCoins?.(coinCost,'exchange-rollback');S.journal.currencyExchange=null;save('currency-exchange-rollback');return false}S.shop.dailyExchanges=S.journal.currencyExchange.dailyAfter;S.elementCoin=Math.max(0,Number(S.elementCoin||0)+gain);S.stats.earned=Number(S.stats.earned||0)+gain;S.journal.currencyExchange.stage='applied';if(!save('currency-exchange-applied')){a.creditCoins?.(coinCost,'exchange-rollback');return false}S.journal.currencyExchange=null;save('currency-exchange-finish');renderShop();a.renderAll?.();toast(`Đổi ${coinCost.toLocaleString('vi-VN')} Coin → +${gain} ${W.economy.currencyName} · còn ${limit-S.shop.dailyExchanges} lượt.`);return true})}
function artifactPityMin(){const cfg=W.economy.artifactSummon?.pity||{},since=S.artifactSummon.since||{},rank=currentTamerRank();for(const r of ['ancient','mythic','legendary','epic','super_rare']){if(!artifactPityUnlocked(r,rank))continue;if(Number(since[r]||0)+1>=Number(cfg[r]||Infinity))return r}return null}
function pickArtifactRarity(minRarity=null){const rates=artifactRatesForRank(),min=minRarity==null?0:rarityOrder[minRarity],tiers=Object.keys(rates).filter(r=>Number(rates[r]||0)>0&&rarityOrder[r]>=min),total=tiers.reduce((a,r)=>a+Number(rates[r]||0),0);if(!tiers.length||total<=0)return 'common';let roll=random01()*total;for(const r of tiers){roll-=Number(rates[r]||0);if(roll<=0)return r}return tiers[tiers.length-1]||'common'}
function summonArtifactOne(){const rank=currentTamerRank(),forced=artifactPityMin(),rarity=pickArtifactRarity(forced),pool=W.artifacts.filter(a=>a.rarity===rarity),it=pool[Math.min(pool.length-1,Math.floor(random01()*pool.length))]||W.artifacts[0];const inst=createArtifactInstance(it.id,{},false);S.artifactSummon.totalPulls=Number(S.artifactSummon.totalPulls||0)+1;for(const r of ['super_rare','epic','legendary','mythic','ancient']){if(!artifactPityUnlocked(r,rank)){S.artifactSummon.since[r]=0;continue}S.artifactSummon.since[r]=rarityOrder[rarity]>=rarityOrder[r]?0:Number(S.artifactSummon.since[r]||0)+1}syncArtifactOwned();return{...it,instanceId:inst?.instanceId||null,upgradeLevel:0}}
function summonArtifacts(count=1){const M=configureArtifactModule();return M?.summon?.(count)||[]}function setCounts(powId){const c={};for(const id of Object.values(S.powEquipment[powId]||{})){const it=equipmentOf(id);if(it)c[it.setId]=(c[it.setId]||0)+1}return c}
function activeSetBonuses(powId){const c=setCounts(powId),out=[];for(const [sid,n] of Object.entries(c)){const set=W.equipmentSets.find(s=>s.id===sid);if(!set)continue;if(n>=2)out.push({setId:sid,count:n,tier:2,text:set.bonus2});if(n>=4)out.push({setId:sid,count:n,tier:4,text:set.bonus4})}return out}
function statText(it){const x=it?.id?equipmentOf(it.id):it,s=x?.mainStat;if(!s)return'';const label=s.stat==='primary'?'ATK/AP theo Pow':s.stat.toUpperCase();return `${Number(x?.v8Stars||0)}★ · +${s.value} ${label}`}
function deltaText(n){n=Math.round(Number(n)||0);return `${n>0?'+':''}${n.toLocaleString('vi-VN')} LC`}
function deltaClass(n){return Number(n)>0?'positive':Number(n)<0?'negative':'neutral'}
function setProgressMarkup(pid=S.selectedPow){const counts=setCountsForSlots(S.powEquipment[pid]||{}),rows=Object.entries(counts).map(([sid,n])=>({set:W.equipmentSets.find(x=>x.id===sid),n})).filter(x=>x.set).sort((a,b)=>b.n-a.n||rarityOrder[b.set.rarity]-rarityOrder[a.set.rarity]);if(!rows.length)return '<small>Chưa có mảnh set nào được trang bị.</small>';return rows.map(({set,n})=>`<div class="ph-set-progress-row"><span><b>${esc(set.name)}</b>${rarityChip(set.rarity)}</span><i><em style="width:${Math.min(100,n/4*100)}%"></em></i><strong>${n}/4</strong><small>${n<2?'Còn '+(2-n)+' món để mở 2/4':n<4?`Đã mở 2/4 · +${Math.round(Number(set.statBonus2Pct||0)*100)}% chỉ số lõi · còn ${4-n} món để mở 4/4`:`Đã kích hoạt 4/4 · +${Math.round(Number(set.statBonus4Pct||0)*100)}% HP/ATK/DEF/AP`}</small></div>`).join('')}
function powerBreakdownMarkup(pid=S.selectedPow){const b=getPowerBreakdown(pid);return `<div class="ph-power-breakdown ph-power-breakdown-v951"><div><small>CHỈ SỐ NỀN</small><b>${b.basePower.toLocaleString('vi-VN')}</b><span>Level · Sao · Shiny</span></div><div><small>TRANG BỊ PHẲNG</small><b>+${b.gearStatPower.toLocaleString('vi-VN')}</b><span>4 slot trang bị</span></div><div><small>% CHỈ SỐ SET</small><b>+${b.setStatPower.toLocaleString('vi-VN')}</b><span>Kích hoạt 2/4 · 4/4</span></div><div><small>HIỆU ỨNG SET</small><b>+${b.setEffectPower.toLocaleString('vi-VN')}</b><span>Giá trị chiến thuật</span></div><div><small>% CHỈ SỐ CỔ VẬT</small><b>+${b.artifactStatPower.toLocaleString('vi-VN')}</b><span>Tăng thật trong Combat</span></div><div><small>HIỆU ỨNG CỔ VẬT</small><b>+${b.artifactEffectPower.toLocaleString('vi-VN')}</b><span>Chức năng riêng</span></div>${b.synergyStatPower?`<div><small>CỘNG HƯỞNG ĐỘI</small><b>+${b.synergyStatPower.toLocaleString('vi-VN')}</b><span>Hệ · Nghề</span></div>`:''}</div>`}
function titleOf(p){return p.ancientTitle?`✦ ${p.ancientTitle}`:p.emperorTitle?`♛ ${p.emperorTitle}`:p.legendTitle?`⚔ ${p.legendTitle}`:p.displayTitle?`◆ ${p.displayTitle}`:''}
function starString(n,max){return Array.from({length:max||7},(_,i)=>`<i class="ph-star ${i<n?'on':''}">★</i>`).join('')}
function teamSelector(){const team=teamPows(),a=appSave(),ids=(a.team||[]).filter(id=>a.owned?.[id]&&powOf(id)),active=a.activePowId,total=ids.reduce((sum,id)=>sum+Number(getPowerBreakdown(id)?.total||0),0);return `<div class="ph-squad-command"><div class="ph-squad-summary"><span><b>${ids.length}/5</b> Pow</span><span><b>${Math.min(3,ids.length)}</b> chính</span><span><b>${Math.max(0,ids.length-3)}</b> dự bị</span><span><b>${total.toLocaleString('vi-VN')}</b> lực chiến</span><span class="ph-squad-active">★ Pow chính: <b>${esc(displayPow(active)?.name||'—')}</b></span></div><div class="ph-team-strip ph-team-strip-command">${Array.from({length:5},(_,i)=>{const p=team[i],id=ids[i],o=id?a.owned?.[id]||{}:{},sel=id===S.selectedPow,isActive=id&&id===active;if(!p)return `<article class="ph-team-slot-card empty ${i<3?'main':'reserve'}"><span class="ph-position">${i<3?'CHÍNH':'DỰ BỊ'} ${i+1}</span><button class="ph-team-add" data-ph-team-add="${i}"><b>＋</b><small>Thêm Pow</small></button></article>`;return `<article class="ph-team-slot-card ${sel?'selected':''} ${isActive?'active':''} ${i<3?'main':'reserve'}"><button class="ph-team-pow" data-ph-pow="${esc(id)}"><span class="ph-position">${i<3?'CHÍNH':'DỰ BỊ'} ${i+1}</span>${isActive?'<i class="ph-main-badge">★ POW CHÍNH</i>':''}<img src="${esc(powThumb(p.asset))}" alt="${esc(p.name)}"><span class="ph-team-copy"><b>${esc(p.name)}</b><small>${esc(p.rolePrimary||p.role)} · Lv.${o.level||1} · ${o.stars||0}★</small></span></button><div class="ph-team-quick-actions"><button data-ph-team-left="${i}" ${i===0?'disabled':''} title="Đổi sang trái">←</button><button data-ph-team-active="${esc(id)}" ${isActive?'disabled':''} title="Đặt làm Pow chính">★</button><button data-ph-team-remove="${i}" ${ids.length<=1?'disabled':''} title="Rời đội">×</button><button data-ph-team-right="${i}" ${i>=ids.length-1?'disabled':''} title="Đổi sang phải">→</button></div></article>`}).join('')}</div></div>${teamSynergyMarkup()}`}
const teamEditor={search:'',rarity:'all',element:'all',role:'all',slot:0};
function ownedTeamEditorPows(){const a=appSave(),q=String(teamEditor.search||'').trim().toLowerCase();return D.pows.filter(p=>a.owned?.[p.id]).map(p=>displayPow(p.id)||p).filter(p=>teamEditor.rarity==='all'||p.rarity===teamEditor.rarity).filter(p=>teamEditor.element==='all'||p.element===teamEditor.element).filter(p=>teamEditor.role==='all'||String(p.rolePrimary||p.role)===teamEditor.role).filter(p=>!q||`${p.name} ${p.rolePrimary||p.role} ${rarityLabel[p.rarity]||p.rarity} ${D.elements?.[p.element]?.name||p.element}`.toLowerCase().includes(q)).sort((a1,b1)=>{const ta=(a.team||[]).indexOf(a1.id),tb=(a.team||[]).indexOf(b1.id);if(ta>=0&&tb<0)return-1;if(tb>=0&&ta<0)return 1;return rarityOrder[b1.rarity]-rarityOrder[a1.rarity]||a1.name.localeCompare(b1.name,'vi')})}
function teamEditorCommit(ids,activePowId){const result=app()?.setTeam?.(ids,activePowId);if(!result){toast('Không thể đổi đội hình trong lúc Combat hoặc đội hình không hợp lệ.');return false}ensureSelected();return true}
function teamEditorMove(index,delta){const a=appSave(),team=[...(a.team||[])],j=index+delta;if(index<0||j<0||j>=team.length)return;[team[index],team[j]]=[team[j],team[index]];if(teamEditorCommit(team,a.activePowId)){teamEditor.slot=j;renderTeamEditor()}}
function teamEditorRemove(index){const a=appSave(),team=[...(a.team||[])];if(team.length<=1){toast('Đội hình phải có ít nhất 1 Pow.');return}team.splice(index,1);if(teamEditorCommit(team,a.activePowId)){teamEditor.slot=Math.min(teamEditor.slot,team.length-1);renderTeamEditor()}}
function teamEditorPlace(powId){const a=appSave(),team=[...(a.team||[])],existing=team.indexOf(powId),slot=Math.max(0,Math.min(4,Number(teamEditor.slot)||0));if(existing===slot){teamEditor.slot=slot;renderTeamEditor();return}if(existing>=0){if(slot<team.length){[team[existing],team[slot]]=[team[slot],team[existing]]}else{team.splice(existing,1);team.push(powId)}}else if(slot<team.length){team[slot]=powId}else if(team.length<5){team.push(powId)}else{toast('Đội đã đủ 5 Pow. Chọn một ô đội hình để thay thế.');return}if(teamEditorCommit(team,a.activePowId)){teamEditor.slot=Math.min(slot,team.length-1);renderTeamEditor()}}
function renderTeamEditor(){const modal=document.querySelector('#phTeamEditor');if(!modal)return;const a=appSave(),team=(a.team||[]).filter(id=>a.owned?.[id]&&powOf(id)),owned=ownedTeamEditorPows(),roles=[...new Set(D.pows.filter(p=>a.owned?.[p.id]).map(p=>String(p.rolePrimary||p.role)).filter(Boolean))].sort((x,y)=>x.localeCompare(y,'vi')),totalPower=team.reduce((sum,id)=>sum+getPowerBreakdown(id).total,0);modal.innerHTML=`<div class="ph-team-editor-shell"><header><div><small>SQUAD COMMAND</small><h2>Chỉnh đội hình</h2><p>Chỉ quản lý đội hình · không chọn kỹ năng tại cửa sổ này.</p></div><button class="ph-team-editor-close" data-ph-team-close aria-label="Đóng">×</button></header><section class="ph-team-editor-summary"><div><small>ĐỘI HÌNH</small><b>${team.length}/5 Pow</b></div><div><small>POW CHÍNH</small><b>${Math.min(3,team.length)}</b></div><div><small>DỰ BỊ</small><b>${Math.max(0,team.length-3)}</b></div><div><small>TỔNG LỰC CHIẾN</small><b>${totalPower.toLocaleString('vi-VN')}</b></div></section>${teamSynergyMarkup()}<section class="ph-team-editor-slots">${Array.from({length:5},(_,i)=>{const id=team[i],p=displayPow(id),o=a.owned?.[id]||{},active=id&&id===a.activePowId,selected=i===teamEditor.slot;return `<article class="ph-team-slot ${selected?'selected':''} ${id?'filled':'empty'} ${i<3?'main':'reserve'}" data-ph-team-slot="${i}"><span class="ph-team-slot-no">${i+1}</span><small>${i<3?'⚔ POW CHÍNH':'↺ DỰ BỊ'}</small>${p?`<img src="${esc(powThumb(p.asset))}" alt="${esc(p.name)}" loading="lazy" decoding="async"><b>${esc(p.name)}</b><em>${esc(p.rolePrimary||p.role)} · Lv.${o.level||1} · ${o.stars||0}★</em>${active?'<strong>POW ĐANG CHỌN</strong>':''}<div class="ph-team-slot-actions"><button data-ph-team-left="${i}" ${i===0?'disabled':''}>←</button><button data-ph-team-active="${esc(id)}" ${active?'disabled':''}>★</button><button data-ph-team-remove="${i}" ${team.length<=1?'disabled':''}>×</button><button data-ph-team-right="${i}" ${i>=team.length-1?'disabled':''}>→</button></div>`:`<div class="ph-team-slot-empty">＋<span>Chọn Pow bên dưới</span></div>`}</article>`}).join('')}</section><section class="ph-team-editor-tools"><input data-ph-team-search value="${esc(teamEditor.search)}" placeholder="Tìm tên Pow, vai trò, nguyên tố..."><select data-ph-team-rarity><option value="all">Tất cả phẩm chất</option>${D.rarities.map(r=>`<option value="${esc(r.id)}" ${teamEditor.rarity===r.id?'selected':''}>${esc(r.name)}</option>`).join('')}</select><select data-ph-team-element><option value="all">Tất cả nguyên tố</option>${Object.entries(D.elements||{}).map(([id,e])=>`<option value="${esc(id)}" ${teamEditor.element===id?'selected':''}>${esc(e.icon||'◈')} ${esc(e.name||id)}</option>`).join('')}</select><select data-ph-team-role><option value="all">Tất cả vai trò</option>${roles.map(r=>`<option value="${esc(r)}" ${teamEditor.role===r?'selected':''}>${esc(r)}</option>`).join('')}</select></section><section class="ph-team-editor-owned">${owned.length?owned.map(p=>{const o=a.owned?.[p.id]||{},idx=team.indexOf(p.id),inTeam=idx>=0,active=p.id===a.activePowId,elem=D.elements?.[p.element]||{};return `<button class="ph-owned-pow ${inTeam?'in-team':''} ${active?'active':''}" data-ph-team-pow="${esc(p.id)}"><div><img src="${esc(powThumb(p.asset))}" alt="${esc(p.name)}" loading="lazy" decoding="async">${inTeam?`<i>${idx<3?'CHÍNH':'DỰ BỊ'} ${idx+1}</i>`:''}</div><span><small>${esc(rarityLabel[p.rarity]||p.rarity)} · ${esc(elem.icon||'◈')} ${esc(elem.name||p.element)}</small><b>${esc(p.name)}</b><em>${esc(p.rolePrimary||p.role)} · Lv.${o.level||1} · ${o.stars||0}★</em></span>${active?'<strong>★ POW CHÍNH</strong>':inTeam?'<strong>TRONG ĐỘI</strong>':'<strong>CHỌN</strong>'}</button>`}).join(''):'<div class="ph-team-editor-empty">Không tìm thấy Pow phù hợp bộ lọc.</div>'}</section><footer><span>Chọn ô 1–5 rồi chọn Pow để thay trực tiếp. Thay đổi được lưu ngay.</span><button class="btn primary" data-ph-team-done>Hoàn tất</button></footer></div>`;modal.querySelector('[data-ph-team-close]').onclick=closeTeamEditor;modal.querySelector('[data-ph-team-done]').onclick=closeTeamEditor;modal.querySelectorAll('[data-ph-team-slot]').forEach(el=>el.onclick=e=>{if(e.target.closest('button'))return;teamEditor.slot=Number(el.dataset.phTeamSlot)||0;renderTeamEditor()});modal.querySelectorAll('[data-ph-team-pow]').forEach(b=>b.onclick=()=>teamEditorPlace(b.dataset.phTeamPow));modal.querySelectorAll('[data-ph-team-left]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditorMove(Number(b.dataset.phTeamLeft),-1)});modal.querySelectorAll('[data-ph-team-right]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditorMove(Number(b.dataset.phTeamRight),1)});modal.querySelectorAll('[data-ph-team-remove]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditorRemove(Number(b.dataset.phTeamRemove))});modal.querySelectorAll('[data-ph-team-active]').forEach(b=>b.onclick=e=>{e.stopPropagation();const current=appSave();if(teamEditorCommit(current.team||[],b.dataset.phTeamActive))renderTeamEditor()});const search=modal.querySelector('[data-ph-team-search]');search.oninput=()=>{teamEditor.search=search.value;clearTimeout(renderTeamEditor._search);renderTeamEditor._search=setTimeout(renderTeamEditor,120)};modal.querySelector('[data-ph-team-rarity]').onchange=e=>{teamEditor.rarity=e.target.value;renderTeamEditor()};modal.querySelector('[data-ph-team-element]').onchange=e=>{teamEditor.element=e.target.value;renderTeamEditor()};modal.querySelector('[data-ph-team-role]').onchange=e=>{teamEditor.role=e.target.value;renderTeamEditor()};}
function openTeamEditor(){if(window.POWDER_COMBAT_ENTRY_V177?.isActive?.()){toast('Không thể chỉnh đội hình khi Combat đang diễn ra.');return}let modal=document.querySelector('#phTeamEditor');if(!modal){modal=document.createElement('section');modal.id='phTeamEditor';modal.className='ph-team-editor-modal';modal.onclick=e=>{if(e.target===modal)closeTeamEditor()};document.body.appendChild(modal)}const a=appSave(),team=a.team||[];teamEditor.slot=Math.max(0,Math.min(4,team.length<5?team.length:0));modal.hidden=false;document.documentElement.classList.add('ph-team-editor-open');renderTeamEditor()}
function closeTeamEditor(){const modal=document.querySelector('#phTeamEditor');if(modal)modal.hidden=true;document.documentElement.classList.remove('ph-team-editor-open');ensureSelected();renderGear()}
function equipmentTeamRail(){const team=teamPows(),a=appSave();if(!team.length)return'';return `<aside class="eq-team-rail"><div class="eq-rail-title"><small>ĐỘI HÌNH</small><b>${team.length}/5 Pow</b></div><div class="eq-team-list">${team.map((p,i)=>{const o=a.owned?.[p.id]||{},sel=p.id===S.selectedPow;return `<button class="eq-team-unit ${sel?'selected':''}" data-ph-pow="${esc(p.id)}" title="${esc(p.name)}"><span>${i+1}</span><img src="${esc(powThumb(p.asset))}" alt="${esc(p.name)}" loading="lazy" decoding="async"><i></i><b>${esc(p.name)}</b><small>Lv.${o.level||1} · ${o.stars||0}★</small></button>`}).join('')}</div></aside>`}
function equipmentSlotCard(slot){const id=S.powEquipment[S.selectedPow]?.[slot],it=equipmentOf(id),label=W.slots[slot]||slot;if(!it)return `<article class="eq-gear-slot empty" data-eq-slot-target="${slot}"><div class="eq-slot-icon">＋</div><div class="eq-slot-copy"><small>${esc(label)}</small><b>Chưa trang bị</b><span>Bấm để lọc ${esc(label.toLowerCase())}</span></div></article>`;return `<article class="eq-gear-slot r-${esc(it.rarity)}" data-eq-slot-target="${slot}"><div class="eq-slot-art"><img src="${esc(itemThumb(it.asset))}" alt="${esc(it.name)}" decoding="async"></div><div class="eq-slot-copy"><small>${esc(label)} · ${esc(it.setName)}</small><b>${esc(it.name)}</b><span>${esc(statText(it))}</span>${rarityChip(it.rarity)}</div><button class="eq-slot-remove" data-w9-unequip="${slot}" title="Tháo ${esc(label)}">×</button></article>`}
function equipmentPowCenter(){const p=displayPow(S.selectedPow),a=appSave(),o=a.owned?.[S.selectedPow]||{},elem=D.elements?.[p?.element]||{},breakdown=getPowerBreakdown();if(!p)return'';return `<section class="eq-pow-center"><div class="eq-pow-heading"><button class="eq-cycle" data-eq-cycle="-1" aria-label="Pow trước">‹</button><div><small>${esc(rarityLabel[p.rarity]||p.rarity)} · ${esc(elem.name||p.element)}</small><h2>${esc(p.name)}</h2><span>${esc(p.rolePrimary||p.role)} · Lv.${o.level||1} · ${o.stars||0}/${p.maxStars||7}★</span></div><button class="eq-cycle" data-eq-cycle="1" aria-label="Pow sau">›</button></div><div class="eq-stage-grid"><div class="eq-left-slots">${equipmentSlotCard('weapon')}${equipmentSlotCard('pants')}</div><div class="eq-pow-portrait"><div class="eq-pow-aura"></div><img src="${esc(p.asset)}" alt="${esc(p.name)}" decoding="async"><span>${esc(titleOf(p))}</span><strong>⚔ ${breakdown.total.toLocaleString('vi-VN')}</strong></div><div class="eq-right-slots">${equipmentSlotCard('armor')}${equipmentSlotCard('shoes')}</div></div><div class="eq-center-stats">${[['HP',breakdown.stats?.hp],['ATK',breakdown.stats?.atk],['DEF',breakdown.stats?.def],['AP',breakdown.stats?.ap],['SPD',breakdown.stats?.speed]].map(([k,v])=>`<div><small>${k}</small><b>${Number(v||0).toLocaleString('vi-VN')}</b></div>`).join('')}</div></section>`}
function equipRecommendedItem(){const slot=S.ui.equipSlot||'all',current=S.powEquipment[S.selectedPow]||{},pool=W.equipment.filter(x=>qty(S.equipmentOwned,x.id)>0&&(slot==='all'||x.slot===slot)).map(it=>({it,free:availableEq(it.id,S.selectedPow),delta:previewEquipmentDelta(it)})).filter(x=>x.free>0&&current[x.it.slot]!==x.it.id&&x.delta>0).sort((a,b)=>b.delta-a.delta);if(!pool.length)return toast('Chưa có trang bị nào mạnh hơn cho Pow này.');equipItem(pool[0].it.id)}

function statGrid(stats){if(!stats)return'';const rows=[['HP',stats.hp],['ATK',stats.atk],['DEF',stats.def],['AP',stats.ap],['SPEED',stats.speed],['CRIT RATE',(stats.critRate||0)+'%'],['CRIT DMG',(stats.critDamage||0)+'%']];return `<div class="ph-stat-grid">${rows.map(([k,v])=>`<div><small>${k}</small><b>${Number.isFinite(v)?Number(v).toLocaleString('vi-VN'):esc(v)}</b></div>`).join('')}</div>`}
function progressData(){const a=appSave(),p=powOf(S.selectedPow),o=a.owned?.[S.selectedPow]||{},prog=app()?.getPowProgress?.(S.selectedPow)||{level:o.level||1,powExp:o.powExp||0,cap:10,next:1,candy:a.powCandy||0,candies:candyWallet(a)};const max=p?.maxStars||7,stars=Number(o.stars||0),target=stars+1,costC=C.starUpgrade.coinByTargetStar[target]||0,costS=C.starUpgrade.shardsByTargetStar[target]||0;return{a,p,o,prog,max,stars,target,costC,costS,locked:(a.lockedPows||[]).includes(S.selectedPow)}}
function overviewTab(){const {a,p,o,prog,max,stars,target,costC,costS,locked}=progressData();if(!p)return'';const stats=getStats(),breakdown=getPowerBreakdown(),power=breakdown.total,hint=roleBuildHint(),pct=prog.level>=prog.cap?100:Math.max(0,Math.min(100,(prog.powExp/Math.max(1,prog.next))*100)),cw=prog.candies||candyWallet(a);const canStar=!locked&&stars<max&&prog.level>=prog.cap&&Number(a.coins||0)>=costC&&Number(o.shards||0)>=costS;const starReason=locked?'Pow đang khóa':stars>=max?'Đã đạt giới hạn sao':prog.level<prog.cap?`Cần đạt Lv.${prog.cap}`:Number(a.coins||0)<costC||Number(o.shards||0)<costS?'Chưa đủ Coin hoặc mảnh Pow':`Nâng lên ${target}★`;const candyCards=Object.entries(candyMeta).map(([type,m])=>`<article class="ph-candy-tier"><img src="${m.asset}" alt="${m.name}"><div><small>${m.name}</small><b>x${Number(cw[type]||0).toLocaleString('vi-VN')}</b><span>+${m.xp.toLocaleString('vi-VN')} EXP / viên</span></div><button data-ph-feed-candy="${type}" data-count="1" ${prog.level>=prog.cap||!cw[type]?'disabled':''}>Dùng ×1</button><button data-ph-feed-candy="${type}" data-count="max" ${prog.level>=prog.cap||!cw[type]?'disabled':''}>Tối đa</button></article>`).join('');
return `<div class="ph-overview-grid"><section class="ph-card ph-progress-card"><div class="ph-card-head"><div><small>HUẤN LUYỆN LEVEL</small><h3>Lv.${prog.level} / ${prog.cap}</h3></div><span class="ph-candy-total">3 loại PowCandy</span></div><div class="ph-xp"><i style="width:${pct}%"></i></div><div class="ph-xp-meta"><span>${prog.level>=prog.cap?'Đã đạt giới hạn Level hiện tại':`${prog.powExp}/${prog.next} Pow EXP`}</span><span>Thường 60 · Hiếm 300 · Huyền thoại 1.200 EXP</span></div><div class="ph-candy-tier-grid">${candyCards}</div><div class="ph-candy-buy"><small>ĐỔI KẸO THƯỜNG BẰNG MILITARY COIN</small><button data-ph-buy-candy="1">×1 · ${currencyIcon('coin')} 150</button><button data-ph-buy-candy="10">×10 · ${currencyIcon('coin')} 1.350</button><button data-ph-buy-candy="50">×50 · ${currencyIcon('coin')} 6.000</button></div></section><section class="ph-card ph-star-card"><div class="ph-card-head"><div><small>THỨC TỈNH SAO</small><h3>${stars}/${max}★</h3></div><span>🧩 ${Number(o.shards||0).toLocaleString('vi-VN')}</span></div><div class="ph-stars">${starString(stars,max)}</div><p>Mỗi lần nâng sao mở giới hạn Level mới và tăng chỉ số nền theo hệ thống tiến triển Pow.</p><div class="ph-star-cost"><span>${currencyIcon('coin')} ${costC.toLocaleString('vi-VN')}</span><span>🧩 ${costS.toLocaleString('vi-VN')}</span></div><button class="ph-star-upgrade" data-ph-star ${canStar?'':'disabled'}>${esc(starReason)}</button></section></div><section class="ph-card ph-stats-card"><div class="ph-card-head"><div><small>CHỈ SỐ CUỐI CÙNG</small><h3>Lực chiến tổng ${power.toLocaleString('vi-VN')}</h3></div><span class="ph-live-badge">BUILD INTELLIGENCE</span></div>${statGrid(stats)}${powerBreakdownMarkup()}<div class="ph-build-hint"><small>ĐỊNH HƯỚNG ROLE</small><b>${esc(hint)}</b><span>Lực chiến Set/Cổ vật là điểm chiến thuật ước tính để so build; Combat vẫn dùng đúng effect MASTER.</span></div><div class="ph-power-sources"><span>Level <b>${prog.level}</b></span><span>Sao <b>${stars}★</b></span><span>Trang bị <b>${Object.keys(S.powEquipment[S.selectedPow]||{}).length}/4</b></span><span>Cổ vật <b>${S.powArtifact[S.selectedPow]?'1/1':'0/1'}</b></span></div></section>`}function equipmentTab(){const slot=S.ui.equipSlot||'all',current=S.powEquipment[S.selectedPow]||{},allOwned=W.equipment.filter(x=>qty(S.equipmentOwned,x.id)>0),owned=allOwned.filter(x=>slot==='all'||x.slot===slot).sort((a,b)=>rarityOrder[b.rarity]-rarityOrder[a.rarity]||a.setId.localeCompare(b.setId)),bonuses=activeSetBonuses(S.selectedPow),scored=owned.map(it=>({it,free:availableEq(it.id,S.selectedPow),delta:previewEquipmentDelta(it)})),best=scored.filter(x=>x.free>0&&current[x.it.slot]!==x.it.id).sort((a,b)=>b.delta-a.delta)[0],bestId=best?.delta>0?best.it.id:null,p=displayPow(S.selectedPow),equipped=Object.keys(current).length;return `<div class="eq-command-center">${equipmentTeamRail()}<main class="eq-main-stage"><div class="eq-command-head"><div><p class="eyebrow">TRUNG TÂM TRANG BỊ</p><h2>Trang bị cho ${esc(p?.name||'Pow')}</h2><span>Chỉ hiển thị một Pow để tập trung build · ${equipped}/4 ô đã trang bị</span></div><div class="eq-head-actions"><button data-ph-team-edit>👥 Chỉnh đội hình</button><button data-eq-recommended ${bestId?'':'disabled'}>⚡ Trang bị món đề xuất</button><button data-ph-shop>🏪 Đến Chợ</button></div></div>${equipmentPowCenter()}<section class="eq-set-panel"><div class="eq-set-head"><div><small>HIỆU ỨNG SET</small><b>Ghép 2/4 và 4/4</b></div><span>${bonuses.length?`${bonuses.length} bonus đang hoạt động`:'Chưa kích hoạt bonus'}</span></div><div class="eq-set-progress">${setProgressMarkup()}</div>${bonuses.length?`<div class="eq-active-bonuses">${bonuses.map(b=>`<div><b>${esc(W.equipmentSets.find(s=>s.id===b.setId)?.name)} ${b.tier}/4</b><span>${esc(b.text)}</span></div>`).join('')}</div>`:''}</section></main><aside class="eq-bank"><div class="eq-bank-head"><div><small>KHO TRANG BỊ</small><h3>${owned.length} món phù hợp</h3></div><span>${allOwned.length} loại đang sở hữu</span></div><div class="eq-slot-filter">${[['all','Tất cả'],['weapon','Vũ khí'],['armor','Áo'],['pants','Quần'],['shoes','Giày']].map(([v,l])=>`<button data-ph-slot="${v}" class="${slot===v?'active':''}">${l}</button>`).join('')}</div><div class="eq-bank-note">Bấm một ô trang bị quanh Pow để lọc đúng vị trí. Mỗi món hiển thị chênh lệch lực chiến trước khi thay.</div><div class="eq-item-list">${scored.length?scored.map(({it,free,delta})=>{const owners=equipmentOwners(it.id,S.selectedPow),isCurrent=current[it.slot]===it.id,recommended=bestId===it.id,currentIt=equipmentOf(current[it.slot]);return `<button class="eq-item ${!free?'unavailable':''} ${isCurrent?'current':''} ${recommended?'recommended':''}" data-w9-equip="${esc(it.id)}" ${!free||isCurrent?'disabled':''}><div class="eq-item-art"><img src="${esc(itemThumb(it.asset))}" alt="${esc(it.name)}" loading="lazy" decoding="async">${recommended?'<i>ĐỀ XUẤT</i>':''}</div><div class="eq-item-info"><small>${esc(it.slotName)} · ${esc(it.setName)}</small><b>${esc(it.name)}</b><span>${esc(statText(it))}</span><em>${currentIt?`${esc(statText(currentIt))} → ${esc(statText(it))}`:`Ô trống → ${esc(statText(it))}`}</em><strong class="ph-delta ${deltaClass(delta)}">${isCurrent?'ĐANG DÙNG':free?deltaText(delta):'ĐÃ GÁN'}</strong><u>${free?`Khả dụng ${free}/×${qty(S.equipmentOwned,it.id)}`:`Đang dùng: ${esc(owners.join(', ')||'Pow khác')}`}</u></div>${rarityChip(it.rarity)}</button>`}).join(''):'<div class="ph-empty">Chưa có trang bị phù hợp. Hãy mua tại Cửa hàng.</div>'}</div></aside></div>`}
function artifactTab(){const artRef=S.powArtifact[S.selectedPow],art=artifactOf(artRef),instances=Object.values(S.artifactInstances||{}).map(x=>artifactOf(x.instanceId)).filter(Boolean).sort((a,b)=>rarityOrder[b.rarity]-rarityOrder[a.rarity]||Number(b.upgradeLevel||0)-Number(a.upgradeLevel||0)||a.order-b.order),scored=instances.map(it=>({it,free:availableArt(it.instanceId,S.selectedPow),delta:previewArtifactDelta(it)})),best=scored.filter(x=>x.free>0&&x.it.instanceId!==artRef).sort((a,b)=>b.delta-a.delta)[0],bestId=best?.delta>0?best.it.instanceId:null,currentPower=getPowerBreakdown().artifactPower;return `<div class="ph-art-grid"><section class="ph-card ph-art-equipped"><div class="ph-card-head"><div><small>CỔ VẬT ĐANG DÙNG</small><h3>1 slot · mỗi bản là một Cổ vật riêng</h3></div><span>Nâng cấp bằng bản +0 cùng ArtifactID</span></div>${art?`<div class="ph-art-focus"><img src="${esc(art.asset)}"><div><b>${esc(art.name)} +${art.upgradeLevel}</b>${rarityChip(art.rarity)}<p class="ph-art-stat-line">${esc(artifactStatText(art))}</p><p>${esc(art.effectText)}</p><strong class="ph-art-power">+${currentPower.toLocaleString('vi-VN')} LC tổng từ Cổ vật</strong><small>Hiệu ứng đặc biệt không tăng theo + · ${esc(art.limit||'')}</small><div class="ph-art-actions"><button data-artifact-detail="${esc(art.instanceId)}">Nâng cấp / Bảo vệ</button><button data-w9-artifact-remove>Tháo</button></div></div></div>`:`<div class="ph-empty ph-empty-big">Chưa trang bị Cổ vật.</div>`}</section><section class="ph-card ph-bank"><div class="ph-card-head"><div><small>KHO CỔ VẬT</small><h3>${instances.length} bản · ${Object.keys(S.artifactOwned||{}).length}/49 loại</h3></div><button class="ph-shop-link" data-ph-artifact-summon>Đến Triệu Hồi</button></div><div class="ph-art-bank">${scored.length?scored.map(({it,free,delta})=>{const isCurrent=artRef===it.instanceId,recommended=bestId===it.instanceId,protectedText=[it.isLocked?'🔒 Khóa':'',it.isFavorite?'★ Yêu thích':'',it.equippedBy&&!isCurrent?`Đang dùng: ${powName(it.equippedBy)}`:''].filter(Boolean).join(' · ');return `<article class="ph-art-item ${!free?'unavailable':''} ${isCurrent?'current':''} ${recommended?'recommended':''}"><button class="ph-art-equip-main" data-w9-artifact="${esc(it.instanceId)}" ${!free||isCurrent?'disabled':''}><img src="${esc(itemThumb(it.asset))}"><div><b>${esc(it.name)} +${it.upgradeLevel}</b>${rarityChip(it.rarity)}<p class="ph-art-stat-line">${esc(artifactStatText(it))}</p><p>${esc(it.effectText)}</p><strong class="ph-delta ${deltaClass(delta)}">${isCurrent?'ĐANG DÙNG':free?deltaText(delta):'ĐÃ GÁN'}</strong><small>${protectedText||`ID ${esc(it.instanceId.slice(-8))}`}</small></div>${recommended?'<i class="ph-recommend">ĐỀ XUẤT</i>':''}</button><button class="ph-art-detail-btn" data-artifact-detail="${esc(it.instanceId)}">Chi tiết · Nâng cấp</button></article>`}).join(''):'<div class="ph-empty">Chưa có Cổ vật. Hãy đến Triệu Hồi Cổ Vật.</div>'}</div></section></div>`}
function skillsTab(){const p=powOf(S.selectedPow),ab=p?.abilities||{};if(!p)return'';return `<section class="ph-card ph-skills"><div class="ph-card-head"><div><small>KỸ NĂNG CHIẾN ĐẤU</small><h3>${esc(p.name)}</h3></div><span>Bộ kỹ năng riêng và cố định của từng Pow</span></div><div class="inventory-ability-list rarity-${esc(p.rarity)}" data-skill-pow="${esc(p.id)}"><div class="skill-board-head"><h3>Bảng chiêu Pow</h3><span class="fixed-kit-badge">BỘ KỸ NĂNG RIÊNG · KHÔNG THỂ CHUYỂN</span></div><div class="pow-skill-board ${p.exclusiveSkill?'has-exclusive':''}"></div><div class="pow-passive-card"><div class="pow-passive-layout ${p.passiveArt?'has-passive-art':''}">${p.passiveArt?`<div class="pow-passive-art"><img src="${esc(p.passiveArt)}" alt="Nội tại ${esc(p.name)}" loading="lazy" decoding="async"></div>`:''}<div class="pow-passive-copy"><b>NỘI TẠI ĐỘC LẬP</b><small>${esc(ab.passive?.description||p.coreMechanic||'Core riêng của Pow.')}</small></div></div></div></div></section>`}
function renderGear(){const root=document.querySelector('#gearView');if(!root)return;ensureSelected();const p=displayPow(S.selectedPow),a=appSave(),o=a.owned?.[S.selectedPow]||{},stats=getStats(),power=getPower(),elem=D.elements?.[p?.element]||{},tab=S.ui.tab||'overview';if(!p){root.innerHTML='<div class="ph-empty">Không có Pow để cường hóa.</div>';return}const tabs=`<nav class="ph-tabs">${[['overview','Tổng quan & Nâng cấp'],['equipment','Trang bị'],['artifact','Cổ vật'],['skills','Kỹ năng']].map(([id,l])=>`<button data-ph-tab="${id}" class="${tab===id?'active':''}">${l}</button>`).join('')}</nav>`;if(tab==='equipment'){root.innerHTML=`<div class="ph-page ph-equipment-page"><div class="section-title ph-title eq-page-title"><div><p class="eyebrow">POW POWER HUB</p><h1>Điện Cường Hóa</h1></div><p>Trang bị được tổ chức theo một Pow mỗi lần: chọn Pow → xem 4 ô → so sánh kho → thay đồ.</p></div>${tabs}<div class="ph-tab-body">${equipmentTab()}</div><div class="ph-upgrade-fx" id="phUpgradeFx" aria-hidden="true"></div></div>`;bind(root);return}root.innerHTML=`<div class="ph-page"><div class="section-title ph-title"><div><p class="eyebrow">POW POWER HUB</p><h1>Điện Cường Hóa</h1></div><p>Mọi thao tác Level, Sao, chỉ số, trang bị, Cổ vật và theo dõi bộ kỹ năng cố định được tập trung tại đây.</p></div><section class="panel ph-team-panel"><div class="ph-card-head"><div><small>SQUAD COMMAND · ĐIỆN CƯỜNG HÓA</small><h3>3 Pow chính · 2 Pow dự bị</h3></div><div class="ph-team-panel-actions"><span>Quản lý đội hình và chọn Pow cường hóa ngay tại đây</span><button class="ph-team-edit-btn" data-ph-team-edit>✦ Chỉnh đội hình đầy đủ</button></div></div>${teamSelector()}</section><section class="panel ph-hero"><div class="ph-portrait"><div class="ph-aura"></div><img src="${esc(p.asset)}" alt="${esc(p.name)}"><span>${esc(titleOf(p))}</span></div><div class="ph-identity"><small>${esc(rarityLabel[p.rarity]||p.rarity)} · ${esc(elem.name||p.element)}</small><h2>${esc(p.name)}</h2><p>${esc(p.rolePrimary||p.role)} · Lv.${o.level||1} · ${o.stars||0}/${p.maxStars||7}★</p><div class="ph-mini-stars">${starString(Number(o.stars||0),p.maxStars||7)}</div><div class="ph-hero-tags"><span>⚔ <b>${power.toLocaleString('vi-VN')}</b> lực chiến</span><span>${currencyIcon('coin')} <b>${Number(a.coins||0).toLocaleString('vi-VN')}</b></span><span><img class="currency-inline-icon" src="assets/items/ui-compact/candy-common.webp" alt=""> <b>${Number(candyWallet(a).common||0).toLocaleString('vi-VN')}</b></span><span>🧩 <b>${Number(o.shards||0).toLocaleString('vi-VN')}</b></span></div></div><div class="ph-hero-stats">${[['HP',stats?.hp],['ATK',stats?.atk],['DEF',stats?.def],['AP',stats?.ap]].map(([k,v])=>`<div><small>${k}</small><b>${Number(v||0).toLocaleString('vi-VN')}</b></div>`).join('')}</div></section>${tabs}<div class="ph-tab-body">${tab==='artifact'?artifactTab():tab==='skills'?skillsTab():overviewTab()}</div><div class="ph-upgrade-fx" id="phUpgradeFx" aria-hidden="true"></div></div>`;bind(root)}
function countdown(){const ms=Math.max(0,S.shop.nextRefresh-Date.now()),h=Math.floor(ms/3600000),m=Math.floor(ms%3600000/60000);return `${h}h ${m}m`}
function equipmentMarketTab(changed){const cap=rankCapRarity(),rank=Number(appSave().rank)||0,benefits=shopRankBenefits(rank),refreshLimit=benefits.refreshLimit,exchangeLimit=benefits.exchangeLimit,refreshCost=currentManualRefreshCost(),selected=displayPow(S.selectedPow),recent=(S.shop.purchaseHistory||[]).map(x=>({...x,item:equipmentOf(x.id)})).filter(x=>x.item).slice(0,8);return `<section class="panel w93-market-status"><div><small>GIỚI HẠN TAMER</small><b>${esc(rankNameById(rank))}</b><span>Mua trực tiếp tối đa: ${rarityChip(cap)}</span></div><div><small>PHIÊN CHỢ</small><b>${changed?'Vừa cập nhật':'Còn '+countdown()}</b><span>${W.economy.shopOfferCount} trang bị · mỗi món chỉ 1 bản</span></div><div><small>GIỚI HẠN HÔM NAY</small><b>Refresh ${S.shop.dailyRefreshes}/${refreshLimit}</b><span>Đổi Tinh Tệ ${S.shop.dailyExchanges}/${exchangeLimit}</span></div></section><section class="panel"><div class="panel-head"><div><p class="eyebrow">CHỢ TRANG BỊ KHAN HIẾM</p><h2>${W.economy.shopOfferCount} món · cập nhật mỗi ${W.economy.refreshHours} giờ</h2></div><span class="w8-beta-note">${changed?'VỪA CẬP NHẬT':'CÒN '+countdown()}</span></div><div class="w94-market-pow"><span>Đang so build cho</span><b>${selected?esc(selected.name):'Chưa chọn Pow'}</b><small>${selected?esc(roleBuildHint(selected.id)):'Chọn Pow tại Điện Cường Hóa để xem chênh lệch lực chiến trước khi mua.'}</small></div><div class="w93-shop-actions"><button class="btn secondary" data-w9-exchange ${S.shop.dailyExchanges>=exchangeLimit?'disabled':''}>Đổi ${benefits.coinCost.toLocaleString('vi-VN')} Coin → ${benefits.currencyGain} ${currencyIcon('element')} · ${Math.max(0,exchangeLimit-S.shop.dailyExchanges)} lượt</button><button class="btn primary" data-w9-refresh ${S.shop.dailyRefreshes>=refreshLimit?'disabled':''}>Làm mới · ${refreshCost} ${currencyIcon('element')} · ${Math.max(0,refreshLimit-S.shop.dailyRefreshes)} lượt</button></div><div class="w9-offer-grid">${S.shop.offers.map((o,i)=>{const it=offerItem(o);if(!it)return'';const price=priceOf(it),req=requiredRankForRarity(it.rarity),locked=rarityOrder[it.rarity]>rarityOrder[rankCapRarity()],delta=selected?previewEquipmentDelta(it,selected.id):0,owned=qty(S.equipmentOwned,it.id);return `<article class="w9-offer ${o.stock<=0?'sold':''} ${locked?'rank-locked':''}"><div class="w9-offer-art"><img src="${esc(itemThumb(it.asset))}">${locked?`<i class="w93-rank-lock">🔒 ${esc(rankNameById(req))}</i>`:''}${owned?`<i class="w94-owned-shop">×${owned}</i>`:''}</div><small>${esc(it.setName+' · '+it.slotName)}</small><b>${esc(it.name)}</b>${rarityChip(it.rarity)}<p>${esc(statText(it))}</p>${selected?`<strong class="w94-market-delta ${deltaClass(delta)}">${deltaText(delta)} nếu gắn cho ${esc(selected.name)}</strong>`:''}<footer><span>${currencyIcon('element')} ${price.toLocaleString('vi-VN')}</span><button ${o.stock<=0||locked?'disabled':''} data-w9-buy="${i}">${o.stock<=0?'Đã bán':locked?'Chưa đủ Rank':'Mua 1 bản'}</button></footer></article>`}).join('')}</div></section>${recent.length?`<section class="panel w94-purchase-history"><div class="panel-head"><div><p class="eyebrow">NHẬT KÝ GIAO DỊCH</p><h2>Trang bị vừa mua</h2></div><span class="w8-beta-note">Lưu 12 giao dịch gần nhất</span></div><div>${recent.map(x=>`<article><img src="${esc(itemThumb(x.item.asset))}"><span><b>${esc(x.item.name)}</b><small>${new Date(Number(x.at)||Date.now()).toLocaleString('vi-VN',{hour:'2-digit',minute:'2-digit',day:'2-digit',month:'2-digit'})}</small></span>${rarityChip(x.item.rarity)}<strong>${currencyIcon('element')}-${Number(x.price||0).toLocaleString('vi-VN')}</strong></article>`).join('')}</div></section>`:''}<section class="panel"><div class="panel-head"><div><p class="eyebrow">BỘ SƯU TẬP TRANG BỊ</p><h2>13 Set · xem hiệu ứng trước khi săn</h2></div></div><div class="w8-set-grid">${W.equipmentSets.map(set=>`<article class="w8-set-card"><header><div><small>SET ${String(set.order).padStart(2,'0')}</small><h3>${esc(set.name)}</h3></div>${rarityChip(set.rarity)}</header><p><b>2/4:</b> ${esc(set.bonus2)}</p><p><b>4/4:</b> ${esc(set.bonus4)}</p></article>`).join('')}</div></section>`}
function artifactCollectionPanel(){const open=!!S.ui.artifactCollectionOpen,ownedCount=Object.keys(S.artifactOwned||{}).length;if(!open)return `<section class="panel w94-collection-closed"><div><p class="eyebrow">BỘ SƯU TẬP CỔ VẬT</p><h2>${ownedCount}/49 đã khám phá</h2><p>Bản trùng cùng ArtifactID dùng để nâng cấp +X.</p></div><button class="btn secondary" data-w94-artifact-collection>Mở bộ sưu tập</button></section>`;return `<section class="panel w94-collection-panel"><div class="panel-head"><div><p class="eyebrow">BỘ SƯU TẬP CỔ VẬT</p><h2>${ownedCount}/49 đã khám phá</h2></div><button class="btn secondary" data-w94-artifact-collection>Thu gọn</button></div><div class="w94-artifact-collection-grid">${W.artifacts.map(raw=>{const list=artifactInstancesById(raw.id),known=list.length>0,best=list.sort((a,b)=>Number(b.upgradeLevel||0)-Number(a.upgradeLevel||0))[0],bestIt=best?artifactOf(best.instanceId):null;return `<article class="${known?'known':'unknown'} r-${esc(raw.rarity)}"><div><img src="${esc(itemThumb(raw.asset))}" loading="lazy" decoding="async" alt="${known?esc(raw.name):'Cổ vật chưa khám phá'}"></div>${rarityChip(raw.rarity)}<b>${known?`${esc(raw.name)} +${bestIt?.upgradeLevel||0}`:'???'}</b><small>${known?`Sở hữu ×${list.length} · cao nhất +${bestIt?.upgradeLevel||0}`:'Chưa khám phá'}</small>${known?`<button class="w126-collection-detail" data-artifact-detail="${esc(best.instanceId)}">Nâng cấp</button>`:''}</article>`}).join('')}</div></section>`}
function configureArtifactModule(){const M=window.POWDER_ARTIFACT_MODULE;if(!M)return null;M.configure({getRank:()=>currentTamerRank(),getSettings:()=>appSave().settings||{},getTickets:()=>artifactTicketBalance(),spendTickets:n=>reserveArtifactTickets(n),refundTickets:n=>refundArtifactReservation(n),getOwned:()=>S.artifactOwned,setOwned:v=>{S.artifactOwned={...(v||{})}},getInstances:()=>S.artifactInstances,setInstances:v=>{S.artifactInstances={...(v||{})};syncArtifactOwned()},addInstance:(artifactId,opts={})=>createArtifactInstance(artifactId,opts,false),getArtifact:ref=>artifactOf(ref),getUpgradePreview:iid=>artifactUpgradePreview(iid),upgradeInstance:iid=>upgradeArtifactInstance(iid),setProtection:(iid,field,value)=>setArtifactProtection(iid,field,value),getEquipped:()=>S.powArtifact,setEquipped:v=>{S.powArtifact={...(v||{})};normalizeArtifactInventory(S);save('artifact-equip')},getSummon:()=>S.artifactSummon,setSummon:v=>{const next=v||{};S.artifactSummon={...S.artifactSummon,...next,since:{...(S.artifactSummon.since||{}),...(next.since||{})},history:Array.isArray(next.history)?next.history.slice(0,30):(S.artifactSummon.history||[])}},onChange:()=>{},onSummon:results=>{const n=Array.isArray(results)?results.length:0;if(!n)return false;syncArtifactOwned();const prevPulls=Number(S.stats.artifactPulls||0);S.stats.artifactPulls=prevPulls+n;S.artifactSummon.totalPulls=Number(S.artifactSummon.totalPulls||0)+n;S.artifactSummon.lastResults=results.map(x=>x.instanceId||x.id);if(S.journal)S.journal.artifactSummon=null;const ok=save('artifact-summon-commit');if(!ok)S.stats.artifactPulls=prevPulls;if(ok)renderGear();return ok}});return M}
function artifactSummonTab(){return `<section class="pa-integrated-host"><div class="artifact-ticket-sources"><div class="artifact-ticket-source-head"><span>🎟️ VÉ CỔ ẤN</span><b data-artifact-ticket-balance>${artifactTicketBalance()} vé</b></div><div class="artifact-ticket-source-grid"><span><b>+2</b> Hạ Boss ngày</span><span><b>+10</b> Hạ Boss tuần</span><span><b>+5</b> Hạ Boss thăng rank</span></div></div><div id="powderArtifactModuleMount"></div></section>`}
function renderShop(){const root=document.querySelector('#shopView');if(!root)return;syncDailyShop();const changed=refreshShop(),tab=S.shop.activeTab||'equipment';root.innerHTML=`<div class="section-title w93-shop-title"><div><p class="eyebrow">ELEMENTAL MARKET</p><h1>Chợ & Triệu Hồi</h1></div><p>Trang bị xuất hiện theo Chợ xoay vòng; Cổ vật được triệu hồi bằng Vé Cổ Ấn và chịu ảnh hưởng bởi Rank Tamer cùng cơ chế bảo chứng.</p></div><section class="panel w9-shop-wallet"><div><small>TÀI SẢN CỬA HÀNG</small><b>${currencyIcon('element')} ${Math.floor(S.elementCoin).toLocaleString('vi-VN')} ${W.economy.currencyName}</b><span>🎟️ ${artifactTicketBalance()} Vé Cổ Ấn · Chợ tự đổi sau ${countdown()}</span></div></section><nav class="w93-shop-tabs"><button data-w93-shop-tab="equipment" class="${tab==='equipment'?'active':''}">⚔ Chợ Trang Bị</button><button data-w93-shop-tab="artifact" class="${tab==='artifact'?'active':''}">✦ Triệu Hồi Cổ Vật</button></nav>${tab==='artifact'?artifactSummonTab():equipmentMarketTab(changed)}`;bind(root);if(tab==='artifact'){const M=configureArtifactModule();M?.mount?.(root.querySelector('#powderArtifactModuleMount'))}updateWallet()}
function playUpgradeSound(kind='star'){const settings=appSave().settings||{};if(settings.vibration&&navigator?.vibrate)navigator.vibrate(kind==='star'?[35,25,50,25,90]:25);if(!settings.sound)return;try{const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return;const ctx=playUpgradeSound.ctx||(playUpgradeSound.ctx=new AC());const notes=kind==='star'?[523.25,659.25,783.99,1046.5]:[587.33,783.99];notes.forEach((f,i)=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type=kind==='star'?'triangle':'sine';o.frequency.value=f;const t=ctx.currentTime+i*.065;g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(kind==='star'?.055:.035,t+.025);g.gain.exponentialRampToValueAtTime(.0001,t+.38);o.connect(g).connect(ctx.destination);o.start(t);o.stop(t+.4)})}catch(_){}}
function celebrateStar(name,stars){const root=document.querySelector('#gearView'),fx=root?.querySelector('#phUpgradeFx');if(!fx)return;fx.innerHTML=`<div class="ph-star-celebration"><div class="ph-star-ring"></div><div class="ph-star-burst">★</div><b>${esc(name)} · ${stars}★</b><span>THỨC TỈNH THÀNH CÔNG</span></div>`;fx.classList.add('show');playUpgradeSound('star');setTimeout(()=>fx.classList.remove('show'),1500)}
async function doFeed(type='common',v=1){const a=app(),before=a?.getPowProgress?.(S.selectedPow);if(!a||!before)return;const wallet=before.candies||candyWallet(appSave()),count=v==='max'?Math.max(1,Number(wallet[type]||0)):Number(v)||1;const ok=await a.feedPowCandy?.(S.selectedPow,count,type);const after=a.getPowProgress?.(S.selectedPow);if(ok&&after&&after.level>before.level){playUpgradeSound('level');toast(`${powName(S.selectedPow)} tăng lên Lv.${after.level}.`)}}
function bind(root){root.querySelectorAll('[data-ph-pow]').forEach(b=>b.onclick=()=>{S.selectedPow=b.dataset.phPow;save();renderGear()});root.querySelector('[data-ph-team-edit]')?.addEventListener('click',openTeamEditor);root.querySelectorAll('[data-ph-team-add]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditor.slot=Math.max(0,Math.min(4,Number(b.dataset.phTeamAdd)||0));openTeamEditor()});root.querySelectorAll('[data-ph-team-left]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditorMove(Number(b.dataset.phTeamLeft),-1)});root.querySelectorAll('[data-ph-team-right]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditorMove(Number(b.dataset.phTeamRight),1)});root.querySelectorAll('[data-ph-team-remove]').forEach(b=>b.onclick=e=>{e.stopPropagation();teamEditorRemove(Number(b.dataset.phTeamRemove))});root.querySelectorAll('[data-ph-team-active]').forEach(b=>b.onclick=e=>{e.stopPropagation();const cur=appSave();if(teamEditorCommit(cur.team||[],b.dataset.phTeamActive)){S.selectedPow=b.dataset.phTeamActive;save();renderGear()}});root.querySelectorAll('[data-ph-tab]').forEach(b=>b.onclick=()=>{S.ui.tab=b.dataset.phTab;save();renderGear()});root.querySelectorAll('[data-ph-slot]').forEach(b=>b.onclick=()=>{S.ui.equipSlot=b.dataset.phSlot;save();renderGear()});root.querySelectorAll('[data-w9-equip]').forEach(b=>b.onclick=()=>equipItem(b.dataset.w9Equip));root.querySelectorAll('[data-w9-artifact]').forEach(b=>b.onclick=()=>equipArtifact(b.dataset.w9Artifact));root.querySelectorAll('[data-artifact-detail]').forEach(b=>b.onclick=e=>{e.preventDefault();e.stopPropagation();configureArtifactModule()?.showUpgrade?.(b.dataset.artifactDetail,()=>{renderGear();renderShop()})});root.querySelectorAll('[data-w9-unequip]').forEach(b=>b.onclick=()=>unequip(b.dataset.w9Unequip));root.querySelector('[data-w9-artifact-remove]')?.addEventListener('click',unequipArtifact);root.querySelectorAll('[data-w9-buy]').forEach(b=>b.onclick=()=>buyOffer(Number(b.dataset.w9Buy)));root.querySelector('[data-w9-refresh]')?.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();manualRefresh()});root.querySelector('[data-w9-exchange]')?.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();exchangeCoins()});root.querySelectorAll('[data-w93-shop-tab]').forEach(b=>b.onclick=()=>{S.shop.activeTab=b.dataset.w93ShopTab;save();renderShop()});root.querySelectorAll('[data-w93-summon]').forEach(b=>b.onclick=()=>summonArtifacts(Number(b.dataset.w93Summon)));root.querySelector('[data-w94-artifact-collection]')?.addEventListener('click',()=>{S.ui.artifactCollectionOpen=!S.ui.artifactCollectionOpen;save();renderShop()});root.querySelectorAll('[data-ph-feed-candy]').forEach(b=>b.onclick=async()=>{if(b.dataset.busy==='1')return;b.dataset.busy='1';b.disabled=true;try{await doFeed(b.dataset.phFeedCandy,b.dataset.count||1)}finally{if(b.isConnected){b.dataset.busy='0';b.disabled=false}}});root.querySelectorAll('[data-ph-buy-candy]').forEach(b=>b.onclick=async()=>{if(b.dataset.busy==='1')return;b.dataset.busy='1';b.disabled=true;try{await app()?.buyPowCandy?.(Number(b.dataset.phBuyCandy))}finally{if(b.isConnected){b.dataset.busy='0';b.disabled=false}}});root.querySelector('[data-ph-star]')?.addEventListener('click',async()=>{const id=S.selectedPow,before=Number(appSave().owned?.[id]?.stars||0),p=powOf(id),ok=await app()?.upgradePow?.(id),after=Number(appSave().owned?.[id]?.stars||0);if(ok&&after>before&&p){const all=[p.abilities?.basic,...(p.abilities?.skills||[]),p.abilities?.ultimate,p.exclusiveSkill||p.abilities?.exclusive].filter(Boolean),unlocked=all.filter(sk=>Number(sk.unlockStar||0)===after);if(unlocked.length){S.ui.tab='skills';save()}renderGear();celebrateStar(p.name,after);if(unlocked.length)toast(`${p.name} đạt ${after}★ · mở ${unlocked.map(x=>x.name).join(' · ')}`)}else renderGear()});root.querySelectorAll('[data-ph-shop]').forEach(b=>b.onclick=()=>{S.shop.activeTab='equipment';save();app()?.showView?.('shop')});root.querySelectorAll('[data-ph-artifact-summon]').forEach(b=>b.onclick=()=>{S.shop.activeTab='artifact';save();app()?.showView?.('shop')});root.querySelectorAll('[data-eq-cycle]').forEach(b=>b.onclick=()=>{const team=teamPows();if(team.length<2)return;const now=Math.max(0,team.findIndex(p=>p.id===S.selectedPow)),dir=Number(b.dataset.eqCycle)||1,next=(now+dir+team.length)%team.length;S.selectedPow=team[next].id;S.ui.equipSlot='all';save();renderGear()});root.querySelectorAll('[data-eq-slot-target]').forEach(card=>card.onclick=e=>{if(e.target.closest('[data-w9-unequip]'))return;S.ui.equipSlot=card.dataset.eqSlotTarget||'all';save();renderGear()});root.querySelector('[data-eq-recommended]')?.addEventListener('click',equipRecommendedItem);}
function applyEquipmentStats(powId,stats,roleKey){const out={...stats},slotsMap=S.powEquipment[powId]||{};for(const id of Object.values(slotsMap)){const it=equipmentOf(id);if(!it?.mainStat)continue;let stat=it.mainStat.stat;if(stat==='primary')stat=['mage','enchanter','healer','musician'].includes(roleKey)?'ap':'atk';out[stat]=Math.max(1,Math.round((out[stat]||0)+Number(it.mainStat.value||0)))}applySetPct(out,setStatPctForSlots(slotsMap));applyPctProfile(out,artifactStatProfile(S.powArtifact[powId]||null));return out}
function normalizeImportedWorld(raw){const d=defaults(),cur=raw&&typeof raw==='object'?JSON.parse(JSON.stringify(raw)):{};const n={...d,...cur,version:9,saveMeta:{...d.saveMeta,...(cur.saveMeta||{}),schema:9},journal:{...d.journal,...(cur.journal||{})},equipmentOwned:{...(cur.equipmentOwned||{})},equipmentEnhance:{...(cur.equipmentEnhance||{})},artifactOwned:{...(cur.artifactOwned||{})},artifactInstances:{...(cur.artifactInstances||{})},powEquipment:{...(cur.powEquipment||{})},powArtifact:{...(cur.powArtifact||{})},artifactTickets:Math.max(0,Math.floor(Number(cur.artifactTickets??d.artifactTickets)||0)),ticketStats:{...d.ticketStats,...(cur.ticketStats||{})},ticketClaims:{...(cur.ticketClaims||{})},shop:{...d.shop,...(cur.shop||{}),purchaseHistory:Array.isArray(cur.shop?.purchaseHistory)?cur.shop.purchaseHistory.slice(0,12):[]},artifactSummon:{...d.artifactSummon,...(cur.artifactSummon||{}),since:{...d.artifactSummon.since,...(cur.artifactSummon?.since||{})},history:Array.isArray(cur.artifactSummon?.history)?cur.artifactSummon.history.slice(0,30):[]},stats:{...d.stats,...(cur.stats||{})},ui:{...d.ui,...(cur.ui||{})}};n.elementCoin=Math.max(0,Math.floor(Number(cur.elementCoin??d.elementCoin)||0));for(const [id,v] of Object.entries(n.equipmentOwned))n.equipmentOwned[id]=Math.max(0,Math.floor(Number(v)||0));for(const [id,v] of Object.entries(n.equipmentEnhance||{}))n.equipmentEnhance[id]=Math.max(0,Math.min(5,Math.floor(Number(v)||0)));n.shop.dailyRefreshes=Math.max(0,Math.floor(Number(n.shop.dailyRefreshes)||0));n.shop.dailyExchanges=Math.max(0,Math.floor(Number(n.shop.dailyExchanges)||0));n.shop.manualRefreshes=Math.max(0,Math.floor(Number(n.shop.manualRefreshes)||0));n.stats.purchases=Math.max(0,Math.floor(Number(n.stats.purchases)||0));n.stats.spent=Math.max(0,Math.floor(Number(n.stats.spent)||0));n.stats.earned=Math.max(0,Math.floor(Number(n.stats.earned)||0));n.stats.artifactPulls=Math.max(0,Math.floor(Number(n.stats.artifactPulls)||0));n.stats.artifactUpgrades=Math.max(0,Math.floor(Number(n.stats.artifactUpgrades)||0));return normalizeArtifactInventory(n)}function importWorldState(raw){if(!raw||typeof raw!=='object')return false;const before=JSON.stringify(S);try{S=normalizeImportedWorld(raw);S.journal={artifactSummon:null,currencyExchange:null};if(!save('world-import'))throw new Error('world import save failed');renderAll();return true}catch(err){console.error('[Powder world import]',err);S=normalizeArtifactInventory(JSON.parse(before));save('world-import-rollback');return false}}function renderAll(){renderGear();renderShop();updateWallet()}function purgeWorldStorage(){const keys=[KEY,`${KEY}_pending`,`${KEY}_recovery`,`${KEY}_backup_latest`,`${KEY}_backup_previous`,OLD,'powder_artifact_module_9520'];for(const key of keys){try{localStorage.removeItem(key)}catch(_){}}S=normalizeArtifactInventory(defaults());return true}
document.addEventListener('click',e=>{const b=e.target.closest?.('[data-view="shop"],[data-open-view="shop"]');if(b)setTimeout(renderShop,0);const g=e.target.closest?.('[data-view="gear"],[data-open-view="gear"]');if(g)setTimeout(renderGear,0)});
window.POWDER_WORLD_V9={version:'15.4-rank-shop-equipment-enhance',getState:()=>JSON.parse(JSON.stringify(S)),getSaveHealth:()=>({schema:Number(S.version||0),meta:{...(S.saveMeta||{})},pendingArtifactSummon:{...(S.journal?.artifactSummon||{})},pendingCurrencyExchange:{...(S.journal?.currencyExchange||{})},artifactInstances:Object.keys(S.artifactInstances||{}).length,ticketClaims:Object.keys(S.ticketClaims||{}).length,bytes:(localStorage.getItem(KEY)||'').length,backupSlots:[`${KEY}_backup_latest`,`${KEY}_backup_previous`].filter(k=>!!localStorage.getItem(k)).length}),createBackup:(reason='manual')=>{worldBackup(String(reason||'manual'));return true},saveNow:(reason='manual')=>save(reason),getLoadout:pid=>({equipment:{...(S.powEquipment[pid]||{})},artifact:S.powArtifact[pid]||null,sets:activeSetBonuses(pid)}),equipmentOf,artifactOf,artifactInstance,getArtifactInstances:()=>JSON.parse(JSON.stringify(S.artifactInstances||{})),getArtifactUpgradePreview:artifactUpgradePreview,upgradeArtifact:upgradeArtifactInstance,setArtifactProtection,activeSetBonuses,applyEquipmentStats,getPowerBreakdown:pid=>({...getPowerBreakdown(pid)}),previewEquipment:(id,pid)=>{const it=equipmentOf(id);return it?previewEquipmentDelta(it,pid||S.selectedPow):0},previewArtifact:(id,pid)=>{const it=artifactOf(id);return it?previewArtifactDelta(it,pid||S.selectedPow):0},grantCurrency,spendCurrency,grantArtifactTickets,spendArtifactTickets,hasTicketClaim,getArtifactTickets:artifactTicketBalance,updateWallet,renderAll,renderGear,renderShop,refreshShop,summonArtifacts,getArtifactRates:rank=>({...artifactRatesForRank(rank)}),isArtifactPityUnlocked:(r,rank)=>artifactPityUnlocked(r,rank),reset:()=>{S=normalizeArtifactInventory(defaults());save('world-reset');renderAll();return true},importState:raw=>importWorldState(raw),purge:()=>purgeWorldStorage()};
window.POWDER_WORLD_V8=window.POWDER_WORLD_V9;setTimeout(()=>{refreshShop();updateWallet();recoverCurrencyExchangeJournal()},120);
})();
