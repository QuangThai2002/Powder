(()=>{'use strict';
if(window.POWDER_COMBAT_TARGET_NAVIGATION_V2193)return;
const VERSION='21.9.3',STYLE_ID='powderCombatTargetNavigation2193Css',STYLE_HREF='css/combat-target-navigation-v2193.css?v=2193';
const state={moves:0,confirms:0,cancels:0,pointerSyncs:0,patches:0,index:0,lastTargetId:'',lastAt:0};let raf=0;
function style(){if(document.getElementById(STYLE_ID))return;const l=document.createElement('link');l.id=STYLE_ID;l.rel='stylesheet';l.href=STYLE_HREF;document.head.appendChild(l)}
function context(){const root=document.querySelector('#battleView:not([hidden]) .combat-v7-mount'),dock=root?.querySelector('.cv7-command.is-targeting');if(!root||!dock)return null;const targets=[...root.querySelectorAll('.cv7-unit.targetable:not([disabled])')].filter(n=>!n.classList.contains('dead'));return{root,dock,targets}}
function clear(){for(const n of document.querySelectorAll('#battleView [data-cv2193-cursor]'))n.removeAttribute('data-cv2193-cursor')}
function currentIndex(targets){if(!targets.length)return-1;const cursor=targets.findIndex(n=>n.hasAttribute('data-cv2193-cursor'));if(cursor>=0)return cursor;const preview=targets.findIndex(n=>n.hasAttribute('data-cv2192-preview'));if(preview>=0)return preview;const locked=targets.findIndex(n=>n.classList.contains('cv69-locked-target')||n.classList.contains('telegraph-target'));return locked>=0?locked:Math.max(0,Math.min(targets.length-1,state.index||0))}
function select(i,focus=true){const ctx=context();if(!ctx||!ctx.targets.length){clear();return false}const len=ctx.targets.length,index=((Number(i)||0)%len+len)%len;clear();const el=ctx.targets[index];el.setAttribute('data-cv2193-cursor','1');state.index=index;state.lastTargetId=String(el.dataset.cv7Unit||'');state.lastAt=Date.now();if(focus){try{el.focus({preventScroll:true})}catch(_){el.focus?.()}}return true}
function move(delta){const ctx=context();if(!ctx||!ctx.targets.length)return false;const i=currentIndex(ctx.targets);state.moves++;return select((i<0?0:i)+delta,true)}
function confirm(){const ctx=context();if(!ctx||!ctx.targets.length)return false;let i=currentIndex(ctx.targets);if(i<0)i=0;const el=ctx.targets[i];if(!el)return false;state.confirms++;state.lastTargetId=String(el.dataset.cv7Unit||'');state.lastAt=Date.now();el.click();return true}
function cancel(){const ctx=context(),b=ctx?.dock?.querySelector('[data-cv7-cancel]');if(!b)return false;state.cancels++;state.lastAt=Date.now();b.click();clear();return true}
function patch(){style();const ctx=context();if(!ctx){clear();return false}if(!ctx.targets.length){clear();return false}const i=currentIndex(ctx.targets);select(i<0?0:i,false);ctx.dock.dataset.cv2193Targets=String(ctx.targets.length);state.patches++;state.lastAt=Date.now();return true}
function schedule(){if(raf)return;raf=requestAnimationFrame(()=>{raf=0;patch()})}
document.addEventListener('keydown',e=>{if(e.defaultPrevented||e.altKey||e.ctrlKey||e.metaKey||e.shiftKey)return;const tag=String(e.target?.tagName||'').toLowerCase();if(['input','textarea','select'].includes(tag)||e.target?.isContentEditable||!context())return;if(['ArrowLeft','ArrowUp'].includes(e.key)){e.preventDefault();move(-1);return}if(['ArrowRight','ArrowDown'].includes(e.key)){e.preventDefault();move(1);return}if(e.key==='Enter'){e.preventDefault();confirm();return}if(e.key==='Escape'){e.preventDefault();cancel()}},true);
document.addEventListener('pointerover',e=>{const el=e.target?.closest?.('#battleView:not([hidden]) .cv7-unit.targetable');if(!el)return;const ctx=context();if(!ctx)return;const i=ctx.targets.indexOf(el);if(i<0)return;state.pointerSyncs++;select(i,false)},true);
document.addEventListener('click',e=>{const el=e.target?.closest?.('#battleView:not([hidden]) .cv7-unit.targetable');if(el){const ctx=context(),i=ctx?.targets?.indexOf(el)??-1;if(i>=0){state.pointerSyncs++;state.index=i;state.lastTargetId=String(el.dataset.cv7Unit||'')}}setTimeout(schedule,0)},true);
['powder:rendered','powder:combat-state','powder:view-changed'].forEach(evt=>window.addEventListener(evt,schedule,{passive:true}));window.addEventListener('pagehide',()=>{if(raf)cancelAnimationFrame(raf);raf=0;clear()},{once:true});
function snapshot(){const ctx=context();return{version:VERSION,...state,active:Boolean(ctx),targetCount:ctx?.targets?.length||0,policy:'target-mode-only keyboard/pointer navigation; arrow cycle, Enter confirms by existing target click, Escape uses existing cancel; no Combat Core, damage, skill, AI, Save, Learning or PvP mutation'}}
window.POWDER_COMBAT_TARGET_NAVIGATION_V2193={version:VERSION,refresh:schedule,move,confirm,cancel,snapshot};style();schedule();
})();