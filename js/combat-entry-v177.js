(()=>{'use strict';
const VERSION='17.7-player-first-entry';
const HANDOFF_VERSION=1,REQUEST_KEY='powder.combat2.handoff.request.v1',RESULT_KEY='powder.combat2.handoff.result.v1',SETTLED_KEY='powder.combat2.handoff.settled.v1',RECEIPT_KEY='powder.combat2.handoff.receipt.v1';
const BATTLE_MODES=new Set(['pve','pvp','boss','daily_boss','dungeon','event']),RESULT_VALUES=new Set(['victory','defeat','draw','cancel']);
let lastEntryResult={ok:false,reason:'not-started',errors:[]};
let settlementRunning=false;
function clone(value){try{return value==null?null:JSON.parse(JSON.stringify(value));}catch{return null;}}
function obj(value){return value&&typeof value==='object'&&!Array.isArray(value)?value:{}}
function ids(value){return Array.isArray(value)?[...new Set(value.map(id=>String(id||'').trim().toLowerCase()).filter(Boolean))].slice(0,5):[]}
function read(key,scope='sessionStorage'){try{const raw=window[scope]?.getItem(key);return raw?JSON.parse(raw):null;}catch{return null;}}
function write(key,value,scope='sessionStorage'){try{window[scope]?.setItem(key,JSON.stringify(value));return true;}catch{return false;}}
function remove(key,scope='sessionStorage'){try{window[scope]?.removeItem(key);}catch{}}
function battleId(){const id=typeof crypto!=='undefined'&&typeof crypto.randomUUID==='function'?crypto.randomUUID().replace(/-/g,''):`${Date.now()}${Math.random().toString(36).slice(2)}`;return `c2-${id}`;}
function validateBattleRequest(raw){const input=obj(raw),battleIdValue=String(input.battleId||'').trim(),battleMode=String(input.battleMode||'').toLowerCase(),playerTeam=ids(input.playerTeam),enemyTeam=ids(input.enemyTeam),errors=[];if(!/^c2-[a-z0-9]+$/i.test(battleIdValue))errors.push('battleId không hợp lệ');if(!BATTLE_MODES.has(battleMode))errors.push('battleMode không được hỗ trợ');if(!playerTeam.length)errors.push('đội người chơi trống');if(!enemyTeam.length)errors.push('đội đối thủ trống');if(battleMode==='boss'&&!String(obj(input.bossContext).bossChallengeId||'').trim())errors.push('bossContext không hợp lệ');if(errors.length)return{ok:false,errors};const formation=obj(input.formation);return{ok:true,value:{version:HANDOFF_VERSION,battleId:battleIdValue,battleMode,sourceContext:clone(obj(input.sourceContext))||{},returnContext:clone(obj(input.returnContext))||{},playerTeam,formation:{active:ids(formation.active).filter(id=>playerTeam.includes(id)).slice(0,3),reserve:ids(formation.reserve).filter(id=>playerTeam.includes(id)).slice(0,2)},enemyTeam,enemyConfig:clone(obj(input.enemyConfig))||{},battleRules:clone(obj(input.battleRules))||{},rosterContext:clone(obj(input.rosterContext))||{},bossContext:clone(obj(input.bossContext))||{},academicContext:clone(obj(input.academicContext))||{},rewardContext:clone(obj(input.rewardContext))||{},seed:input.seed==null?null:String(input.seed),createdAt:Number(input.createdAt)||Date.now()}};}
function createBattleRequest(input={}){return validateBattleRequest({...obj(input),battleId:input.battleId||battleId(),createdAt:Date.now()});}
function storeBattleRequest(request){const checked=validateBattleRequest(request);if(!checked.ok)return checked;if(!write(REQUEST_KEY,checked.value))return{ok:false,errors:['không thể lưu BattleRequest']};remove(RESULT_KEY);return checked;}
function readBattleRequest(){const value=read(REQUEST_KEY);return value?validateBattleRequest(value):{ok:false,errors:['không có BattleRequest']};}
function validateBattleResult(raw,request){const input=obj(raw),result=String(input.result||'').toLowerCase(),errors=[];if(!request||input.battleId!==request.battleId)errors.push('BattleResult không khớp BattleRequest');if(!RESULT_VALUES.has(result))errors.push('kết quả trận không hợp lệ');if(String(input.battleMode||'')!==String(request?.battleMode||''))errors.push('battleMode không khớp');if(errors.length)return{ok:false,errors};return{ok:true,value:{version:HANDOFF_VERSION,battleId:request.battleId,battleMode:request.battleMode,result,survivingState:clone(obj(input.survivingState))||{},battleSummary:clone(obj(input.battleSummary))||{},rewardOutcome:clone(obj(input.rewardOutcome))||{},academicOutcome:clone(obj(input.academicOutcome))||{},progressionOutcome:clone(obj(input.progressionOutcome))||{},bossOutcome:clone(obj(input.bossOutcome))||{},returnContext:clone(obj(input.returnContext))||request.returnContext,finishedAt:Number(input.finishedAt)||Date.now()}};}
function publishBattleResult(result){const request=readBattleRequest();if(!request.ok)return request;const checked=validateBattleResult(result,request.value);if(!checked.ok)return checked;return write(RESULT_KEY,checked.value)?checked:{ok:false,errors:['không thể lưu BattleResult']};}
function readBattleResult(){const request=readBattleRequest(),value=read(RESULT_KEY);return request.ok&&value?validateBattleResult(value,request.value):{ok:false,errors:['không có BattleResult']};}
function settledIds(){const values=read(SETTLED_KEY,'localStorage');return Array.isArray(values)?values.map(String).filter(Boolean).slice(-32):[];}
function markSettled(id){const prior=settledIds();return prior.includes(id)?false:write(SETTLED_KEY,[...prior,id].slice(-32),'localStorage');}
function onCombat2Page(){return /(?:^|\/)combat2\.html$/i.test(window.location.pathname);}
function restoreContext(context){const target=obj(context),view=String(target.view||'home');window.POWDER_APP?.showView?.(view);if(view==='adventure')window.POWDER_ADVENTURE?.restoreCombatContext?.(target);}
function continueBattleResult(result,settled=true){if(settled){remove(RESULT_KEY);remove(REQUEST_KEY);remove(RECEIPT_KEY);}restoreContext(result?.returnContext);return{ok:true};}
function safeAmount(value){const number=Number(value);return Number.isFinite(number)?Math.max(0,Math.round(number)):0;}
function formatResultNumber(value){return safeAmount(value).toLocaleString('vi-VN');}
function formatBattleDuration(ms){const safe=Number(ms);if(!Number.isFinite(safe)||safe<0)return'Không ghi nhận';const total=Math.max(0,Math.round(safe/1000)),minutes=Math.floor(total/60),seconds=total%60;return minutes>0?`${minutes} phút ${seconds} giây`:`${seconds} giây`;}
function resultPowMeta(powId){const id=String(powId||'').trim().toLowerCase(),pows=Array.isArray(window.POWDER_DATA?.pows)?window.POWDER_DATA.pows:[];return pows.find(pow=>String(pow?.id||'').trim().toLowerCase()===id)||null;}
function resultPowName(powId){const pow=resultPowMeta(powId),fallback=String(powId||'Pow').trim();return String(pow?.name||fallback||'Pow');}
function resultPowArtwork(powId){const pow=resultPowMeta(powId),raw=String(pow?.asset||pow?.assetUrl||pow?.image||'').trim(),mapped=raw&&window.POWDER_THUMBNAILS?.[raw];return String(mapped||raw||'assets/ui/pow-fallback.svg');}
function battleResultStageLabel(result){const request=readBattleRequest(),source=obj(request.ok?request.value.sourceContext:{}),stage=obj(source.stage),progression=obj(result?.progressionOutcome),id=String(stage.id||source.stageId||progression.stageId||'').trim(),name=String(stage.name||stage.title||stage.label||'').trim();if(name&&id&&!name.toLowerCase().includes(id.toLowerCase()))return`${name} · Chặng ${id}`;if(name)return name;if(id)return`Chặng ${id}`;return'Kết quả trận';}
function playerResultRows(result){const summary=obj(result?.battleSummary),players=Array.isArray(summary.players)?summary.players:[];return players.map(raw=>{const row=obj(raw),powId=String(row.powId||'').trim();return{powId,name:resultPowName(powId),artwork:resultPowArtwork(powId),survived:row.survived===true,damageDealt:safeAmount(row.damageDealt),damageTaken:safeAmount(row.damageTaken),healingDone:safeAmount(row.healingDone),shieldDone:safeAmount(row.shieldDone),shieldAbsorbed:safeAmount(row.shieldAbsorbed)};});}
function contributionDimensions(row){return{damage:row.damageDealt,healing:row.healingDone,shield:row.shieldDone,tank:row.damageTaken+row.shieldAbsorbed};}
function mvpContributionScore(row,maxima){const value=contributionDimensions(row);return(value.damage/(maxima.damage||1))*.45+(value.healing/(maxima.healing||1))*.25+(value.shield/(maxima.shield||1))*.15+(value.tank/(maxima.tank||1))*.15;}
function selectBattleMvp(rows){if(!rows.length)return null;const maxima=rows.reduce((acc,row)=>{const value=contributionDimensions(row);for(const key of Object.keys(acc))acc[key]=Math.max(acc[key],value[key]);return acc;},{damage:0,healing:0,shield:0,tank:0});const ranked=rows.map((row,index)=>({row,index,score:mvpContributionScore(row,maxima),total:Object.values(contributionDimensions(row)).reduce((sum,value)=>sum+value,0)})).filter(item=>item.total>0).sort((a,b)=>b.score-a.score||b.total-a.total||a.index-b.index);return ranked[0]||null;}
function mvpHighlights(row){const values=[{label:'Sát thương',value:row.damageDealt},{label:'Hồi máu',value:row.healingDone},{label:'Khiên',value:row.shieldDone},{label:'Chống chịu',value:row.damageTaken+row.shieldAbsorbed}].sort((a,b)=>b.value-a.value);return values.slice(0,3);}
function ensureBattleResultStyles(){if(document.getElementById('combat2BattleResultV2Styles'))return;const style=document.createElement('style');style.id='combat2BattleResultV2Styles';style.textContent=`#combat2SettlementResult{position:fixed;inset:0;z-index:3000;display:grid;place-items:center;background:radial-gradient(circle at 50% 18%,rgba(42,88,111,.32),transparent 42%),rgba(2,8,15,.92);padding:18px;box-sizing:border-box}#combat2SettlementResult *{box-sizing:border-box}.c2-result-shell{width:min(1040px,100%);max-height:94vh;overflow:auto;border:1px solid rgba(222,193,118,.76);border-radius:24px;background:linear-gradient(180deg,rgba(15,38,54,.985),rgba(5,18,30,.99));color:#edf9fa;box-shadow:0 30px 90px rgba(0,0,0,.72),inset 0 0 45px rgba(89,181,212,.06);padding:24px;font-family:Inter,system-ui,sans-serif}.c2-result-header{text-align:center;padding:6px 12px 18px;border-bottom:1px solid rgba(206,184,119,.26)}.c2-result-kicker{margin:0 0 4px;color:#cfb873;font-size:12px;font-weight:800;letter-spacing:.18em}.c2-result-title{margin:0;font-size:clamp(32px,5vw,52px);line-height:1;font-weight:900;letter-spacing:.05em;color:#ffb3bd;text-shadow:0 0 24px rgba(255,114,130,.22)}.c2-result-shell.is-victory .c2-result-title{color:#9affc4;text-shadow:0 0 24px rgba(115,240,170,.22)}.c2-result-meta{display:flex;justify-content:center;flex-wrap:wrap;gap:8px;margin-top:14px}.c2-result-meta span{padding:7px 11px;border:1px solid rgba(151,204,220,.24);border-radius:999px;background:rgba(7,27,40,.7);color:#d8e8ee;font-size:13px;font-weight:700}.c2-result-section{margin-top:18px;padding:18px;border:1px solid rgba(140,198,214,.2);border-radius:18px;background:rgba(7,27,40,.62)}.c2-result-section-title{margin:0 0 13px;color:#f5e4b0;font-size:14px;font-weight:900;letter-spacing:.12em}.c2-mvp-card{display:grid;grid-template-columns:112px minmax(0,1fr);gap:18px;align-items:center}.c2-mvp-art{width:112px;height:112px;border-radius:18px;object-fit:contain;background:radial-gradient(circle,rgba(215,184,108,.22),rgba(4,15,24,.7));border:1px solid rgba(222,193,118,.45)}.c2-mvp-top{display:flex;align-items:center;gap:10px;flex-wrap:wrap}.c2-mvp-name{font-size:25px;font-weight:900;color:#fff8e7}.c2-badge{display:inline-flex;align-items:center;min-height:25px;padding:4px 9px;border-radius:999px;font-size:11px;font-weight:900;letter-spacing:.08em;border:1px solid rgba(255,255,255,.18);background:rgba(33,105,88,.58);color:#a7ffd0}.c2-badge.is-defeated{background:rgba(128,43,58,.6);color:#ffc0ca}.c2-badge.is-mvp{background:linear-gradient(135deg,#806727,#b39138);color:#fff5c7;border-color:#e6cb79}.c2-mvp-highlights{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}.c2-mvp-highlights span{padding:8px 10px;border-radius:11px;background:rgba(11,48,65,.78);color:#dff6fb;font-size:13px;font-weight:700}.c2-team-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px}.c2-pow-card{min-width:0;padding:12px;border:1px solid rgba(132,192,211,.22);border-radius:15px;background:linear-gradient(180deg,rgba(15,50,68,.62),rgba(6,25,38,.72))}.c2-pow-head{display:grid;grid-template-columns:52px minmax(0,1fr);gap:9px;align-items:center;margin-bottom:11px}.c2-pow-art{width:52px;height:52px;border-radius:12px;object-fit:contain;background:rgba(4,16,26,.72);border:1px solid rgba(210,185,113,.28)}.c2-pow-name{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#fff7df;font-size:15px;font-weight:900}.c2-pow-status{margin-top:4px}.c2-stat-list{display:grid;gap:6px}.c2-stat{display:flex;justify-content:space-between;gap:8px;align-items:center;padding-top:6px;border-top:1px solid rgba(255,255,255,.06);font-size:12px}.c2-stat span{color:#a9c3ce}.c2-stat strong{color:#f3fbff;font-variant-numeric:tabular-nums}.c2-reward-box{display:flex;flex-wrap:wrap;gap:9px}.c2-reward-item{min-width:150px;padding:11px 13px;border:1px solid rgba(211,184,111,.25);border-radius:12px;background:rgba(45,40,20,.3)}.c2-reward-item small{display:block;color:#b8cbd2;font-size:11px}.c2-reward-item strong{display:block;margin-top:3px;color:#fff0b1;font-size:18px}.c2-reward-state{margin:0;color:#d9e7eb;font-size:15px;font-weight:800}.c2-result-actions{display:flex;justify-content:center;padding-top:20px}.c2-result-continue{min-width:230px;padding:13px 26px;border:1px solid #c9b36d;border-radius:13px;background:linear-gradient(180deg,#2d806a,#1d6457);color:#fff;font-size:15px;font-weight:900;letter-spacing:.05em;cursor:pointer;box-shadow:0 9px 24px rgba(0,0,0,.24)}.c2-result-continue:hover{filter:brightness(1.08)}.c2-result-empty{margin:0;color:#acc5cf}@media(max-width:900px){.c2-team-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.c2-result-shell{padding:18px}.c2-mvp-card{grid-template-columns:88px minmax(0,1fr)}.c2-mvp-art{width:88px;height:88px}}@media(max-width:540px){#combat2SettlementResult{padding:8px}.c2-result-shell{max-height:98vh;border-radius:17px;padding:13px}.c2-result-section{padding:13px;margin-top:12px}.c2-team-grid{grid-template-columns:1fr}.c2-mvp-card{grid-template-columns:74px minmax(0,1fr);gap:12px}.c2-mvp-art{width:74px;height:74px}.c2-mvp-name{font-size:20px}.c2-result-meta span{font-size:12px}.c2-result-title{font-size:34px}}`;document.head?.appendChild(style);}
function resultNode(tag,className,text){const node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=String(text);return node;}
function appendStat(card,label,value){const row=resultNode('div','c2-stat');row.append(resultNode('span','',label),resultNode('strong','',formatResultNumber(value)));card.appendChild(row);}
function receiptRewardItems(receipt,result){if(!receipt||receipt.battleId!==result?.battleId)return[];const granted=obj(receipt.granted),items=[],push=(label,value,prefix='+')=>{const amount=safeAmount(value);if(amount>0)items.push({label,value:`${prefix}${formatResultNumber(amount)}`});};push('Military Coin',granted.coins);push('EXP',granted.exp);push('EXP Tamer',granted.tamerExp);push('EXP Pow',granted.powExp);push('PowCandy',granted.powCandy,'×');push('Vé Cổ Vật',granted.artifactTickets,'×');return items;}
function showSettledBattleResult(result,receipt=null,errorCode=''){
  if(typeof document?.getElementById!=='function'||typeof document?.createElement!=='function')return;
  ensureBattleResultStyles();
  if(errorCode)console.warn('[Combat2 settlement]',errorCode,{battleId:result?.battleId,result:result?.result});
  let overlay=document.getElementById('combat2SettlementResult');
  if(!overlay){overlay=document.createElement('section');overlay.id='combat2SettlementResult';overlay.setAttribute('role','dialog');overlay.setAttribute('aria-modal','true');document.body.appendChild(overlay);}
  overlay.replaceChildren();
  const win=result?.result==='victory',shell=resultNode('article',`c2-result-shell ${win?'is-victory':'is-defeat'}`),header=resultNode('header','c2-result-header');
  header.append(resultNode('p','c2-result-kicker','KẾT QUẢ CHIẾN ĐẤU'),resultNode('h2','c2-result-title',win?'CHIẾN THẮNG':'THẤT BẠI'));
  const summary=obj(result?.battleSummary),meta=resultNode('div','c2-result-meta');
  meta.append(resultNode('span','',battleResultStageLabel(result)),resultNode('span','',`${safeAmount(summary.turnCount)} lượt`),resultNode('span','',formatBattleDuration(summary.durationMs)));
  header.appendChild(meta);shell.appendChild(header);
  const players=playerResultRows(result),mvp=selectBattleMvp(players);
  const mvpSection=resultNode('section','c2-result-section'),mvpTitle=resultNode('h3','c2-result-section-title','POW NỔI BẬT');
  mvpSection.appendChild(mvpTitle);
  if(mvp){const card=resultNode('div','c2-mvp-card'),image=resultNode('img','c2-mvp-art');image.src=mvp.row.artwork;image.alt=mvp.row.name;image.loading='eager';const copy=resultNode('div',''),top=resultNode('div','c2-mvp-top');top.append(resultNode('strong','c2-mvp-name',mvp.row.name),resultNode('span','c2-badge is-mvp','MVP'),resultNode('span',`c2-badge ${mvp.row.survived?'':'is-defeated'}`,mvp.row.survived?'Còn sống':'Bị hạ'));const highlights=resultNode('div','c2-mvp-highlights');for(const item of mvpHighlights(mvp.row))highlights.appendChild(resultNode('span','',`${item.label}: ${formatResultNumber(item.value)}`));copy.append(top,highlights);card.append(image,copy);mvpSection.appendChild(card);}else mvpSection.appendChild(resultNode('p','c2-result-empty','Chưa có đóng góp đủ để xác định Pow nổi bật.'));
  shell.appendChild(mvpSection);
  const teamSection=resultNode('section','c2-result-section');teamSection.appendChild(resultNode('h3','c2-result-section-title','THỐNG KÊ ĐỘI'));const grid=resultNode('div','c2-team-grid');
  for(const row of players){const card=resultNode('article','c2-pow-card'),head=resultNode('div','c2-pow-head'),image=resultNode('img','c2-pow-art');image.src=row.artwork;image.alt=row.name;image.loading='lazy';const identity=resultNode('div',''),name=resultNode('strong','c2-pow-name',row.name),status=resultNode('div','c2-pow-status');status.appendChild(resultNode('span',`c2-badge ${row.survived?'':'is-defeated'}`,row.survived?'Còn sống':'Bị hạ'));identity.append(name,status);head.append(image,identity);card.appendChild(head);const stats=resultNode('div','c2-stat-list');appendStat(stats,'Sát thương',row.damageDealt);appendStat(stats,'Chịu sát thương',row.damageTaken);appendStat(stats,'Hồi máu',row.healingDone);appendStat(stats,'Khiên',row.shieldDone);appendStat(stats,'Khiên hấp thụ',row.shieldAbsorbed);card.appendChild(stats);grid.appendChild(card);}
  if(!players.length)grid.appendChild(resultNode('p','c2-result-empty','Không có thống kê Pow được ghi nhận.'));teamSection.appendChild(grid);shell.appendChild(teamSection);
  const rewardSection=resultNode('section','c2-result-section');rewardSection.appendChild(resultNode('h3','c2-result-section-title','PHẦN THƯỞNG'));const eligible=obj(result?.rewardOutcome).eligible===true,validReceipt=!!receipt&&receipt.battleId===result?.battleId&&!!receipt.granted;
  if(!eligible)rewardSection.appendChild(resultNode('p','c2-reward-state','KHÔNG CÓ PHẦN THƯỞNG'));else if(validReceipt){const items=receiptRewardItems(receipt,result),box=resultNode('div','c2-reward-box');if(items.length){for(const item of items){const card=resultNode('div','c2-reward-item');card.append(resultNode('small','',item.label),resultNode('strong','',item.value));box.appendChild(card);}}else box.appendChild(resultNode('p','c2-reward-state','Không có vật phẩm nhận thêm.'));rewardSection.appendChild(box);}else rewardSection.appendChild(resultNode('p','c2-reward-state','Phần thưởng đang chờ đồng bộ.'));shell.appendChild(rewardSection);
  const actions=resultNode('div','c2-result-actions'),button=resultNode('button','c2-result-continue','TIẾP TỤC');button.type='button';const canClear=!errorCode&&(!eligible||validReceipt);button.onclick=()=>{overlay.remove();continueBattleResult(result,canClear);};actions.appendChild(button);shell.appendChild(actions);overlay.appendChild(shell);button.focus();
}
async function settleAcademicOutcome(request,result){if(request?.battleMode!=='pve'||request?.academicContext?.requiresActionQuestions!==true)return{ok:true,applied:0};const responses=Array.isArray(result?.academicOutcome?.responses)?result.academicOutcome.responses:[];if(!responses.length)return{ok:true,applied:0};const allowed=Array.isArray(request.academicContext.allowedQuestionPool)?request.academicContext.allowedQuestionPool:[],pool=new Map(allowed.map(q=>[String(q?.id||'').trim(),q]).filter(([id])=>id)),seen=new Set(),sanitized=[];for(const raw of responses){const response=obj(raw),id=String(obj(response.question).id||'').trim(),question=pool.get(id);if(!id||!question)return{ok:false,reason:'academic-question-not-allowed'};if(seen.has(id))return{ok:false,reason:'duplicate-academic-question'};if(typeof response.correct!=='boolean'||typeof response.powId!=='string')return{ok:false,reason:'invalid-academic-response'};seen.add(id);sanitized.push({powId:response.powId.trim(),correct:response.correct,question:clone(question)});}const apply=window.POWDER_APP?.applyCombatAcademicOutcome;if(typeof apply!=='function')return{ok:false,reason:'academic-authority-unavailable'};return apply({battleId:result.battleId,battleCreatedAt:request.createdAt,responses:sanitized,allowedQuestionPool:allowed});}
async function settleReturnedResult(){if(onCombat2Page())return{ok:false,reason:'combat2-page'};if(settlementRunning)return{ok:false,reason:'settlement-in-progress'};settlementRunning=true;try{const request=readBattleRequest(),result=readBattleResult();if(!request.ok||!result.ok)return{ok:false,reason:'missing-result'};if(settledIds().includes(result.value.battleId)){const receipt=read(RECEIPT_KEY);showSettledBattleResult(result.value,receipt?.battleId===result.value.battleId?receipt:null,receipt?.battleId===result.value.battleId?'':'COMBAT_REWARD_RECEIPT_MISSING');return{ok:true,duplicate:true};}if(result.value.battleMode==='pve'&&result.value.result==='victory'&&(window.POWDER_ONLINE_V150?.hasSession?.()||window.POWDER_SECURE_ECONOMY_V152?.hasAccount?.())){showSettledBattleResult(result.value,null,'COMBAT_REWARD_ONLINE_AUTHORITY_UNAVAILABLE');return{ok:false,reason:'online-reward-authority-unavailable'};}const academicResult=await settleAcademicOutcome(request.value,result.value);if(!academicResult?.ok){showSettledBattleResult(result.value,null,'COMBAT_ACADEMIC_SETTLEMENT_FAILED');return academicResult;}let rewardResult={ok:true,granted:{coins:0,exp:0,wins:0}};if(result.value.battleMode==='boss'){const boss=obj(request.value.bossContext),stage=obj(request.value.sourceContext).stage,safety=window.POWDER_TX_SAFETY_V2090;if(typeof safety?.mutate!=='function'||typeof window.POWDER_APP?.onBossCombatFinished!=='function'){showSettledBattleResult(result.value,null,'COMBAT_BOSS_SETTLEMENT_UNAVAILABLE');return{ok:false,reason:'boss-settlement-unavailable'};}try{rewardResult=await safety.mutate({scope:'reward',operation:'combat2-boss-settlement',payload:{battleId:result.value.battleId,bossChallengeId:String(boss.bossChallengeId||''),result:result.value.result},dedupeKey:`combat2-boss:${result.value.battleId}`,stable:true,txKey:`combat2-boss:${result.value.battleId}`,send:async()=>{const value=window.POWDER_APP.onBossCombatFinished({id:String(boss.bossChallengeId||''),win:result.value.result==='victory',stage:clone(stage),summary:result.value.academicOutcome?.summary||{players:[]}});if(value?.ok===false)return Promise.reject(new Error('boss-authority-rejected'));return value||{ok:true};}});}catch{showSettledBattleResult(result.value,null,'COMBAT_BOSS_SETTLEMENT_FAILED');return{ok:false,reason:'boss-authority-rejected'};}}else if(result.value.battleMode==='pve'&&result.value.result==='victory'){const reward=obj(request.value.rewardContext),safety=window.POWDER_TX_SAFETY_V2090;if(typeof safety?.mutate!=='function'){showSettledBattleResult(result.value,null,'COMBAT_REWARD_TX_UNAVAILABLE');return{ok:false,reason:'reward-idempotency-unavailable'};}try{rewardResult=await safety.mutate({scope:'reward',operation:'combat2-pve-reward',payload:{battleId:result.value.battleId,rewardId:String(reward.rewardId||''),coins:Math.max(0,Number(reward.coins)||0),exp:Math.max(0,Number(reward.exp)||0),wins:Math.max(0,Number(reward.wins)||1)},dedupeKey:`combat2:${result.value.battleId}`,stable:true,txKey:`combat2:${result.value.battleId}`,send:async()=>{const battleRewardPayload={coins:Math.max(0,Number(reward.coins)||0),exp:Math.max(0,Number(reward.exp)||0),wins:Math.max(0,Number(reward.wins)||1)};Object.defineProperties(battleRewardPayload,{battleId:{value:result.value.battleId,enumerable:false},rewardId:{value:String(reward.rewardId||''),enumerable:false},battleCreatedAt:{value:request.value.createdAt,enumerable:false}});const before=window.POWDER_APP?.getSave?.()||{},value=window.POWDER_APP?.grantBattleRewards?.(battleRewardPayload);if(value?.ok!==true||value?.serverProtected===true)throw new Error('reward-authority-rejected');const after=window.POWDER_APP?.getSave?.()||{};return{ok:true,granted:{coins:Math.max(0,Number(after.coins||0)-Number(before.coins||0)),exp:Math.max(0,Number(after.exp||0)-Number(before.exp||0)),wins:Math.max(0,Number(after.wins||0)-Number(before.wins||0))}};}});}catch{showSettledBattleResult(result.value,null,'COMBAT_REWARD_SETTLEMENT_FAILED');return{ok:false,reason:'reward-authority-rejected'};}if(rewardResult?.ok!==true||!rewardResult.granted){showSettledBattleResult(result.value,null,'COMBAT_REWARD_RECEIPT_INVALID');return{ok:false,reason:'reward-receipt-invalid'};}const stageId=String(request.value.sourceContext?.stageId||''),progression=window.POWDER_APP?.applyAdventureBattleOutcome?.({battleId:result.value.battleId,battleCreatedAt:request.value.createdAt,stageId,battleSummary:result.value.academicOutcome?.summary||{players:[]},result:'victory'});if(progression?.ok!==true){showSettledBattleResult(result.value,null,'COMBAT_ADVENTURE_PROGRESSION_FAILED');return{ok:false,reason:progression?.reason||'adventure-progression-rejected'};}}const bossGrant=result.value.battleMode==='boss'&&result.value.result==='victory'?obj(rewardResult.reward):{};const granted=rewardResult.granted||{coins:Math.max(0,Number(bossGrant.coins)||0),exp:Math.max(0,Number(bossGrant.exp)||0),wins:0};if(!write(RECEIPT_KEY,{battleId:result.value.battleId,granted})){showSettledBattleResult(result.value,null,'COMBAT_RECEIPT_STORAGE_FAILED');return{ok:false,reason:'receipt-write-failed'};}if(!markSettled(result.value.battleId)){showSettledBattleResult(result.value,null,'COMBAT_SETTLEMENT_STORAGE_FAILED');return{ok:false,reason:'settlement-write-failed'};}showSettledBattleResult(result.value,read(RECEIPT_KEY));return{ok:true,duplicate:false,rewardOutcome:granted};}finally{settlementRunning=false;}}
function scheduleSettlement(){if(onCombat2Page())return;let attempts=0;const run=()=>{attempts+=1;const ready=window.POWDER_APP&&window.POWDER_ADVENTURE&&typeof window.POWDER_TX_SAFETY_V2090?.mutate==='function'&&window.POWDER_SECURE_ECONOMY_V152;if(ready){void settleReturnedResult();return;}if(attempts<120){window.setTimeout(run,100);return;}const result=readBattleResult();if(result.ok)showSettledBattleResult(result.value,null,'COMBAT_SETTLEMENT_RUNTIME_NOT_READY');};window.setTimeout(run,120);}
function launchCombat2(request){const stored=storeBattleRequest(request);if(!stored.ok)return stored;try{const url=new URL('combat2.html',window.location.href);url.searchParams.set('battle',stored.value.battleId);window.location.assign(url);return{ok:true,value:stored.value};}catch{remove(REQUEST_KEY);return{ok:false,errors:['không thể mở Combat2']};}}
function returnToMain(result){const published=publishBattleResult(result);if(!published.ok)return published;try{window.location.assign(new URL('./',window.location.href));return published;}catch{return{ok:false,errors:['không thể quay về Main']};}}
function playerTeam(){const save=window.POWDER_APP?.getSave?.()||{},policy=eligibility(),known=new Set((window.POWDER_DATA?.pows||[]).filter(pow=>policy?.isPlayerEligible?.(pow)).map(pow=>String(pow?.id||'').toLowerCase())),team=ids(save.team).filter(id=>known.has(id));if(!team.length&&save.starterId&&known.has(String(save.starterId).toLowerCase()))team.push(String(save.starterId).toLowerCase());return team;}
function academicQuestionSnapshot(stage,gate){const requiredLessons=new Set(ids(gate?.requirement?.RequiredLessonIDs)),requiredConcepts=new Set(ids(gate?.requirement?.RequiredConceptIDs)),source=window.POWDER_APP?.getCombatQuestionPool?.({stage,mode:'pve'}),out=[],seen=new Set();for(const raw of Array.isArray(source)?source:[]){const q=obj(raw),id=String(q.id||'').trim(),lessonId=String(q.lessonId||'').trim(),conceptId=String(q.conceptId||q.learningMeta?.ConceptID||'').trim(),prompt=String(q.prompt||'').trim(),answer=String(q.answer||'').trim(),options=[...new Set((Array.isArray(q.options)?q.options:[]).map(x=>String(x||'').trim()).filter(Boolean))].slice(0,4);if(!id||seen.has(id)||!lessonId||!conceptId||!prompt||!answer||options.length<2||!options.includes(answer))continue;if(requiredLessons.size&&!requiredLessons.has(lessonId))continue;if(requiredConcepts.size&&!requiredConcepts.has(conceptId))continue;seen.add(id);out.push({id,lessonId,conceptId,language:String(q.language||'ZH').toUpperCase(),prompt,answer,options,explain:String(q.explain||'').trim()});if(out.length>=24)break;}return out;}
function resolveLearningGate(stage,provided){return provided&&typeof provided==='object'?provided:window.POWDER_ADVENTURE?.learningGate?.(stage)||window.POWDER_APP?.getDungeonLearningGate?.(stage)||{};}
function createPvePilotRequest(stage,options={}){stage=playerStage(stage);if(!stage)return{ok:false,errors:['player eligibility không hợp lệ']};const player=playerTeam(),enemy=ids(stage?.enemyIds).slice(0,Math.max(1,Math.min(5,Number(stage?.enemyCount)||3))),gate=resolveLearningGate(stage,options?.learningGate),save=window.POWDER_APP?.getSave?.()||{},questions=academicQuestionSnapshot(stage,gate),roster=window.POWDER_COMBAT2_BOSS_BOOTSTRAP?.buildPve?.(stage,{playerIds:player,enemyIds:enemy,save});return createBattleRequest({battleMode:'pve',sourceContext:{kind:'adventure',stage:clone(stage),stageId:String(stage?.id||'')},returnContext:{view:'adventure',stageId:String(stage?.id||''),islandId:Number(stage?.islandId)||1},playerTeam:player,formation:{active:player.slice(0,3),reserve:player.slice(3,5)},enemyTeam:enemy,enemyConfig:{stageId:String(stage?.id||''),kind:String(stage?.kind||'normal')},battleRules:{stageScale:Number(stage?.scale)||1,initiative:Number(stage?.initiative)||0,learningActionScale:true},rosterContext:roster||{},academicContext:{authority:'main-learning',stageId:String(stage?.id||''),rank:Number(gate?.requirement?.Rank)||0,islandId:Number(stage?.islandId)||0,curriculum:clone(gate?.requirement?.Curriculum)||{},learnedLessonIds:ids(save.lessonsDone),requiredLessonIds:Array.isArray(gate?.requirement?.RequiredLessonIDs)?[...gate.requirement.RequiredLessonIDs]:[],requiredConceptIds:Array.isArray(gate?.requirement?.RequiredConceptIDs)?[...gate.requirement.RequiredConceptIDs]:[],requiredMastery:Number(gate?.requirement?.RequiredMastery)||0,srs:{mode:'due-first',at:Date.now()},allowedQuestionPool:questions,requiresActionQuestions:true,gateReady:gate?.ready!==false,onboardingCombat:gate?.onboardingCombat===true},rewardContext:{rewardId:`pve:${String(stage?.id||'')}`,coins:Math.max(0,Number(stage?.rewards?.coins)||0),exp:Math.max(0,Number(stage?.rewards?.exp)||0),wins:1}});}
function pvePilotErrors(request){const errors=[...(Array.isArray(request?.errors)?request.errors:[])],value=request?.value,roster=value?.rosterContext,academic=value?.academicContext;if(!request?.ok)return errors.length?errors:['BattleRequest không hợp lệ'];if(!Array.isArray(roster?.playerRoster)||!roster.playerRoster.length)errors.push('đội người chơi trống');if(!Array.isArray(roster?.enemyRoster)||!roster.enemyRoster.length)errors.push('đội đối thủ trống');if(academic?.gateReady===false)errors.push('chưa đủ điều kiện học thuật');if(academic?.requiresActionQuestions!==true)errors.push('thiếu câu hỏi hành động học thuật');if(!Array.isArray(academic?.allowedQuestionPool)||!academic.allowedQuestionPool.length)errors.push('không có câu hỏi học thuật hợp lệ');if(typeof window.POWDER_APP?.applyCombatAcademicOutcome!=='function'||typeof window.POWDER_COMBAT_ACADEMIC_AUTHORITY_V1?.applyOfflineCombatAcademicOutcome!=='function')errors.push('thiếu quyền ghi kết quả học thuật');if(typeof window.POWDER_APP?.applyAdventureBattleOutcome!=='function'||typeof window.POWDER_ADVENTURE_PROGRESSION_AUTHORITY_V1?.applyOfflineAdventureVictory!=='function')errors.push('thiếu quyền ghi tiến trình Phiêu lưu');if(typeof window.POWDER_TX_SAFETY_V2090?.mutate!=='function')errors.push('thiếu lớp giao dịch an toàn');return[...new Set(errors)];}
function canRunPvePilot(request){return pvePilotErrors(request).length===0;}
function bossConfig(type){const config=window.POWDER_BOSS_ENCOUNTER_V1860?.encounter?.(type);return clone(config)||null;}
function createBossPilotRequest(stage){const raw=stage;stage=playerStage(stage);if(!stage||(raw?.serverCombatSessionId&&eligibility()?.containsHiddenPow?.(raw.enemyIds)))return{ok:false,errors:['Boss server trả về Pow không hợp lệ cho player mode']};const player=playerTeam(),challengeId=String(stage?.bossChallengeId||'').toLowerCase(),enemy=ids(stage?.enemyIds).slice(0,1),config=bossConfig(challengeId),save=window.POWDER_APP?.getSave?.()||{},bootstrap=window.POWDER_COMBAT2_BOSS_BOOTSTRAP?.build?.(stage,{playerIds:player,enemyIds:enemy,save});return createBattleRequest({battleMode:'boss',sourceContext:{kind:'boss',stage:clone(stage),stageId:String(stage?.id||''),bossChallengeId:challengeId},returnContext:{view:'boss',bossChallengeId:challengeId},playerTeam:player,formation:{active:player.slice(0,3),reserve:player.slice(3,5)},enemyTeam:enemy,enemyConfig:{bossId:enemy[0]||null,difficulty:String(stage?.difficultyLabel||'BOSS')},battleRules:{stageScale:Number(stage?.scale)||1,initiative:Number(stage?.initiative)||0},bossContext:{bossChallengeId:challengeId,bossType:challengeId,bossId:enemy[0]||null,rank:Number(save.rank)||0,difficulty:String(stage?.difficultyLabel||'BOSS'),phaseConfig:config,bootstrap,rewardContext:{claimId:String(stage?.id||challengeId),rewardEligible:stage?.rewardEligibleServer!==false},cooldownContext:{challengeId}},academicContext:{authority:'boss-qualified-main',requiresActionQuestions:false},rewardContext:{rewardId:`boss:${challengeId}`}});}
function canRunBossPilot(request){const bootstrap=request?.value?.bossContext?.bootstrap;return !!request?.ok&&request.value.battleMode==='boss'&&!!request.value.bossContext?.phaseConfig&&Array.isArray(bootstrap?.playerRoster)&&bootstrap.playerRoster.length>0&&Array.isArray(bootstrap?.enemyRoster)&&bootstrap.enemyRoster.length>0&&typeof window.POWDER_APP?.onBossCombatFinished==='function'&&typeof window.POWDER_TX_SAFETY_V2090?.mutate==='function'&&!window.POWDER_ONLINE_V150?.hasSession?.()&&!request.value.sourceContext?.stage?.serverCombatSessionId;}
function scene(){return window.POWDER_BATTLE_PLAYER_V177||null;}
function normalize(stage,source){const s=stage&&typeof stage==='object'?JSON.parse(JSON.stringify(stage)):null;if(!s)return null;s.entrySource=source;return s;}
function startMap(stage,options={}){const s=normalize(stage,'map');if(!s||Number(s.islandId)<1||!s.id){lastEntryResult={ok:false,reason:'invalid-stage',errors:['stage không hợp lệ']};return options?.structured?lastEntryResult:false;}const pilot=createPvePilotRequest(s,options);const errors=pvePilotErrors(pilot);if(errors.length){lastEntryResult={ok:false,reason:errors.includes('chưa đủ điều kiện học thuật')?'academic-gate':'pve-entry-not-ready',errors};return options?.structured?lastEntryResult:false;}const launched=launchCombat2(pilot.value);lastEntryResult=launched.ok?{ok:true,reason:'launched',value:launched.value}:{ok:false,reason:'launch-failed',errors:launched.errors||['không thể mở Combat2']};return options?.structured?lastEntryResult:launched.ok;}
function startBoss(stage){const s=normalize(stage,'boss');if(!s||s.kind!=='boss'||!s.bossChallengeId)return false;const pilot=createBossPilotRequest(s);if(!canRunBossPilot(pilot))return false;return launchCombat2(pilot.value).ok;}
function startEvent(stage){const s=normalize(stage,'event');if(!s||s.eventCombat!==true||!s.eventId)return false;return !!scene()?.startEncounter?.(s,'event');}
function isActive(){return !!scene()?.isActive?.();}
function getState(){return scene()?.getState?.()||null;}
function forfeit(reason='navigation'){return !!scene()?.forfeit?.(reason);}
function getExitCost(){return Math.max(0,Number(scene()?.getExitCost?.()||0));}
function eligibility(){return window.POWDER_PLAYER_POW_ELIGIBILITY_V1||null;}
function playerStage(stage){return eligibility()?.sanitizeStage?.(stage)||null;}
if(window.__POWDER_APP_BOOTED__)scheduleSettlement();else window.addEventListener('powder:app-booted',scheduleSettlement,{once:true});
window.POWDER_COMBAT2_HANDOFF=Object.freeze({version:HANDOFF_VERSION,createBattleRequest,validateBattleRequest,storeBattleRequest,readBattleResult,readBattleRequest,publishBattleResult,returnToMain,settleReturnedResult,continueBattleResult,launchCombat2});
window.POWDER_COMBAT_ENTRY_V177=Object.freeze({version:VERSION,startMap,startBoss,startEvent,isActive,getState,forfeit,getExitCost,createPvePilotRequest,createBossPilotRequest,canRunPvePilot,canRunBossPilot,launchCombat2,getLastEntryResult:()=>clone(lastEntryResult),combat2Cutover:Object.freeze({pve:true,boss:true,legacyFallback:false}),modes:Object.freeze(['map','boss','event'])});
})();
