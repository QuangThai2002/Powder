(()=>{'use strict';
const VERSION='20.16.0',URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';let state=null;
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function ses(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,p={}){const s=ses();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-security',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...p}),cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||d.code||`HTTP ${r.status}`);return d}
function ensure(){let h=$('#security20160');if(h)return h;const page=$('#pageOps')||$('#pageReliability')||document.querySelector('main');if(!page)return null;const sec=document.createElement('section');sec.className='admin-card security2030';sec.innerHTML='<div class="card-head"><div><p class="eyebrow">SECURITY · 20.16.0</p><h2>Security & Permission Final Hardening</h2><p class="muted">Central Admin guard · token freshness/revoke · origin allowlist · MFA/AAL2 cho thao tác production · SECURITY DEFINER privilege scan.</p></div><span id="security20160Badge">CHECK</span></div><div id="security20160"></div>';page.appendChild(sec);return $('#security20160')}
const yn=v=>v?'<b class="sec-ok">PASS</b>':'<b class="sec-bad">HOLD</b>';
function render(){const h=ensure();if(!h||!state)return;const p=state.posture||{},c=p.counts||{},g=p.guards||{},cfg=state.config||{},owner=state.role==='owner';$('#security20160Badge').textContent=p.ready?'READY':'HOLD';const origins=(cfg.allowed_origins||[]).join('\n');h.innerHTML=`<div class="sec-grid"><div><small>Critical blockers</small><b>${c.critical??'-'}</b></div><div><small>High blockers</small><b>${c.high??'-'}</b></div><div><small>SECURITY DEFINER exposed</small><b>${c.publicSecurityDefinerExec??'-'}</b></div><div><small>Direct-write violations</small><b>${c.directWriteViolations??'-'}</b></div><div><small>Admin surfaces</small><b>${c.adminSurfaces??'-'}/17</b></div><div><small>Denied 24h</small><b>${c.denied24h??'-'}</b></div></div><div class="security20160-blockers"><span class="${g.originAllowlistConfigured?'pass':'hold'}">Origin allowlist ${yn(g.originAllowlistConfigured)}</span><span class="${g.allAdminFunctionsCentralGuard?'pass':'hold'}">17/17 central guard ${yn(g.allAdminFunctionsCentralGuard)}</span><span class="pass">Token freshness ${yn(g.tokenFreshness)}</span><span class="pass">Session revoke ${yn(g.sessionRevocation)}</span><span class="pass">Critical MFA/AAL2 ${yn(g.criticalAal2)}</span></div><div class="sec-role"><b>Role: ${esc(state.role)}</b><span>Gate: ${cfg.enabled?'ARMED':'DISARMED'} · Max token age ${cfg.max_token_age_seconds||3600}s</span></div>${owner?`<div class="security20160-config"><label><span>Allowed Admin Origins · mỗi dòng một URL</span><textarea id="security20160Origins" placeholder="https://admin.example.com">${esc(origins)}</textarea></label><button class="admin-btn primary" data-config>${cfg.enabled?'Lưu & giữ ARMED':'Lưu & ARM'}</button></div>`:''}<div class="sec-actions"><button data-refresh>Refresh posture</button>${state.role==='support'?'':'<button data-probe>Run duplicate probe</button>'}${owner?'<button data-snapshot>Save Security snapshot</button>':''}</div><h4>Denied access gần đây</h4><div class="security20160-denied">${(state.recentDenied||[]).slice(0,20).map(x=>`<div><span><b>${esc(x.function_name)} · ${esc(x.action)}</b><small>${esc(x.email)} · ${esc(x.code)} · ${esc(x.aal||'no-aal')}</small></span><small>${new Date(x.created_at).toLocaleString('vi-VN')}</small></div>`).join('')||'<p class="muted">Không có deny gần đây.</p>'}</div><p class="muted">Production chỉ READY khi Critical=0, High=0. Request production quan trọng yêu cầu Owner có MFA/AAL2. Token Admin chỉ đọc từ sessionStorage.</p>`;h.querySelector('[data-refresh]').onclick=load;h.querySelector('[data-probe]')?.addEventListener('click',async()=>{await api('run_probe');await load()});h.querySelector('[data-snapshot]')?.addEventListener('click',async()=>{if(!confirm('Lưu Security snapshot 20.16?'))return;await api('snapshot');await load()});h.querySelector('[data-config]')?.addEventListener('click',async()=>{const allowedOrigins=String($('#security20160Origins')?.value||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean);if(!allowedOrigins.length&&!confirm('Origin allowlist rỗng sẽ giữ Production ở HOLD. Tiếp tục?'))return;await api('configure',{allowedOrigins,enabled:true});await load()})}
async function load(){try{state=await api('state');render()}catch(e){const h=ensure();if(h)h.innerHTML=`<p class="is-error">${esc(e.message)}</p>`}}
function boot(){try{localStorage.removeItem(STORE)}catch{}ensure();setTimeout(load,1200)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.POWDER_ADMIN_SECURITY_V20160={version:VERSION,load,state:()=>state};
})();
