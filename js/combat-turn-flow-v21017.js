(()=>{'use strict';
if(window.POWDER_COMBAT_TURN_FLOW_V21017)return;
const VERSION='21.0.17';
const state={syncs:0,handoffs:0,hudCreates:0,phaseChanges:0,impactEvents:0,queueAnnotations:0,phases:{idle:0,turn:0,charge:0,release:0,impact:0},lastPhase:'idle',lastActor:'',lastAbility:'',lastTarget:'',lastResult:'',lastNext:'',lastPressure:'calm',active:false};
let mount=null,hud=null,raf=0,impactTimer=0,lastImpact=null,lastImpactAt=0,lastAction={actor:'',ability:'',sourceId:''};
const now=()=>performance?.now?.()||Date.now();
const pressure=()=>String(window.POWDER_ADAPTIVE_PRESSURE_V21011?.level?.()||document.documentElement.dataset.resourcePressure||'calm');
const active=n=>Boolean(n?.isConnected&&!n.closest?.('[hidden]'));
const escSel=v=>window.CSS?.escape?CSS.escape(String(v)):String(v).replace(/[^a-zA-Z0-9_-]/g,'\\$&');
function pickMount(){const all=[...document.querySelectorAll('.combat-v7-mount')];return all.find(active)||all[0]||null}
function unitName(u){return String(u?.querySelector('.cv7-unit-info>b,.cv7-unit-info > b')?.textContent||u?.querySelector('.cv7-unit-top+*')?.getAttribute?.('aria-label')||'').trim()}
function directTurns(){const order=mount?.querySelector('.cv7-turn-order');if(!order)return[];return [...order.children].filter(n=>n.tagName==='SPAN')}
function turnName(n){return String(n?.querySelector('em')?.textContent||n?.getAttribute('title')||'').split('·')[0].trim()}
function ensureHud(){if(!mount)return null;const field=mount.querySelector('.cv7-field')||mount;let h=field.querySelector(':scope > .tf217-hud');if(!h){h=document.createElement('aside');h.className='tf217-hud';h.setAttribute('role','status');h.setAttribute('aria-live','polite');h.innerHTML='<span class="tf217-phase"><small>NHỊP LƯỢT</small><b data-tf217-phase-label>CHIẾN ĐẤU</b></span><span class="tf217-action"><small data-tf217-actor>—</small><b data-tf217-ability>Đang xác định hành động</b></span><i aria-hidden="true">→</i><span class="tf217-target"><small>MỤC TIÊU</small><b data-tf217-target>—</b></span><span class="tf217-result"><small>KẾT QUẢ</small><b data-tf217-result>—</b></span><span class="tf217-next"><small>TIẾP THEO</small><b data-tf217-next>—</b></span>';field.appendChild(h);state.hudCreates++}hud=h;return h}
function setText(sel,value){const el=hud?.querySelector(sel);if(!el)return;const v=String(value||'—');if(el.textContent!==v)el.textContent=v}
function clearAnnotations(root=mount){if(!root)return;for(const u of root.querySelectorAll('.cv7-unit[data-tf217-actor],.cv7-unit[data-tf217-target]')){delete u.dataset.tf217Actor;delete u.dataset.tf217Target}for(const n of root.querySelectorAll('.cv7-turn-order>span[data-tf217-now],.cv7-turn-order>span[data-tf217-next],.cv7-turn-order>span[data-tf217-rank]')){delete n.dataset.tf217Now;delete n.dataset.tf217Next;delete n.dataset.tf217Rank}}
function annotate(actor,targets){clearAnnotations();if(actor)actor.dataset.tf217Actor='1';for(const t of targets)if(t&&t!==actor)t.dataset.tf217Target='1';const turns=directTurns();turns.forEach((n,i)=>n.dataset.tf217Rank=String(i+1));const nowNode=turns.find(n=>n.classList.contains('now'))||turns[0]||null;if(nowNode)nowNode.dataset.tf217Now='1';const next=turns.find(n=>n!==nowNode&&!n.classList.contains('now'))||null;if(next)next.dataset.tf217Next='1';state.queueAnnotations+=turns.length;return{nowNode,next}}
function latestFxFor(unit){if(!mount||!unit)return'';const side=unit.classList.contains('enemy')?'enemy':unit.classList.contains('player')?'player':'';const slot=[0,1,2].find(i=>unit.classList.contains(`slot-${i}`));let q='.cv7-fx[data-fx-id]';if(side)q+=`.${side}`;if(slot!==undefined)q+=`.slot-${slot}`;const nodes=[...mount.querySelectorAll(q)].filter(n=>getComputedStyle(n).display!=='none');nodes.sort((a,b)=>(Number(b.dataset.fxId)||0)-(Number(a.dataset.fxId)||0));return String(nodes[0]?.textContent||'').trim()}
function impactLabel(detail,target){const fx=latestFxFor(target);if(fx)return fx;const f=String(detail?.feedback||''),c=String(detail?.control||''),s=String(detail?.severity||'');if(f==='fatal'||s==='fatal')return'HẠ GỤC';if(f==='break')return'PHÁ KHIÊN';if(f==='heal')return'HỒI MÁU';if(f==='shield')return'KHIÊN';if(f==='control')return c==='freeze'?'ĐÓNG BĂNG':c==='stun'?'CHOÁNG':'KHỐNG CHẾ';if(f==='evade')return'NÉ ĐÒN';if(s==='heavy')return'ĐÒN NẶNG';if(s==='medium')return'ĐÒN KỸ NĂNG';if(s==='light')return'TRÚNG ĐÒN';return'KẾT QUẢ ĐÃ GHI NHẬN'}
function phaseLabel(p){return p==='turn'?'ĐẾN LƯỢT':p==='charge'?'CHUẨN BỊ':p==='release'?'RA CHIÊU':p==='impact'?'KẾT QUẢ':'CHIẾN ĐẤU'}
function collect(){if(!mount)return null;const callout=mount.querySelector('.cv7-attack-callout'),flow=mount.querySelector('.cv7-attack-flow'),current=mount.querySelector('.cv7-unit.current'),targets=[...mount.querySelectorAll('.cv7-unit.telegraph-target,.cv7-unit.cv69-locked-target,[data-tf217-impact-target="1"]')].filter((n,i,a)=>a.indexOf(n)===i);const callActor=String(callout?.querySelector('b')?.textContent||'').trim(),callAbility=String(callout?.querySelector('span')?.textContent||'').trim();if(callActor||callAbility)lastAction={actor:callActor||unitName(current),ability:callAbility||'Kỹ năng',sourceId:current?.dataset.cv7Unit||''};const impactFresh=lastImpact&&now()-lastImpactAt<900;const phase=impactFresh?'impact':flow?'release':callout?'charge':current?'turn':'idle';let actor=callActor||unitName(current)||lastAction.actor||'—',ability=callAbility||(phase==='turn'?'Chờ chọn hành động':lastAction.ability||'—');let targetNames=targets.map(unitName).filter(Boolean);if(impactFresh&&lastImpact?.unitId){const t=mount.querySelector(`.cv7-unit[data-cv7-unit="${escSel(lastImpact.unitId)}"]`);if(t){t.dataset.tf217ImpactTarget='1';if(!targets.includes(t))targets.push(t);targetNames=[unitName(t)||...targetNames]}}const q=annotate(current||null,targets);const next=turnName(q.next)||'—';const target=targetNames.join(' + ')||'—';const result=impactFresh?(lastImpact?.label||'KẾT QUẢ'):'—';return{phase,actor,ability,target,result,next,current,targets}}
function render(){raf=0;const next=pickMount();if(next!==mount){if(mount){clearAnnotations(mount);delete mount.dataset.tf217Ready;delete mount.dataset.tf217Phase;delete mount.dataset.tf217Pressure;state.handoffs++}mount=next;hud=null}state.active=!!mount?.isConnected;if(!mount)return snapshot();state.syncs++;state.lastPressure=pressure();ensureHud();const v=collect();if(!v)return snapshot();mount.dataset.tf217Ready='1';mount.dataset.tf217Phase=v.phase;mount.dataset.tf217Pressure=state.lastPressure;hud.dataset.tf217Phase=v.phase;hud.dataset.tf217Pressure=state.lastPressure;if(v.phase!==state.lastPhase){state.phaseChanges++;state.phases[v.phase]=(state.phases[v.phase]||0)+1;state.lastPhase=v.phase;try{window.dispatchEvent(new CustomEvent('powder:combat-turn-flow',{detail:{version:VERSION,phase:v.phase,actor:v.actor,ability:v.ability,target:v.target,next:v.next,pressure:state.lastPressure}}))}catch(_){}}setText('[data-tf217-phase-label]',phaseLabel(v.phase));setText('[data-tf217-actor]',v.actor);setText('[data-tf217-ability]',v.ability);setText('[data-tf217-target]',v.target);setText('[data-tf217-result]',v.result);setText('[data-tf217-next]',v.next);state.lastActor=v.actor;state.lastAbility=v.ability;state.lastTarget=v.target;state.lastResult=v.result;state.lastNext=v.next;return snapshot()}
function schedule(){if(raf||document.hidden)return;raf=requestAnimationFrame(render)}
function onImpact(e){const d=e?.detail||{};const next=pickMount();if(next&&next!==mount){mount=next;hud=null}const target=mount&&d.unitId?mount.querySelector(`.cv7-unit[data-cv7-unit="${escSel(d.unitId)}"]`):null;if(target)target.dataset.tf217ImpactTarget='1';lastImpact={...d,label:impactLabel(d,target)};lastImpactAt=now();state.impactEvents++;if(impactTimer)clearTimeout(impactTimer);impactTimer=setTimeout(()=>{lastImpact=null;lastImpactAt=0;if(mount)for(const n of mount.querySelectorAll('[data-tf217-impact-target]'))delete n.dataset.tf217ImpactTarget;schedule()},920);schedule()}
function detach(){if(raf){cancelAnimationFrame(raf);raf=0}if(impactTimer){clearTimeout(impactTimer);impactTimer=0}if(mount){clearAnnotations(mount);delete mount.dataset.tf217Ready;delete mount.dataset.tf217Phase;delete mount.dataset.tf217Pressure;mount.querySelector(':scope .tf217-hud')?.remove()}mount=null;hud=null;lastImpact=null;state.active=false}
function snapshot(){return{version:VERSION,...state,phases:{...state.phases},active:!!mount?.isConnected,hud:!!hud?.isConnected,pressure:state.lastPressure}}
window.addEventListener('powder:combat-presentation-sync',schedule,{passive:true});window.addEventListener('powder:combat-impact-feedback',onImpact,{passive:true});window.addEventListener('powder:resource-pressure',schedule,{passive:true});document.addEventListener('visibilitychange',()=>{if(document.hidden){if(raf){cancelAnimationFrame(raf);raf=0}}else schedule()},{passive:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',schedule,{once:true});else queueMicrotask(schedule);
window.POWDER_COMBAT_TURN_FLOW_V21017={version:VERSION,snapshot,sync:render,detach};
})();
