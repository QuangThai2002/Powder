(()=>{'use strict';
const URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),fmt=v=>v?new Date(v).toLocaleString('vi-VN'):'—';let st=null;
function ses(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,p={}){const s=ses();if(!s?.access_token)throw new Error('Chưa đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-transactions',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...p})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function ensure(){let root=$('#tx2090');if(root)return root;const page=$('#pageReliability');if(!page)return null;const sec=document.createElement('section');sec.className='admin-card tx2090-card';sec.innerHTML='<div class="card-head"><div><p class="eyebrow">DATA INTEGRITY · 20.9.0</p><h2>Transaction Safety & Recovery Queue</h2><p class="muted">Receipt ledger, idempotency key, stale-pending scan và recovery thủ công. Không tự replay tài nguyên.</p></div><span id="tx2090Badge">CHECK</span></div><div id="tx2090"></div>';page.appendChild(sec);return $('#tx2090')}
function render(){const root=ensure();if(!root||!st)return;const p=st.posture||{},c=p.counts||{},ready=!!p.ready,q=(st.recovery||[]).filter(x=>['queued','investigating'].includes(x.status));$('#tx2090Badge').textContent=ready?'PASS':'HOLD';$('#tx2090Badge').className=ready?'pass':'hold';root.innerHTML=`<div class="tx2090-kpis">${[['Pending',c.pending],['Stale',c.stalePending],['Recovery mở',c.recoveryOpen],['Key mismatch 24h',c.keyReuseMismatch24h],['Commit 24h',c.committed24h],['Fail 24h',c.failed24h]].map(x=>`<article><small>${x[0]}</small><b>${Number(x[1]||0).toLocaleString('vi-VN')}</b></article>`).join('')}</div><div class="tx2090-actions"><button class="admin-btn ghost" id="tx2090Refresh">Làm mới</button><button class="admin-btn primary" id="tx2090Scan">Scan stale pending</button><span>${ready?'Transaction posture sạch.':'Launch gate sẽ HOLD khi còn stale/recovery/mismatch.'}</span></div><div class="tx2090-grid"><section><h3>Recovery Queue</h3>${q.length?q.map(x=>`<article class="tx2090-row"><div><b>${esc(x.operation)} · ${esc(x.scope)}</b><small>${esc(x.tx_key)} · ${fmt(x.created_at)}</small><small>${esc(x.reason)}</small></div><button class="admin-btn ghost small" data-tx-resolve="${esc(x.id)}">Điều tra</button></article>`).join(''):'<p class="muted">Không có transaction cần recovery.</p>'}</section><section><h3>Receipt gần nhất</h3>${(st.receipts||[]).slice(0,20).map(x=>`<article class="tx2090-row"><div><b>${esc(x.operation)} · ${esc(x.status)}</b><small>${esc(x.scope)} · attempt ${Number(x.attempt_count||0)} · ${fmt(x.updated_at)}</small><code>${esc(x.tx_key)}</code></div></article>`).join('')||'<p class="muted">Chưa có receipt.</p>'}</section></div><p class="muted">Role: ${esc(st.role||'—')} · Server ${fmt(st.serverTime)}</p>`;$('#tx2090Refresh').onclick=load;$('#tx2090Scan').onclick=scan;root.querySelectorAll('[data-tx-resolve]').forEach(b=>b.onclick=()=>resolve(b.dataset.txResolve))}
async function load(){try{ensure();st=await api('state');render()}catch(e){const r=ensure();if(r)r.innerHTML=`<p class="muted">${esc(e.message)}</p>`}}
async function scan(){if(!confirm('Scan transaction pending quá 5 phút? Hệ thống chỉ đưa vào recovery queue, không tự cấp/trừ tài nguyên.'))return;try{await api('scan',{limit:100});await load()}catch(e){alert(e.message)}}
async function resolve(id){const choice=prompt('Kết quả điều tra: committed / not_committed / cancelled','not_committed');if(!choice)return;const resolution=choice.trim();if(!['committed','not_committed','cancelled'].includes(resolution))return alert('Giá trị không hợp lệ.');const note=prompt('Ghi chú bằng chứng điều tra (>=12 ký tự):','');if(!note||note.trim().length<12)return alert('Cần ghi chú rõ bằng chứng.');try{await api('resolve',{recoveryId:id,resolution,note});await load()}catch(e){alert(e.message)}}
function bind(){document.querySelector('[data-page="reliability"]')?.addEventListener('click',()=>setTimeout(load,100));ensure()}
window.POWDER_ADMIN_TX_V2090={version:'20.9.0',load};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
