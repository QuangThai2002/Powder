(()=>{'use strict';
const VERSION='20.3.0',URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';let state=null;
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function ses(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,p={}){const s=ses();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-security',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...p}),cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function ensure(){let h=$('#security2030');if(h)return h;const page=$('#pageOps')||$('#pageReliability')||document.querySelector('main');if(!page)return null;const sec=document.createElement('section');sec.className='admin-card security2030';sec.innerHTML='<div class="card-head"><div><p class="eyebrow">SECURITY · 20.3.0</p><h2>Security & Anti-Abuse Posture</h2><p class="muted">Server-authoritative audit: RLS, direct writes, sensitive RPC grants, reward/PvP idempotency, CAS, rate guard và Admin role separation.</p></div><span id="security2030Badge">CHECK</span></div><div id="security2030"></div>';page.appendChild(sec);return $('#security2030')}
const yn=v=>v?'<b class="sec-ok">PASS</b>':'<b class="sec-bad">FAIL</b>';
function render(){const h=ensure();if(!h||!state)return;const p=state.posture||{},c=p.counts||{},g=p.guards||{},roles=p.adminRoles||{},probe=g.duplicateProbe||{};$('#security2030Badge').textContent=p.ready?'READY':'HOLD';h.innerHTML=`<div class="sec-grid"><div><small>RLS missing</small><b>${c.rlsMissing??'-'}</b></div><div><small>Direct-write violations</small><b>${c.directWriteViolations??'-'}</b></div><div><small>Sensitive RPC client EXECUTE</small><b>${c.sensitiveRpcClientExec??'-'}</b></div><div><small>Invalid Admin roles</small><b>${c.invalidAdminRoles??'-'}</b></div></div><div class="sec-guards"><span>Reward idempotency ${yn(g.rewardIdempotency)}</span><span>PvP idempotency ${yn(g.pvpIdempotency)}</span><span>Device revoke ${yn(g.deviceRevoke)}</span><span>Cloud CAS service-only ${yn(g.cloudCasServiceOnly)}</span><span>Rate guard service-only ${yn(g.rateGuardServiceOnly)}</span><span>Duplicate probe ${yn(probe.duplicateBlocked&&probe.cleaned)}</span></div><div class="sec-role"><b>Role hiện tại: ${esc(state.role)}</b><span>Owner ${roles.owner||0} · Admin ${roles.admin||0} · Support ${roles.support||0}</span></div><div class="sec-actions"><button data-refresh>Refresh posture</button>${state.role==='support'?'':'<button data-probe>Run duplicate probe</button>'}${state.role==='owner'?'<button data-snapshot>Save signed review snapshot</button>':''}</div><p class="muted">Support chỉ xem; Admin có thể chạy probe; chỉ Owner được lưu Production Security snapshot.</p>`;h.querySelector('[data-refresh]').onclick=load;h.querySelector('[data-probe]')?.addEventListener('click',async()=>{await api('run_probe');await load()});h.querySelector('[data-snapshot]')?.addEventListener('click',async()=>{if(!confirm('Lưu Production Security snapshot 20.3?'))return;await api('snapshot');await load()})}
async function load(){try{state=await api('state');render()}catch(e){const h=ensure();if(h)h.innerHTML=`<p class="is-error">${esc(e.message)}</p>`}}
function boot(){ensure();setTimeout(load,1300)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.POWDER_ADMIN_SECURITY_V2030={version:VERSION,load,state:()=>state};
})();
