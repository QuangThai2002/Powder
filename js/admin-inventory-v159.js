(()=>{'use strict';const URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[m]));let st=null;function sess(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}async function api(action,p={}){const s=sess();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-inventory',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...p})});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={error:t}}if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}function toast(t){const e=$('#adminToast');if(!e)return;e.textContent=t;e.hidden=false;clearTimeout(e._t);e._t=setTimeout(()=>e.hidden=true,3000)}function current(){return st?.profile?.id||''}
function render(){const host=$('#inventoryAdminDetail');if(!host)return;if(!st){host.innerHTML='<p class="muted">Tìm UID hoặc email để mở kho người chơi.</p>';return}const raw=st.raw||{},eq=st.equipment||[],arts=st.artifacts||[],items=st.items||[],snaps=st.snapshots||[],hist=st.history||[];host.innerHTML=`<div class="inv159-admin-summary"><div><small>TAMER</small><b>${esc(st.profile.display_name)}</b><small>${esc(st.profile.tamer_uid)}</small></div><div><small>TRANG BỊ</small><b>${eq.reduce((a,x)=>a+Number(x.copies||0),0)} bản</b><small>${eq.length} loại</small></div><div><small>CỔ VẬT</small><b>${arts.length} bản</b></div><div><small>VẬT PHẨM</small><b>${items.reduce((a,x)=>a+Number(x.quantity||0),0)}</b></div></div><div class="inv159-admin-columns"><section class="admin-card"><div class="card-head"><div><small>EVENT ITEMS</small><h2>Cấp / thu hồi vật phẩm</h2></div></div><div class="inv159-actions"><select id="invAdminItem">${(st.itemCatalog||[]).map(x=>`<option value="${esc(x.item_id)}">${esc(x.name)} · ${esc(x.rarity)}</option>`).join('')}</select><input id="invAdminQty" type="number" value="1" min="1"><button class="admin-btn primary" data-inv-act="grant_item">Cấp</button><button class="admin-btn danger" data-inv-act="remove_item">Thu hồi</button></div><div class="inv159-list">${items.length?items.map(x=>`<div class="inv159-row"><span><b>${esc(x.item_catalog?.name||x.item_id)}</b><small>${esc(x.item_id)}${x.is_locked?' · 🔒':''}</small></span><strong>×${Number(x.quantity||0)}</strong></div>`).join(''):'<p class="muted">Chưa có vật phẩm sự kiện.</p>'}</div></section><section class="admin-card"><div class="card-head"><div><small>SAFE SNAPSHOTS</small><h2>Phục hồi kho</h2></div><button class="admin-btn ghost" data-inv-snapshot>Tạo bản sao</button></div><div class="inv159-note">Phục hồi snapshot thay thế toàn bộ Trang bị, Cổ vật, loadout và vật phẩm của người chơi. Economy Coin/Tinh Tệ không bị quay ngược.</div><div class="inv159-list">${snaps.map(x=>`<div class="inv159-row ${x.restored_at?'':'inv159-danger'}"><span><b>${new Date(x.created_at).toLocaleString('vi-VN')}</b><small>${esc(x.reason||'Snapshot')}${x.restored_at?' · đã dùng':''}</small></span><button class="admin-btn ${x.restored_at?'ghost':'danger'}" data-restore-snapshot="${esc(x.id)}" ${x.restored_at?'disabled':''}>Phục hồi</button></div>`).join('')||'<p class="muted">Chưa có snapshot.</p>'}</div></section></div><div class="inv159-admin-columns"><section class="admin-card"><div class="card-head"><div><small>EQUIPMENT</small><h2>Trang bị server</h2></div></div><div class="inv159-list">${eq.slice(0,80).map(x=>`<div class="inv159-row"><span><b>${esc(x.equipment_catalog?.name||x.equipment_id)} +${Number(x.enhance_level||0)}</b><small>${esc(x.equipment_catalog?.set_id||'')} · ${esc(x.equipment_catalog?.slot||'')}</small></span><strong>×${Number(x.copies||0)}</strong></div>`).join('')||'<p class="muted">Trống.</p>'}</div></section><section class="admin-card"><div class="card-head"><div><small>ARTIFACTS</small><h2>Cổ vật server</h2></div></div><div class="inv159-list">${arts.slice(0,80).map(x=>`<div class="inv159-row"><span><b>${esc(x.artifact_catalog?.name||x.artifact_id)} +${Number(x.upgrade_level||0)}</b><small>${esc(x.instance_id)}${x.equipped_by?' · Pow '+esc(x.equipped_by):''}</small></span><span>${x.is_locked?'🔒':''}${x.is_favorite?'★':''}</span></div>`).join('')||'<p class="muted">Trống.</p>'}</div></section></div><section class="admin-card"><div class="card-head"><div><small>INVENTORY JOURNAL</small><h2>Lịch sử nhận/mất/thay đổi</h2></div></div><div class="inv159-list">${hist.map(x=>`<div class="inv159-row"><span><b>${esc(x.action)}</b><small>${esc(x.entity_type)} · ${esc(x.entity_id)}</small></span><small>${new Date(x.created_at).toLocaleString('vi-VN')}</small></div>`).join('')||'<p class="muted">Chưa có giao dịch kho.</p>'}</div></section>`;bindActions()}
function bindActions(){document.querySelectorAll('[data-inv-act]').forEach(b=>b.onclick=async()=>{try{b.disabled=true;const d=await api(b.dataset.invAct,{userId:current(),itemId:$('#invAdminItem').value,quantity:Number($('#invAdminQty').value)||1});st=d.state;render();toast('Đã cập nhật vật phẩm.')}catch(e){toast(e.message)}finally{b.disabled=false}});$('[data-inv-snapshot]')?.addEventListener('click',async()=>{try{const d=await api('snapshot',{userId:current(),reason:'Admin manual backup'});st=d.state;render();toast('Đã tạo bản sao kho.')}catch(e){toast(e.message)}});document.querySelectorAll('[data-restore-snapshot]').forEach(b=>b.onclick=async()=>{if(!confirm('Phục hồi toàn bộ kho về thời điểm snapshot này?'))return;try{const d=await api('restore_snapshot',{userId:current(),snapshotId:b.dataset.restoreSnapshot,reason:'Admin recovery'});st=d.state;render();toast('Đã phục hồi kho thành công.')}catch(e){toast(e.message)}})}
async function search(){const q=$('#inventoryAdminSearch')?.value.trim();if(!q)return;try{$('#inventoryAdminState').textContent='Đang tải...';const d=await api('state',{query:q});st=d.state;render();$('#inventoryAdminState').textContent='Đã đồng bộ'}catch(e){$('#inventoryAdminState').textContent=e.message;toast(e.message)}}
function bind(){document.querySelector('#adminNav [data-page="inventory"]')?.addEventListener('click',()=>setTimeout(()=>{$('#adminPageTitle').textContent='Kho & phục hồi';render()},0));$('#inventoryAdminSearchBtn')?.addEventListener('click',search);$('#inventoryAdminSearch')?.addEventListener('keydown',e=>{if(e.key==='Enter')search()})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();})();
