(()=>{'use strict';
const C=window.POWDER_COMBAT_CORE_V7,E=window.POWDER_ENGINE,D=window.POWDER_DATA,W=window.POWDER_WORLD_V9,WD=window.POWDER_WORLD_DATA_V9,M=window.POWDER_MASTER_DATA_V9;
if(!C?.BattleCore||!E||!W||!WD)return;
const P=C.BattleCore.prototype,clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
const oldStart=P.start,oldBegin=P.beginTurn,oldFinish=P.finishCurrent,oldCan=P.canUse,oldExec=P.executeAction,oldEffective=P.effective,oldTargets=P.targetsFor,oldApply=P.applyStatus,oldActionInfo=P.actionInfo,oldMechanicEvent=P.processMechanicEvent;
const HARD=new Set(['Stun','Freeze','Petrify','Sleep','Bind','Paralysis']);
const BAD_BUILTIN=new Set(['Burn','Poison','Stun','Freeze','Slow','Curse']);
const ROLE_SUPPORT=new Set(['mage','enchanter','healer','musician']);
const RARITY={common:1,rare:2,super_rare:3,epic:4,legendary:5,mythic:6,ancient:7};
function v81Stage(u){if(['starter_fire_flarion','starter_water_aquelion','starter_leaf_sylvion'].includes(u?.powId))return null;const v=window.POWDER_SKILL_V81?.pows?.[u?.powId];if(!v)return null;const star=Math.max(0,Number(u?.owned?.stars??v.nativeStar)||0);if(star<Number(v.nativeStar||0))return null;return (v.starProgression||[]).find(x=>Number(x.star)===star)||(v.starProgression||[]).at(-1)||null}
function directScale(u){return Number(v81Stage(u)?.coefficient)||1}
function effectScale(u){return Number(v81Stage(u)?.effect)||1}
const FROSTMAW_FORM_LABEL={kun:'CÔN · BẮC MINH CỰ NGƯ',peng:'BẰNG · THÁI CỔ BĂNG ĐIỂU'};
function frostmawPow(){return D.pows.find(x=>x.id==='frostmaw')||null}
function frostmawFormData(form){return frostmawPow()?.combatForms?.[form]||null}
function setFrostmawForm(core,u,form,reason=''){
  if(!u||u.powId!=='frostmaw'||!['kun','peng'].includes(form))return false;
  init(u);
  const current=u.combatForm||u.v9.battleFlags.frostmawForm||'peng';
  if(current===form)return false;
  const fromData=frostmawFormData(current),toData=frostmawFormData(form);
  const fromAsset=u.asset||fromData?.asset||u.combatBaseAsset;
  const toAsset=toData?.asset||u.asset;
  u.combatForm=form;u.combatFormLabel=toData?.label||toData?.name||form;u.asset=toAsset;u.v9.battleFlags.frostmawForm=form;
  core.pushEvent('frostmaw-form',{sourceId:u.id,powId:u.powId,from:current,to:form,fromAsset,toAsset,label:FROSTMAW_FORM_LABEL[form]||form,reason});
  core.pushLog(`${u.name} chuyển sang ${form==='kun'?'Dạng Côn · Bắc Minh Cự Ngư':'Dạng Bằng · Thái Cổ Băng Điểu'}.`,u.side==='player'?'player':'enemy');
  return true;
}
const ELEMENT_MATRIX={
 fire:{fire:.9,steel:1.25,water:.8,leaf:1.25,earth:1,wind:1,lightning:1,lava:1,storm:1,ice:1.2,poison:1,light:1,dark:1},
 steel:{fire:.8,steel:.9,water:1,leaf:1,earth:.8,wind:1.25,lightning:1,lava:.75,storm:1,ice:1.2,poison:1.2,light:1.2,dark:1},
 water:{fire:1.25,steel:1,water:.9,leaf:.8,earth:1.25,wind:1,lightning:.8,lava:1.2,storm:.75,ice:1,poison:.75,light:1,dark:1},
 leaf:{fire:.8,steel:1,water:1.25,leaf:.9,earth:1.25,wind:.8,lightning:1.25,lava:.75,storm:.75,ice:.75,poison:.75,light:1,dark:1},
 earth:{fire:1,steel:1.25,water:.8,leaf:.8,earth:.9,wind:1,lightning:1.25,lava:.75,storm:1.2,ice:.75,poison:.75,light:1,dark:1},
 wind:{fire:1,steel:.8,water:1,leaf:1.25,earth:1,wind:.9,lightning:.8,lava:1,storm:.75,ice:1.2,poison:1.2,light:1,dark:1},
 lightning:{fire:1,steel:1,water:1.25,leaf:.8,earth:.8,wind:1.25,lightning:.9,lava:1,storm:1,ice:1.2,poison:1,light:1,dark:.75},
 lava:{fire:1.05,steel:1.35,water:.8,leaf:1.35,earth:1.35,wind:1.05,lightning:1.05,lava:.9,storm:1,ice:1.25,poison:1,light:1,dark:1},
 storm:{fire:1.05,steel:1.05,water:1.35,leaf:1.35,earth:.8,wind:1.35,lightning:1.05,lava:1,storm:.9,ice:1.25,poison:1,light:.8,dark:.8},
 ice:{fire:.8,steel:.8,water:1.05,leaf:1.35,earth:1.35,wind:1.35,lightning:.8,lava:.8,storm:.8,ice:.9,poison:1,light:1,dark:1},
 poison:{fire:1.05,steel:.8,water:1.35,leaf:1.35,earth:1.35,wind:.8,lightning:1.05,lava:1,storm:1,ice:1,poison:.9,light:.8,dark:1},
 light:{fire:1.05,steel:.8,water:1.05,leaf:1.05,earth:1.05,wind:1.05,lightning:1.05,lava:1,storm:1.25,ice:1,poison:1.25,light:.9,dark:1.25},
 dark:{fire:1.05,steel:1.05,water:1.05,leaf:1.05,earth:1.05,wind:1.05,lightning:1.35,lava:1,storm:1.25,ice:1,poison:1,light:1.25,dark:.9}
};
function elemMult(att,target){let v=ELEMENT_MATRIX[att?.element]?.[target?.element]??1;if(target?.boss)v=Math.min(1.2,v);return v}
function init(u){
  if(!u)return;
  u.v9=u.v9||{};
  const d={cooldowns:{},round:-1,roundFlags:{},battleFlags:{},totalManaSpent:0,lastMainTarget:null,sameTargetCount:0,gearProc:{},artifactProc:{},role:{},mythic:{resource:0,breakthrough:false,domain:false},receivedDirect:false};
  for(const [k,v] of Object.entries(d)){
    if(u.v9[k]===undefined||u.v9[k]===null)u.v9[k]=(v&&typeof v==='object'&&!Array.isArray(v))?{...v}:v;
  }
  u.v9.cooldowns=u.v9.cooldowns&&typeof u.v9.cooldowns==='object'?u.v9.cooldowns:{};
  u.v9.roundFlags=u.v9.roundFlags&&typeof u.v9.roundFlags==='object'?u.v9.roundFlags:{};
  u.v9.battleFlags=u.v9.battleFlags&&typeof u.v9.battleFlags==='object'?u.v9.battleFlags:{};
  u.v9.gearProc=u.v9.gearProc&&typeof u.v9.gearProc==='object'?u.v9.gearProc:{};
  u.v9.artifactProc=u.v9.artifactProc&&typeof u.v9.artifactProc==='object'?u.v9.artifactProc:{};
  u.v9.role=u.v9.role&&typeof u.v9.role==='object'?u.v9.role:{};
  u.v9.mythic=u.v9.mythic&&typeof u.v9.mythic==='object'?u.v9.mythic:{resource:0,breakthrough:false,domain:false};
  if(u.v9.mythic.resource===undefined)u.v9.mythic.resource=0;
  if(u.v9.mythic.breakthrough===undefined)u.v9.mythic.breakthrough=false;
  if(u.v9.mythic.domain===undefined)u.v9.mythic.domain=false;
  if(u.powId==='frostmaw'){const f=u.combatForm||u.v9.battleFlags.frostmawForm||'peng',fd=frostmawFormData(f);u.combatForm=f;u.v9.battleFlags.frostmawForm=f;if(fd?.asset&&!u.asset)u.asset=fd.asset;}
  const l=W.getLoadout(u.powId);u.v9.loadout=l;u.v9.artifact=l.artifact?(W.artifactOf?.(l.artifact)||WD.artifacts.find(a=>a.id===l.artifact)):null;u.v9.sets=l.sets||[];
}
function newRound(u,round){init(u);if(u.v9.round!==round){u.v9.round=round;u.v9.roundFlags={};}}
function art(u){return u?.v9?.artifact}
function setName(setId){return WD.equipmentSets.find(x=>x.id===setId)?.name||''}
function hasSet(u,name,tier=2){init(u);return (u.v9.sets||[]).some(s=>setName(s.setId)===name&&Number(s.tier)>=tier)}
function bad(t){return Object.keys(t?.statuses||{}).some(s=>BAD_BUILTIN.has(s)||!['Defense Up','Attack Up','AP Up','Shield','Regeneration'].includes(s))||Object.keys(t?.customStatuses||{}).some(s=>!/Up|Shield|Guard|Oath|Buff/i.test(s))}
function cursed(t){return Boolean(t?.statuses?.Curse||t?.customStatuses?.Curse||Object.keys(t?.customStatuses||{}).some(k=>/curse|nguyền/i.test(k)))}
function slowed(t){return Boolean(t?.statuses?.Slow||t?.statuses?.Freeze||t?.customStatuses?.Paralysis||Object.keys(t?.customStatuses||{}).some(k=>/slow|frost|hàn khí|speed down/i.test(k)))}
function markedBy(t,u){return Object.values(t?.customStatuses||{}).some(x=>x?.sourceId===u?.id&&x?.kind==='mark')}
function debuffedBy(t,u){return Object.entries(t?.statuses||{}).some(([k,v])=>v?.sourceId===u?.id&&!['Defense Up','Attack Up','AP Up','Shield','Regeneration'].includes(k))||Object.values(t?.customStatuses||{}).some(x=>x?.sourceId===u?.id&&x?.kind==='debuff')}
function controlled(t){return slowed(t)||Object.keys(t?.statuses||{}).some(k=>HARD.has(k))||Object.keys(t?.customStatuses||{}).some(k=>HARD.has(k)||/meter down|tempo/i.test(k))}
function front(t){return Number(t?.teamIndex||0)<2}
function strong(att,target){return elemMult(att,target)>1.0001}
function statMods(u){return Object.values(u?.customStatuses||{}).filter(v=>v?.stat&&Number(v.pct))}
function incomingReduction(u){let r=0;for(const v of Object.values(u?.customStatuses||{}))r+=Math.max(0,Number(v?.incomingDR)||0);return Math.min(.5,r)}
function healingTaken(u){let m=1;for(const v of Object.values(u?.customStatuses||{}))m*=1+Math.min(0,Number(v?.healingReceived)||0);return Math.max(u?.boss?.85:.6,m)}
const oldHeal=E.healTarget;E.healTarget=function(target,amount,opts){const pressure=C.combatPressure?.(target?.combatRound||1)?.sustain||1;return oldHeal(target,Math.max(0,Math.round(amount*pressure)),opts)};
const oldShield=E.addShield;E.addShield=function(target,amount,source,duration,options){
  const pressure=C.combatPressure?.(target?.combatRound||1)?.sustain||1;
  const scaled=amount==null?amount:Math.max(1,Math.round((Number(amount)||0)*pressure));
  return oldShield(target,scaled,source,duration,options);
};
P.effective=function(u){const s=oldEffective.call(this,u);let defDown=0;for(const m of statMods(u)){if(s[m.stat]==null)continue;if(m.stat==='def'&&m.pct<0){defDown+=m.pct;continue}s[m.stat]=Math.max(1,Math.round(s[m.stat]*(1+m.pct)))}if(defDown<0&&s.def!=null)s.def=Math.max(1,Math.round(s.def*(1+Math.max(-.40,defDown))));for(const v of Object.values(u?.customStatuses||{}))if(Number(v?.critFlat))s.critRate=clamp(Number(s.critRate||0)+Number(v.critFlat),0,100);return s};
function desc(a){return String(a?.rulesText||a?.description||'')}
function totalCoeff(a,index=0){const d=desc(a),base={...(a?.canonicalDamageCoefficients||a?.coefficients||{})};
  const total=d.match(/tổng\s+(\d+(?:\.\d+)?)%\s*(ATK|AP|DEF|HP)/i);if(total)return{[total[2].toLowerCase()]:Number(total[1])};
  const multi=d.match(/(?:bắn|đánh|chém|tung|liên kích)?\s*(\d+)\s*(?:phát|nhát|lần)[^.;]{0,55}?mỗi\s*(?:phát|nhát|lần)[^.;]{0,25}?(\d+(?:\.\d+)?)%\s*(ATK|AP|DEF|HP)/i);if(multi){const out={[multi[3].toLowerCase()]:Number(multi[1])*Number(multi[2])};for(const [k,v] of Object.entries(base))if(!out[k])out[k]=v;return out}
  const twice=d.match(/(?:gây\s+)?(\d+(?:\.\d+)?)%\s*(ATK|AP|DEF)[^.;]{0,22}?(?:hai|2)\s*lần/i);if(twice)return{[twice[2].toLowerCase()]:Number(twice[1])*2};
  const pri=d.match(/mục tiêu (?:chính|trước)[^.;]{0,55}?(\d+(?:\.\d+)?)%\s*(ATK|AP|DEF)/i),sec=d.match(/mục tiêu (?:phụ|sau)[^.;]{0,55}?(\d+(?:\.\d+)?)%\s*(ATK|AP|DEF)/i);if(pri&&sec)return{[(index>0?sec:pri)[2].toLowerCase()]:Number((index>0?sec:pri)[1])};
  return base;
}
function ignoreDefPct(att,target,a){const d=desc(a);if(!a?.masterEffects?.ignoreDef&&!/bỏ qua\s+\d+(?:\.\d+)?%\s*DEF/i.test(d))return 0;
  // Conditional rules are deliberately conservative: ignore DEF is only applied when the tooltip condition is actually met.
  if(/mục tiêu(?:\s+đang|\s+mang)?\s+Nguyền[^.;]{0,90}?bỏ qua/i.test(d)&&!cursed(target))return 0;
  if(/đủ\s+3\s+Vết Bóng/i.test(d)){const r=att?.v9?.resource;if(!r||Number(r.value||0)<3)return 0;}
  let m=d.match(/bỏ qua\s+(\d+(?:\.\d+)?)%\s*DEF/i),pct=m?Number(m[1])/100:0;
  // Ironmantis: each Từ Nhận affects one of three hits. The compact damage engine uses the mathematically equivalent average penetration.
  if(/mỗi Từ Nhận[^.;]{0,80}?bỏ qua\s+(\d+(?:\.\d+)?)%\s*DEF/i.test(d)){
    const per=Number((d.match(/mỗi Từ Nhận[^.;]{0,80}?bỏ qua\s+(\d+(?:\.\d+)?)%\s*DEF/i)||[])[1]||10)/100;
    const stacks=Math.min(3,Number(att?.v9?.resource?.value||0));pct=Math.min(.30,per*stacks/3);
  }
  return clamp(pct,0,.50);
}
function masterHitRoll(core,att,target,a,key='skill1'){
 const as=core.effective(att),ds=core.effective(target),unavoidable=Boolean(a?.sureHit||a?.unavoidable||a?.tags?.includes?.('UNAVOIDABLE'));
 let evasion=clamp(Number(ds.evasion||0),0,['wind','storm','dark'].includes(target?.element)?75:60);
 const area=Boolean(C.isArea?.(a)||a?.target==='all'||a?.target==='all-enemies'||a?.target==='two-enemies');if(area)evasion*=.70;
 const accBonus=(Number(as.accuracy||100)-100)+(key==='ultimate'?10:key==='exclusive'?15:0),hitChance=unavoidable?100:clamp(100+accBonus-evasion,25,100),hit=unavoidable||core.rng()*100<hitChance;
 return{hit,evaded:!hit,hitChance,unavoidable};
}
function coeffDamage(core,att,target,a,index=0){const co=totalCoeff(a,index);if(!Object.keys(co).length)return null;const sx=directScale(att);for(const k of Object.keys(co))co[k]=Number(co[k]||0)*sx;const as=core.effective(att),ds=core.effective(target);let raw=(as.atk*(co.atk||0)+as.ap*(co.ap||0)+as.def*(co.def||0)+att.maxHp*(co.hp||0))/100;if(raw<=0)return null;const physical=(co.atk||0)+(co.def||0)>=(co.ap||0),ignore=ignoreDefPct(att,target,a),defUsed=ds.def*(1-ignore),factor=E.scaledDefenseFactor(physical?as.atk:as.ap,defUsed,physical?.85:.70);let crit=core.rng()*100<as.critRate;if(a.masterEffects?.cannotCrit)crit=false;return{amount:Math.max(1,Math.round(raw*factor*elemMult(att,target)*(crit?as.critDamage/100:1))),crit,element:elemMult(att,target),ignoreDef:ignore}}
function roleBonus(u,a,key){init(u);let b=0;if(u.combatRole==='mage'&&key!=='basic'&&Number(a?.manaCost)>0){const seal=Number(u.v9.role.arcaneSeal||0);if(seal>=3){b+=.10;u.v9.role.arcaneSeal=0}}if(u.combatRole==='fighter'){b+=Math.min(.10,Number(u.v9.role.fury||0)*.02)}return b}
function gearArtifactDamage(core,att,target,a,key){init(att);newRound(att,core.state.round);let b=0;if(att.powId==='starter_fire_flarion'){const fs=flarionState(att);if(fs.awakened&&target?.statuses?.Burn)b+=.60;if(key==='skill1'&&att.v9.battleFlags.flarionSkill1Bonus){b+=att.v9.battleFlags.flarionSkill1Bonus;att.v9.battleFlags.flarionSkill1Bonus=0}}const A=art(att),code=A?.effect?.code,val=Number(A?.effect?.value)||0,hp=target.hp/Math.max(1,target.maxHp),round=core.state.round,rf=att.v9.roundFlags,af=att.v9.artifactProc,gf=att.v9.gearProc;
  switch(code){case'basic_damage':if(key==='basic')b+=val;break;case'ap_skill_damage':if(a.type==='magic'&&key!=='basic')b+=val;break;case'target_high_hp_damage':if(hp>.7)b+=val;break;case'target_low_hp_damage':if(hp<(Number(A?.order)===4?.4:.5))b+=val;break;case'target_low_hp_damage_30':if(hp<.3)b+=val;break;case'burn_damage':if(a.status==='Burn'||target.statuses?.Burn||/thiêu đốt/i.test(desc(a)))b+=val;break;case'poison_damage':if(a.status==='Poison'||target.statuses?.Poison||/độc/i.test(desc(a)))b+=val;break;case'after_basic_skill1':if(key==='skill1'&&rf.afterBasic){b+=val;rf.afterBasic=false}break;case'self_low_hp_damage':if(att.hp/att.maxHp<.5)b+=val;break;case'self_mark_damage':if(markedBy(target,att))b+=val;break;case'shield_target_damage':if(Number(target.shield)>0)b+=val;break;case'slowed_target_damage':if(slowed(target))b+=val;break;case'debuffed_target_damage':if(bad(target))b+=val;break;case'after_skill_basic':if(key==='basic'&&rf.afterSkill){b+=val;rf.afterSkill=false}break;case'self_debuff_target_damage':if(debuffedBy(target,att))b+=val;break;case'element_advantage_damage':if(strong(att,target))b+=val;break;case'first_skill_damage':if(['skill1','skill2'].includes(key)&&!af.firstSkill){b+=val;af.firstSkill=true}break;case'same_target_chain':if(att.v9.battleFlags.artChainReady&&att.v9.lastMainTarget===target.id){b+=val;att.v9.battleFlags.artChainReady=false}break;case'round3_damage':if(round>=3)b+=val;break;case'frontline_damage':if(front(target)||Number(target.presence)>=68)b+=val;break;case'reaction_damage':if(a.masterEffects?.followup||a.masterEffects?.counter||/truy kích|phản kích/i.test(desc(a)))b+=val;break;case'after_basic_skill_damage':if(key!=='basic'&&rf.afterBasic){b+=val;rf.afterBasic=false}break;case'after_hit_skill_damage':if(key!=='basic'&&rf.receivedHit){b+=val;rf.receivedHit=false}break;case'controlled_target_damage':if(controlled(target))b+=val;break;case'lava_dot_damage':if(att.element==='lava'||target.statuses?.Burn||a.status==='Burn')b+=val;break;case'poison_damage_antiheal':if(a.status==='Poison'||target.statuses?.Poison||/độc/i.test(desc(a)))b+=val;break;case'cursed_target_damage':if(cursed(target))b+=val;break;case'round3_skill_effect':if(round>=3&&['skill1','skill2'].includes(key))b+=val;break;case'first_round_skill_debuff':if(['skill1','skill2'].includes(key)&&bad(target)&&rf.art44!==round){b+=val;rf.art44=round}break;case'element_advantage_effect':if(strong(att,target))b+=val;break;case'ultimate_effect':if(key==='ultimate')b+=val;break;case'self_low_hp_effect':if(att.hp/att.maxHp<.4)b+=val;break;case'domain_skill_effect':if(['skill1','skill2'].includes(key)&&(core.activeDomain?.()||att.v9.mythic.domain))b+=val;break;case'after_reaction_effect':if(att.v9.battleFlags.afterReaction){b+=val;att.v9.battleFlags.afterReaction=false}break;}
  if(hasSet(att,'Tân Binh',2))b+=.08;if(hasSet(att,'Tân Binh',4)&&key==='basic'&&rf.set1Basic!==round){b+=.12;rf.set1Basic=round}
  if(hasSet(att,'Tinh Nhuệ',2)&&key==='skill1')b+=.12;if(hasSet(att,'Tinh Nhuệ',4)&&key==='skill1'&&att.v9.battleFlags.eliteSkill1){b+=.20;att.v9.battleFlags.eliteSkill1=false}
  if(hasSet(att,'Cuồng Công',2))b+=.18;if(hasSet(att,'Cuồng Công',4)&&att.v9.battleFlags.rageNext&&att.v9.lastMainTarget===target.id){b+=.30;att.v9.battleFlags.rageNext=false}
  if(hasSet(att,'Truy Kích',2)&&hp<.5)b+=.25;if(hasSet(att,'Truy Kích',4)&&key!=='basic'&&att.v9.battleFlags.afterKill){b+=.40;att.v9.battleFlags.afterKill=false}
  if(hasSet(att,'Điều Nhịp',2)&&Number(a.manaCost)>0)b+=.22;if(att.v9.battleFlags.manaTempoBoost&&key!=='basic'){b+=.15;att.v9.battleFlags.manaTempoBoost=false}if(hasSet(att,'Phá Giáp',2)&&Number(target.shield)>0)b+=.35;if(target.customStatuses?.[`V9 ArmorBreak ${att.id}`])b+=.30
  if(hasSet(att,'Bách Chiến',2)&&round>=3)b+=.30;if(hasSet(att,'Bách Chiến',4)&&round>=5)b+=Math.min(.48,.12*Math.floor((round-3)/2))
  if(hasSet(att,'Nguyên Tố Cộng Hưởng',2)&&strong(att,target))b+=.40;if(hasSet(att,'Tuyệt Kỹ',2)&&key==='ultimate')b+=.45;if(hasSet(att,'Tuyệt Kỹ',4)&&key==='skill1'&&att.v9.battleFlags.afterUlt){b+=.65;att.v9.battleFlags.afterUlt=false}
  if(hasSet(att,'Đột Phá',2)&&att.v9.mythic.breakthrough)b+=.50;if(hasSet(att,'Đột Phá',4)&&att.v9.mythic.breakthrough&&att.v9.mythic.postBreakActions<2&&['skill1','skill2'].includes(key))b+=.75
  if(hasSet(att,'Cổ Thần Điều Hòa',2)&&key==='skill2')b+=.60;
  b+=roleBonus(att,a,key);return Math.min(2.50,b)
}
function supportBonus(core,att,a,key){init(att);newRound(att,core.state.round);let b=0;const A=art(att),code=A?.effect?.code,val=Number(A?.effect?.value)||0,rf=att.v9.roundFlags;if(code==='shield_power'&&a.masterEffects?.shieldHpPct)b+=val;if(code==='heal_shield')b+=val;if(code==='ultimate_effect'&&key==='ultimate')b+=val;if(code==='self_low_hp_effect'&&att.hp/att.maxHp<.4)b+=val;if(code==='round3_skill_effect'&&core.state.round>=3&&['skill1','skill2'].includes(key))b+=val;if(code==='domain_skill_effect'&&['skill1','skill2'].includes(key)&&(core.activeDomain?.()||att.v9.mythic.domain))b+=val;if(hasSet(att,'Bền Bỉ',2)&&(a.masterEffects?.shieldHpPct||a.masterEffects?.shieldDefPct))b+=.20;if(hasSet(att,'Điều Nhịp',2)&&Number(a.manaCost)>0)b+=.22;if(att.v9.battleFlags.manaTempoBoost&&key!=='basic'){b+=.15;att.v9.battleFlags.manaTempoBoost=false}if(hasSet(att,'Tuyệt Kỹ',2)&&key==='ultimate')b+=.45;if(hasSet(att,'Tuyệt Kỹ',4)&&key==='skill1'&&att.v9.battleFlags.afterUlt){b+=.65;att.v9.battleFlags.afterUlt=false}if(hasSet(att,'Đột Phá',2)&&att.v9.mythic.breakthrough)b+=.50;if(hasSet(att,'Đột Phá',4)&&att.v9.mythic.breakthrough&&att.v9.mythic.postBreakActions<2&&['skill1','skill2'].includes(key))b+=.75;if(hasSet(att,'Thiên Hộ',2))b+=.65;if(hasSet(att,'Cổ Thần Điều Hòa',2)&&key==='skill2')b+=.60;if((a.masterEffects?.healHpPct||a.masterEffects?.healApPct)&&att.teamSynergy?.healingPct)b+=Number(att.teamSynergy.healingPct)||0;if((a.masterEffects?.shieldHpPct||a.masterEffects?.shieldDefPct)&&att.teamSynergy?.shieldPct)b+=Number(att.teamSynergy.shieldPct)||0;return Math.min(2.50,b)}
function shield(core,att,target,a,m=1){const e=a.masterEffects||{};if(!e.shieldHpPct&&!e.shieldDefPct)return 0;let v=(att.maxHp*(e.shieldHpPct||0)+core.effective(att).def*(e.shieldDefPct||0))*m;const got=E.addShield?.(target,Math.max(1,Math.round(v)),att,2,{capRatio:target.combatRole==='tank'?.80:.60});return Math.max(0,Number(got?.added??got)||0)}
function applyStat(target,stat,pct,source,turns=2,kind='buff'){if(!target||!stat||!pct)return;const sign=pct>0?'Up':'Down',key=`V9 ${stat.toUpperCase()} ${sign} ${source.id}`;target.customStatuses[key]={turns,stat,pct,sourceId:source.id,kind}}
function alliesOf(core,att){return att.side==='player'?core.living('player'):core.living('enemy')}
function enemiesOf(core,att){return att.side==='player'?core.living('enemy'):core.living('player')}
function lowAllies(core,att,n=1,excludeSelf=false){return [...alliesOf(core,att)].filter(x=>!excludeSelf||x.id!==att.id).sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp).slice(0,n)}
function effectTargets(core,att,a,targets,offensive,kind){if(!offensive)return targets;if(a?.canonicalDualMode&&['heal','shield','buff','mana'].includes(kind))return[];const d=desc(a);
  if(kind==='heal'&&/(?:hồi|phục hồi)[^.;]{0,40}?(?:toàn đội|đội)|hồi toàn đội|hồi đội/i.test(d))return alliesOf(core,att);
  if(kind==='shield'&&/(?:Khiên|khiên)[^.;]{0,45}?(?:toàn đội|đội)|Khiên đội|khiên đội/i.test(d))return alliesOf(core,att);
  if(kind==='buff'&&/(?:toàn đội|đội)[^.;]{0,80}?(?:tăng|\+)|(?:tăng|\+)[^.;]{0,50}?(?:toàn đội|đội)/i.test(d))return alliesOf(core,att);
  if(kind==='mana'&&/(?:toàn đội|đồng minh|đội)[^.;]{0,70}?hồi\s+\d+\s*Mana/i.test(d))return alliesOf(core,att);
  if(/bản thân và (?:đồng minh|chủ lực) thấp HP/i.test(d))return[att,...lowAllies(core,att,1,true)];
  if(/đồng minh (?:có )?(?:%?HP )?thấp nhất|đồng minh thấp HP/i.test(d))return lowAllies(core,att,1,true);
  return[att];
}
function applyBuffOp(core,t,b,att,a,effectMultiplier=1){let stat=String(b?.stat||''),pct=(Number(b?.pct)||0)*clamp(Number(effectMultiplier)||1,.20,1.36);if(!stat||!pct)return;const d=desc(a);
  if(stat==='atk'&&/ATK\/AP\/DEF/i.test(d)){const st=core.effective(t),best=st.def>=Math.max(st.atk,st.ap)?'def':st.ap>st.atk?'ap':'atk';applyStat(t,best,pct,att,2,'buff');return}
  if(stat==='atk'&&/ATK\/AP/i.test(d)){if(/theo chỉ số chính/i.test(d)){const st=core.effective(t),best=st.ap>st.atk?'ap':'atk';applyStat(t,best,pct,att,2,'buff')}else{applyStat(t,'atk',pct,att,2,'buff');applyStat(t,'ap',pct,att,2,'buff')}return}
  applyStat(t,stat,pct,att,2,'buff');
}
function protectionSpec(a){const d=desc(a);let m=d.match(/nhận thay\s+(\d+(?:\.\d+)?)%/i)||d.match(/chia\s+(\d+(?:\.\d+)?)%\s+sát thương/i);if(!m)return null;const pct=Math.min(.70,Number(m[1])/100);const rm=d.match(/phần chuyển(?:\s+sang)?\s+giảm(?:\s+thêm)?\s+(\d+(?:\.\d+)?)%/i)||d.match(/phần\s+[\p{L}Đđ]+\s+nhận\s+giảm\s+(\d+(?:\.\d+)?)%/iu);const hm=d.match(/(?:trong\s+\d+\s+lượt\s*(?:\/|hoặc)\s*)?(\d+)\s*đòn(?:\s+đơn|\s+mục tiêu)?/i);const tm=d.match(/(?:trong\s+)?(\d+)\s*lượt/i);return{pct,reduction:Math.min(.50,Number(rm?.[1]||0)/100),hits:Math.max(1,Number(hm?.[1]||99)),turns:Math.max(1,Number(tm?.[1]||2))}}
function gainCore1850(core,u,amount=1,reason='Utility 18.5'){const ch=window.POWDER_MECHANICS_V2?.gainResource?.(u,amount,reason);if(ch?.delta>0)core.emitCoreResource?.(u,ch);return ch}
function installProtection(core,att,a,targets,effectMultiplier=1){const spec=protectionSpec(a);if(!spec)return 0;const d=desc(a),fx=clamp(Number(effectMultiplier)||1,.20,1.36);spec.pct=Math.min(.70,spec.pct*fx);spec.reduction=Math.min(.50,spec.reduction*fx);let protectedTargets=targets.filter(t=>t&&t.id!==att.id);
  if(/bảo hộ toàn bộ tuyến sau/i.test(d))protectedTargets=alliesOf(core,att).filter(t=>Number(t.teamIndex)>=2&&t.id!==att.id);
  const oathMention=/Lời Thề/i.test(d),altPenalty=Number((d.match(/đồng minh khác[^.;]{0,40}?hiệu quả giảm\s+(\d+(?:\.\d+)?)%/i)||[])[1]||0)/100;
  if(oathMention&&protectedTargets[0]&&!att.coreRuntime?.oathTargetId){att.coreRuntime=att.coreRuntime||{};att.coreRuntime.oathTargetId=protectedTargets[0].id;core.pushEvent('oath-lock',{sourceId:att.id,targetId:protectedTargets[0].id,ability:a.name,utility1850:true});}
  const gainOnSplit=/(?:mỗi\s+đòn\s+được\s+chia[^.;]{0,35}?(?:tạo|nhận)|mỗi\s+lần\s+chia[^.;]{0,35}?(?:tạo|nhận))\s+1\s+/i.test(d);
  let installed=0;for(const t of protectedTargets){let mult=1;if(altPenalty&&att.coreRuntime?.oathTargetId&&att.coreRuntime.oathTargetId!==t.id)mult=Math.max(.25,1-altPenalty);t.customStatuses[`V9 Protection ${att.id}`]={turns:spec.turns,hits:spec.hits,pct:spec.pct*mult,reduction:spec.reduction*mult,protectorId:att.id,sourceId:att.id,kind:'protection',gainOnSplit,abilityId:a.id,abilityName:a.name};installed++;core.pushEvent('protection-apply',{sourceId:att.id,targetId:t.id,pct:spec.pct*mult,reduction:spec.reduction*mult,hits:spec.hits,turns:spec.turns,ability:a.name,utility1850:true});}return installed;
}
function strongestProtection(core,target){const list=Object.entries(target?.customStatuses||{}).filter(([,v])=>v?.kind==='protection'&&Number(v.hits||0)>0).map(([k,v])=>({k,v,protector:core.allUnits.find(x=>x.id===v.protectorId&&!x.defeated)})).filter(x=>x.protector);return list.sort((a,b)=>Number(b.v.pct||0)-Number(a.v.pct||0))[0]||null}
function applyCrit(target,points,source,turns=1){if(!target||!points)return;target.customStatuses[`V9 CRIT Up ${source.id}`]={turns,critFlat:Number(points),sourceId:source.id,kind:'buff'}}
function explicitMeterGain(a){const d=desc(a);if(/\+\s*\d+(?:\.\d+)?%\s*\/\s*\+/.test(d))return 0;let m=d.match(/(?:toàn đội|\bđội\b)[^.;]{0,55}?\+\s*(\d+(?:\.\d+)?)%\s*thanh lượt/i);if(!m)m=d.match(/chọn một đồng minh[^.;:]{0,40}:?[^.;]{0,30}?\+\s*(\d+(?:\.\d+)?)%\s*thanh lượt/i);return m?Math.min(35,Number(m[1])):0}
function explicitCritBuff(a){const d=desc(a),m=d.match(/\+\s*(\d+(?:\.\d+)?)%\s*chí mạng/i)||d.match(/tăng\s+(\d+(?:\.\d+)?)%\s*chí mạng/i);return m?Number(m[1]):0}
function enemyEffectTargets(core,att,a,mode='general'){const foes=enemiesOf(core,att),d=desc(a);if(!foes.length)return[];
  if(mode==='damage'&&/tuyến trước/i.test(d))return foes.filter(x=>Number(x.teamIndex)<2).slice(0,2).length?foes.filter(x=>Number(x.teamIndex)<2).slice(0,2):foes.slice(0,2);
  if(/hai kẻ địch[^.;]{0,45}SPEED cao nhất/i.test(d))return[...foes].sort((x,y)=>core.effective(y).speed-core.effective(x).speed).slice(0,2);
  if(/kẻ địch (?:nhanh nhất|có SPEED cao nhất)/i.test(d))return[[...foes].sort((x,y)=>core.effective(y).speed-core.effective(x).speed)[0]].filter(Boolean);
  if(/kẻ địch (?:cao thanh lượt nhất|có thanh lượt cao nhất)/i.test(d))return[[...foes].sort((x,y)=>y.meter-x.meter)[0]].filter(Boolean);
  if(/(?:toàn bộ kẻ địch|toàn địch|toàn đội địch|đội địch|\bđịch\s*-\s*\d+%)/i.test(d))return foes;
  if(mode==='damage'&&/toàn tuyến trước/i.test(d))return foes.filter(x=>Number(x.teamIndex)<2).slice(0,2);
  return[]}
function applyEnemySupportEffects(core,att,a,key,effectMultiplier=1,damageMultiplier=1){const e=a.masterEffects||{},d=desc(a),fx=clamp(Number(effectMultiplier)||1,.20,1.36),dmg=clamp(Number(damageMultiplier)||1,.20,1.60);if(/(?:khi|nếu)[^.;]{0,80}?(?:Khiên|khiên)[^.;]{0,50}?(?:vỡ|bị phá)|nếu đến lượt|lượt chính kế tiếp/i.test(d))return{damage:0,impacts:[]};
  const effectTargets=enemyEffectTargets(core,att,a,'general');
  for(const t of effectTargets){if(e.meterLoss){let cut=Math.min(t.boss?5:15,(Number(e.meterLoss)||0)*fx);t.meter=Math.max(0,t.meter-cut)}for(const x of e.debuffs||[]){let pct=(Number(t.boss&&x.bossPct!=null?x.bossPct:x.pct)||0)*fx;if(pct)applyStat(t,x.stat,-Math.min(x.stat==='def'?.30:pct,pct),att,2,'debuff')}applyAntiHeal(t,att,a,fx)}
  let damage=0,impacts=[];const dm=d.match(/(?:^|[.;])[^.;]{0,100}?\bgây\s+(\d+(?:\.\d+)?)%\s*(ATK|AP|DEF)\s*(?:toàn(?:\s+bộ)?\s*(?:đội\s*)?địch|toàn\s+tuyến\s+trước|lên\s+tuyến\s+trước)/i);
  if(dm&&!/nếu đến lượt|lượt chính kế tiếp|phản chấn[^.;]{0,35}?gây/i.test(d)){const pct=Number(dm[1])*directScale(att),stat=dm[2].toLowerCase(),targets=enemyEffectTargets(core,att,a,'damage');for(const t of targets){const as=core.effective(att),ds=core.effective(t),raw=(stat==='atk'?as.atk:stat==='ap'?as.ap:as.def)*pct/100,factor=E.scaledDefenseFactor(stat==='ap'?as.ap:(stat==='atk'?as.atk:as.def),ds.def,stat==='ap'?.70:.85),crit=core.rng()*100<as.critRate,amount=incoming(core,t,Math.max(1,Math.round(raw*factor*elemMult(att,t)*(crit?as.critDamage/100:1)*C.DAMAGE_PACING*dmg))),hpBefore=t.hp,shieldBefore=Number(t.shield)||0,dealt=E.damageTarget(t,amount);damage+=dealt.damage;impacts.push({targetId:t.id,damage:dealt.damage,absorbed:dealt.absorbed,crit,element:elemMult(att,t),hpBefore,hpAfter:t.hp,shieldBefore,shieldAfter:Number(t.shield)||0,killed:t.defeated||t.hp<=0,secondary:true})}if(impacts.length)core.pushEvent('damage',{sourceId:att.id,impacts,total:damage,secondary:true})}
  if(a.id==='frostmaw.ultimate')for(const t of enemiesOf(core,att)){const k=`V9 Hàn Khí ${att.id}`,old=t.customStatuses[k];t.customStatuses[k]={turns:3,stacks:Math.min(5,Number(old?.stacks||0)+1),sourceId:att.id,kind:'debuff'};if(Number(t.customStatuses[k].stacks)>=4)t.meter=Math.max(0,t.meter-Math.min(t.boss?3:8,t.boss?3:8))}
  return{damage,impacts}}
function installEmergencyHeal(core,att,a,targets,effectMultiplier=1){if(a.id!=='brookfin.skill2')return;const fx=clamp(Number(effectMultiplier)||1,.20,1.36);for(const t of targets)t.customStatuses[`V9 Emergency Heal ${att.id}`]={turns:2,sourceId:att.id,kind:'trigger-heal',threshold:.45,hpPct:.08,apPct:.30,effectMultiplier:fx}}
function triggerEmergencyHeals(core,target){for(const [k,v] of Object.entries(target?.customStatuses||{})){if(v?.kind!=='trigger-heal'||target.hp/Math.max(1,target.maxHp)>=Number(v.threshold||0))continue;const src=core.allUnits.find(x=>x.id===v.sourceId&&!x.defeated);if(!src){delete target.customStatuses[k];continue}const amount=Math.round((src.maxHp*Number(v.hpPct||0)+core.effective(src).ap*Number(v.apPct||0))*effectScale(src)*clamp(Number(v.effectMultiplier)||1,.20,1.36)),healed=E.healTarget(target,amount);delete target.customStatuses[k];if(healed)core.pushEvent('heal',{sourceId:src.id,targetIds:[target.id],amount:healed,trigger:'emergency'})}}
function transferProtection(core,att,target,a,amount){if(['all','all-enemies','two-enemies','front-row','back-row'].includes(a?.target)||/không thể (?:bảo hộ|chuyển sát thương)|bỏ qua Bảo hộ/i.test(desc(a)))return{remaining:amount,transfer:null};const x=strongestProtection(core,target);if(!x)return{remaining:amount,transfer:null};const pct=Math.min(.70,Math.max(0,Number(x.v.pct||0))),base=Math.round(amount*pct),remain=Math.max(1,amount-base),reduced=Math.round(base*(1-Math.min(.50,Math.max(0,Number(x.v.reduction||0))))),prot=x.protector,protAmount=incoming(core,prot,Math.max(1,reduced)),hpBefore=prot.hp,shieldBefore=Number(prot.shield)||0,dealt=E.damageTarget(prot,protAmount);x.v.hits=Math.max(0,Number(x.v.hits||1)-1);if(x.v.hits<=0)delete target.customStatuses[x.k];core.pushEvent('guard',{guardId:prot.id,protectedId:target.id,chance:100,transferPct:pct,utility1850:true});if(x.v.gainOnSplit)gainCore1850(core,prot,1,`${x.v.abilityName||'Bảo hộ'} · chia sát thương`);else core.processMechanicEvent?.('GUARD',{actorId:prot.id,sourceId:prot.id,targetId:target.id,protectedId:target.id,round:core.state.round},[prot]);core.processMechanicEvent?.('DAMAGE_TAKEN',{actorId:att.id,sourceId:att.id,targetId:prot.id,damage:dealt.damage,absorbed:dealt.absorbed,hpBefore,hpAfter:prot.hp,area:false,key:'protection-transfer',round:core.state.round},[prot]);return{remaining:remain,transfer:{targetId:prot.id,damage:dealt.damage,absorbed:dealt.absorbed,crit:false,element:1,hpBefore,hpAfter:prot.hp,shieldBefore,shieldAfter:Number(prot.shield)||0,killed:prot.defeated||prot.hp<=0,protection:true}}}
function antiHealPct(a){const d=desc(a);const pats=[/(?:giảm|-)\s*(\d+(?:\.\d+)?)%\s*(?:hồi(?:\s+máu)?(?:\s+nhận)?|hồi nhận|hiệu quả hồi)/i,/giảm\s+(?:hồi(?:\s+máu)?(?:\s+nhận)?|hiệu quả hồi)\s*(\d+(?:\.\d+)?)%/i];for(const r of pats){const m=d.match(r);if(m)return Number(m[1])/100}return 0}
function applyAntiHeal(target,att,a,effectMultiplier=1){let pct=antiHealPct(a)*clamp(Number(effectMultiplier)||1,.20,1.36);if(!pct)return;const A=art(att);if(A?.effect?.code==='poison_damage_antiheal'&&(a.status==='Poison'||/độc/i.test(desc(a))))pct*=1+Number(A.effect.value||0);pct=Math.min(target?.boss?.15:.40,pct);if(pct>0)target.customStatuses[`V9 AntiHeal ${att.id}`]={turns:2,healingReceived:-pct,sourceId:att.id,kind:'debuff'}}
function applyMaster(core,att,a,targets,key,offensive=false,knowledgeMultiplier=1){const e=a.masterEffects||{},fx=clamp(Number(knowledgeMultiplier)||1,.20,1.36),sup=effectScale(att)*(1+supportBonus(core,att,a,key))*fx;let heal=0,shieldTotal=0;
  const healTargets=effectTargets(core,att,a,targets,offensive,'heal'),shieldTargets=effectTargets(core,att,a,targets,offensive,'shield'),buffTargets=effectTargets(core,att,a,targets,offensive,'buff'),manaTargets=effectTargets(core,att,a,targets,offensive,'mana'),debuffTargets=offensive?targets:[];
  // Direct recovery. Delayed emergency-heal skills install their trigger in supportEffect instead of healing immediately.
  if((e.healHpPct||e.healApPct)&&a.id!=='brookfin.skill2')for(const t of healTargets){let v=t.maxHp*(e.healHpPct||0)+core.effective(att).ap*(e.healApPct||0);const before=t.hp;heal+=E.healTarget(t,Math.round(v*sup),{source:att,capRatio:(key==='ultimate'||key==='exclusive')?.50:.35});if(att.combatRole==='healer'&&t.hp===t.maxHp&&before===t.maxHp){const ov=Math.round(v*.25);t.shield=Math.max(t.shield||0,ov)}}
  for(const t of shieldTargets)shieldTotal+=shield(core,att,t,a,sup);
  if(e.restoreMana)for(const t of manaTargets)t.mana=clamp(t.mana+Math.round(e.restoreMana*fx),0,t.maxMana);
  const directMeter=Number(e.meterGain||explicitMeterGain(a)||0)*fx,critPoints=explicitCritBuff(a)*fx;for(const t of buffTargets){if(directMeter){let add=Math.min(35,directMeter);const A=art(att);if(A?.effect?.code==='turn_gauge_effect')add=Math.min(35,add+Math.min(8,add*Number(A.effect.value||0)));t.meter=Math.min(90,t.meter+add)}if(critPoints)applyCrit(t,critPoints,att,1);for(const b of e.buffs||[])applyBuffOp(core,t,b,att,a,fx)}
  for(const t of debuffTargets){if(e.drainMana)t.mana=Math.max(0,t.mana-Math.round(e.drainMana*fx));if(e.meterLoss){let cut=Math.min(t.boss?5:15,e.meterLoss*fx);const A=art(att);if(A?.effect?.code==='turn_gauge_effect')cut=Math.min(t.boss?5:15,cut+Math.min(8,cut*Number(A.effect.value||0)));t.meter=Math.max(0,t.meter-cut)}for(const d of e.debuffs||[]){let pct=(Number(t.boss&&d.bossPct!=null?d.bossPct:d.pct)||0)*fx;const A=art(att);if(d.stat==='speed'&&A?.effect?.code==='slow_effect')pct=Math.min(.50,pct*(1+Number(A.effect.value||0)));applyStat(t,d.stat,-Math.min(d.stat==='def'?.30:pct,pct),att,2,'debuff')}applyAntiHeal(t,att,a,fx);}
  if(e.guard)att.guardBonus=clamp(att.guardBonus+15*fx,0,30);return{heal,shield:shieldTotal}}
function statusChance(att,a,key,target){let c=key==='exclusive'?.90:key==='ultimate'?.75:.50;if(a.status==='Burn'||a.status==='Poison')c=Math.max(c,.75);if(HARD.has(a.status)){const tenacity=Math.max(0,Math.min(60,Number(E.effectiveStats?.(target)?.tenacity||target?.stats?.tenacity||0)))/100;c*=1-tenacity;}const A=art(att);if(A?.effect?.code==='element_advantage_effect'&&strong(att,target))c=Math.min(1,c*(1+Number(A.effect.value||0)));if(att.v9.battleFlags.elementEffectBoost){c=Math.min(1,c*1.6);att.v9.battleFlags.elementEffectBoost=false}return Math.max(0,Math.min(1,c))}
function attemptStatus(core,att,target,a,key,impactDamage=0){if(!a?.status||!target||target.defeated)return false;const chance=statusChance(att,a,key,target),roll=core.rng();if(roll<chance){const ok=core.applyStatus(att,target,a.status,a,{impactDamage:Math.max(0,Number(impactDamage)||0),key});if(ok!==false)return true;core.pushEvent('status-resist',{sourceId:att.id,targetId:target.id,status:a.status,key,ability:a.name,chance,reason:'IMMUNE_OR_RULE'});core.processMechanicEvent?.('RESIST',{actorId:att.id,sourceId:att.id,targetId:target.id,status:a.status,key,round:core.state?.round||1},[target]);return false;}core.pushEvent('status-resist',{sourceId:att.id,targetId:target.id,status:a.status,key,ability:a.name,chance,roll,reason:HARD.has(a.status)?'TENACITY_OR_ROLL':'ROLL'});core.processMechanicEvent?.('RESIST',{actorId:att.id,sourceId:att.id,targetId:target.id,status:a.status,key,round:core.state?.round||1},[target]);return false}

function statusTurns1850(att,target,d,base=2){const m=String(d||'').match(/(?:trong\s+)?(\d+)\s*lượt/i),n=Math.max(1,Number(m?.[1]||base));return target?.id===att?.id?n+1:n}
function addUtilityStatus1850(core,att,target,a,props={}){if(!target)return null;const key=`V1850 ${a.id} ${att.id}`;target.customStatuses=target.customStatuses||{};target.customStatuses[key]={turns:statusTurns1850(att,target,desc(a),props.turns||2),sourceId:att.id,kind:'utility1850',abilityId:a.id,abilityName:a.name,...props};core.pushEvent('utility-status',{sourceId:att.id,targetId:target.id,ability:a.name,statusKey:key,utility1850:true,...props});return target.customStatuses[key]}
function directDRPct1850(d){const m=String(d||'').match(/(\d+(?:\.\d+)?)%\s+giảm\s+sát thương trực tiếp/i)||String(d||'').match(/giảm\s+(\d+(?:\.\d+)?)%\s+sát thương trực tiếp/i);return Math.max(0,Number(m?.[1]||0)/100)}
function oneShotDR1850(d){const x=String(d||''),split=x.match(/đòn[^.;]{0,45}?đầu tiên\s+giảm\s+(\d+(?:\.\d+)?)%\s*\/\s*(\d+(?:\.\d+)?)%\s+sát thương/i),one=x.match(/đòn[^.;]{0,45}?đầu tiên\s+giảm\s+(\d+(?:\.\d+)?)%\s+sát thương/i);return split?{self:Number(split[1])/100,ally:Number(split[2])/100}:one?{self:Number(one[1])/100,ally:Number(one[1])/100}:null}
function redistributeShield1850(core,att,target,a){const d=desc(a),m=d.match(/phân\s+lại[^.;]{0,55}?(\d+(?:\.\d+)?)%[^.;]{0,30}?(\d+(?:\.\d+)?)%/i);if(!m||!target||target.id===att.id)return false;const total=Math.max(0,Math.round(Number(att.shield||0)+Number(target.shield||0))),low=(att.hp/Math.max(1,att.maxHp))<=(target.hp/Math.max(1,target.maxHp))?att:target,other=low.id===att.id?target:att,lowPct=clamp(Number(m[1])/100,0,1),lowShare=Math.round(total*lowPct),otherShare=total-lowShare,before={[att.id]:Number(att.shield||0),[target.id]:Number(target.shield||0)};low.shield=lowShare;other.shield=otherShare;core.pushEvent('shield-redistribute',{sourceId:att.id,targetIds:[att.id,target.id],before,after:{[att.id]:att.shield,[target.id]:target.shield},total,ability:a.name,utility1850:true});return true}
function utilitySupport1850(core,att,a,targets,key,effectMultiplier=1){const d=desc(a),fx=clamp(Number(effectMultiplier)||1,.20,1.36);let count=0;count+=installProtection(core,att,a,targets,fx)||0;
  const dr=directDRPct1850(d);if(dr>0&&!/đòn[^.;]{0,45}?đầu tiên\s+giảm/i.test(d)){let recipients=targets;if(/(?:^|[.;])\s*Tuyến sau\s+giảm/i.test(d))recipients=alliesOf(core,att).filter(x=>Number(x.teamIndex)>=2);for(const t of recipients){const st=addUtilityStatus1850(core,att,t,a,{incomingDR:Math.min(.5,dr*fx),directOnly:true,turns:2});if(st){const life=d.match(/nhận\s+(\d+(?:\.\d+)?)%\s+hút máu/i);if(life&&t.id===att.id)st.lifestealPct=Math.min(.5,Number(life[1])/100*fx);const acc=d.match(/kẻ đánh[^.;]{0,45}?giảm\s+(\d+(?:\.\d+)?)%\s+chính xác/i);if(acc){st.retaliateAccuracyPct=Math.min(.3,Number(acc[1])/100*fx);st.retaliatePoison=/nhận\s+1\s+Độc/i.test(d);}if(/(?:^|[.;])\s*Tuyến sau\s+giảm/i.test(d)){st.kind='verdantusk-canopy';st.growthOnHeal=/nhận hồi máu[^.;]{0,60}?Sinh Trưởng/i.test(d);}count++;}}}
  const one=oneShotDR1850(d);if(one){for(const t of targets){const rate=t.id===att.id?one.self:one.ally,st=addUtilityStatus1850(core,att,t,a,{incomingDR:Math.min(.5,rate*fx),oneShotDirect:true,debuffImmune:/miễn debuff|không gắn debuff/i.test(d),afterHitMeter:Number((d.match(/nếu sống sót\s*\+\s*(\d+(?:\.\d+)?)%\s*thanh lượt/i)||[])[1]||0)*fx,turns:2});if(st)count++;}}
  const lead=d.match(/Người Dẫn Đầu nhận\s+(\d+(?:\.\d+)?)%\s+thanh lượt/i);if(lead&&targets[0]){const t=targets[0],before=Number(t.meter)||0,add=Math.min(35,Number(lead[1])*fx);t.meter=Math.min(90,before+add);const nums=[...d.matchAll(/lần lượt nhận\s+(\d+(?:\.\d+)?)%\s+và\s+(\d+(?:\.\d+)?)%\s+thanh lượt/gi)][0];addUtilityStatus1850(core,att,t,a,{kind:'wind-leader',meterLossResist:/kháng giảm thanh lượt/i.test(d),chainMeter:nums?[Number(nums[1])*fx,Number(nums[2])*fx]:[],turns:1});core.pushEvent('meter-change',{sourceId:att.id,targetId:t.id,before,after:t.meter,delta:t.meter-before,ability:a.name,utility1850:true});count++;}
  const assist=d.match(/Đòn cơ bản kế tiếp[^.;]{0,90}?bắn hỗ trợ\s+(\d+(?:\.\d+)?)%\s+ATK/i);if(assist&&targets[0]){addUtilityStatus1850(core,att,targets[0],a,{kind:'torrento-assist',assistAtkPct:Number(assist[1])*fx,hits:1,turns:99});count++;}
  if(/Tính tổng Khiên hiện tại/i.test(d)&&/phân lại/i.test(d)&&targets[0])if(redistributeShield1850(core,att,targets[0],a))count++;
  return count;
}
function directAuxDamage1850(core,source,target,atkPct,label='Hỗ trợ'){if(!source||!target||target.defeated||!(atkPct>0))return 0;const as=core.effective(source),ds=core.effective(target),factor=E.scaledDefenseFactor(as.atk,ds.def,.85),crit=core.rng()*100<Number(as.critRate||0),raw=as.atk*(Number(atkPct)||0)/100,amount0=Math.max(1,Math.round(raw*factor*elemMult(source,target)*(crit?as.critDamage/100:1)*C.DAMAGE_PACING)),amount=incoming(core,target,amount0),hpBefore=target.hp,shieldBefore=Number(target.shield)||0,dealt=E.damageTarget(target,amount),impact={targetId:target.id,damage:dealt.damage,absorbed:dealt.absorbed,crit,element:elemMult(source,target),hpBefore,hpAfter:target.hp,shieldBefore,shieldAfter:Number(target.shield)||0,killed:target.defeated||target.hp<=0,secondary:true,utility1850:true};target.lastDamage=dealt.damage;core.pushEvent('damage',{sourceId:source.id,secondary:true,assist:true,label,impacts:[impact],total:dealt.damage,utility1850:true});core.processMechanicEvent?.('DAMAGE_TAKEN',{actorId:source.id,sourceId:source.id,targetId:target.id,damage:dealt.damage,absorbed:dealt.absorbed,hpBefore,hpAfter:target.hp,area:false,key:'assist',round:core.state.round},[target]);if(impact.killed){core.pushEvent('kill',{sourceId:source.id,targetId:target.id,ability:label,crit,utility1850:true});oldMechanicEvent?.call(core,'KILL',{actorId:source.id,sourceId:source.id,targetId:target.id,bonus:1,round:core.state.round},core.allUnits);}return dealt.damage}
function utilityMechanic1850(core,event,payload={}){if(event==='HEAL'){for(const id of payload.targetIds||[payload.targetId].filter(Boolean)){const t=core.allRosterUnits.find(x=>x.id===id);if(!t)continue;for(const st of Object.values(t.customStatuses||{})){if(st?.kind!=='verdantusk-canopy'||!st.growthOnHeal)continue;const src=core.allRosterUnits.find(x=>x.id===st.sourceId&&!x.defeated);if(!src)continue;init(src);if(src.v9.roundFlags.v1850CanopyGrowth===core.state.round)continue;src.v9.roundFlags.v1850CanopyGrowth=core.state.round;const ch=gainCore1850(core,src,1,'Tán Lá · đồng minh được hồi');if(ch?.delta>0)core.pushEvent('utility-trigger',{sourceId:src.id,targetId:t.id,kind:'growth-on-heal',utility1850:true});}}}
  if(event==='ALLY_ACTION'){const actor=core.allRosterUnits.find(x=>x.id===payload.actorId);if(actor){for(const [k,st] of Object.entries(actor.customStatuses||{})){if(st?.kind!=='wind-leader')continue;const src=core.allRosterUnits.find(x=>x.id===st.sourceId&&!x.defeated),gains=Array.isArray(st.chainMeter)?st.chainMeter:[];if(!src||!gains.length){delete actor.customStatuses[k];continue}const allies=alliesOf(core,actor).filter(x=>x.id!==actor.id&&!x.defeated).sort((a,b)=>Number(a.meter||0)-Number(b.meter||0));gains.slice(0,2).forEach((g,i)=>{const t=allies[i];if(!t)return;const before=Number(t.meter)||0;t.meter=Math.min(90,before+Number(g||0));if(t.meter!==before){core.pushEvent('meter-change',{sourceId:src.id,targetId:t.id,before,after:t.meter,delta:t.meter-before,ability:st.abilityName,chain:true,utility1850:true});oldMechanicEvent?.call(core,'METER_CHANGE',{actorId:src.id,sourceId:src.id,targetId:t.id,side:src.side,before,after:t.meter,delta:t.meter-before,round:core.state.round},core.allUnits);}});delete actor.customStatuses[k];core.pushEvent('utility-trigger',{sourceId:src.id,targetId:actor.id,kind:'wind-leader-chain',utility1850:true});}}}
  if(event==='ALLY_DAMAGE'&&payload.key==='basic'){const actor=core.allRosterUnits.find(x=>x.id===payload.actorId),target=core.allRosterUnits.find(x=>x.id===payload.targetId);if(actor&&target&&!target.defeated){for(const [k,st] of Object.entries(actor.customStatuses||{})){if(st?.kind!=='torrento-assist'||Number(st.hits||0)<=0)continue;const src=core.allRosterUnits.find(x=>x.id===st.sourceId&&!x.defeated);delete actor.customStatuses[k];if(!src)continue;const dealt=directAuxDamage1850(core,src,target,Number(st.assistAtkPct)||0,st.abilityName||'Bắn hỗ trợ');gainCore1850(core,src,1,`${st.abilityName||'Bắn hỗ trợ'} · Phối Hợp`);core.pushEvent('assist-shot',{sourceId:src.id,triggerId:actor.id,targetId:target.id,damage:dealt,utility1850:true});break;}}}
  if(event==='DAMAGE_TAKEN'){const target=core.allRosterUnits.find(x=>x.id===payload.targetId),attacker=core.allRosterUnits.find(x=>x.id===payload.actorId);if(target){init(target);const meter=Number(target.v9.battleFlags.v1850AfterHitMeter||0);if(meter){delete target.v9.battleFlags.v1850AfterHitMeter;if(!target.defeated){const before=Number(target.meter)||0;target.meter=Math.min(90,before+meter);if(target.meter!==before){core.pushEvent('meter-change',{sourceId:target.id,targetId:target.id,before,after:target.meter,delta:target.meter-before,ability:'Vũ Mạc',afterHit:true,utility1850:true});oldMechanicEvent?.call(core,'METER_CHANGE',{actorId:target.id,sourceId:target.id,targetId:target.id,side:target.side,before,after:target.meter,delta:target.meter-before,round:core.state.round},core.allUnits);}}}if(attacker&&attacker.side!==target.side&&!attacker.defeated){for(const st of Object.values(target.customStatuses||{})){if(!Number(st?.retaliateAccuracyPct)&&!st?.retaliatePoison)continue;const src=core.allRosterUnits.find(x=>x.id===st.sourceId&&!x.defeated);if(!src)continue;if(Number(st.retaliateAccuracyPct)>0)applyStat(attacker,'accuracy',-Number(st.retaliateAccuracyPct),src,1,'debuff');if(st.retaliatePoison)core.applyStatus?.(src,attacker,'Poison',{id:`${st.abilityId||'v1850'}.retaliate`,name:st.abilityName||'Độc Vụ',status:'Poison',type:'magic',target:'enemy'},{});core.pushEvent('utility-retaliate',{sourceId:src.id,targetId:attacker.id,protectedId:target.id,accuracyDown:Number(st.retaliateAccuracyPct)||0,poison:Boolean(st.retaliatePoison),utility1850:true});}}}}
}
P.processMechanicEvent=function(event,payload={},units=null){const out=oldMechanicEvent.call(this,event,payload,units);utilityMechanic1850(this,event,payload);return out};
P.applyStatus=function(att,target,status,a,context={}){init(att);init(target);newRound(att,this.state.round);newRound(target,this.state.round);if(!status||!target||target.defeated)return false;const block=target.v9?.battleFlags?.v1850DebuffBlockSource;if(block&&att?.side!==target.side&&(!block.sourceId||block.sourceId===att.id)){delete target.v9.battleFlags.v1850DebuffBlockSource;this.pushEvent('status-resist',{sourceId:target.id,targetId:target.id,blockedSourceId:att.id,status,reason:'Ẩn Ảnh · miễn debuff',utility1850:true});return false;}
  if(status==='Cleanse'){if(att.element!=='light')return false;const d=desc(a).toLowerCase(),count=/tối đa\s*2|2 debuff/.test(d)?2:1;if(target.side===att.side){const bads=[...Object.keys(target.statuses||{}).filter(s=>!['Defense Up','Attack Up','AP Up','Shield','Regeneration'].includes(s)),...Object.keys(target.customStatuses||{}).filter(s=>target.customStatuses[s]?.kind==='debuff')];for(const k of bads.slice(0,count)){if(target.statuses[k])delete target.statuses[k];else delete target.customStatuses[k]}return true}else{const buffs=[...Object.keys(target.statuses||{}).filter(s=>['Defense Up','Attack Up','AP Up','Shield','Regeneration'].includes(s)),...Object.keys(target.customStatuses||{}).filter(s=>target.customStatuses[s]?.kind==='buff')];for(const k of buffs.slice(0,count)){if(target.statuses[k])delete target.statuses[k];else delete target.customStatuses[k]}return true}}
  if(status==='Curse'){if(att.element!=='dark')return false;target.customStatuses.Curse={turns:1,sourceId:att.id,kind:'debuff'};return true}
  const hard=HARD.has(status);if(hard){if(target.boss){applyStat(target,'speed',-.10,att,1,'debuff');target.meter=Math.max(0,target.meter-5);return true}const prev=target.v9.battleFlags.hardBySource?.[att.id];if(prev===this.state.round-1)return false;target.v9.battleFlags.hardBySource=target.v9.battleFlags.hardBySource||{};target.v9.battleFlags.hardBySource[att.id]=this.state.round;status=status==='Paralysis'?'Slow':status==='Bind'?'Stun':status;const ok=oldApply.call(this,att,target,status,a,context);if(target.statuses?.[status])target.statuses[status].turns=1;return ok}
  const ok=oldApply.call(this,att,target,status,a,context);return ok};
P.targetsFor=function(att,a,key,req){const legal=this.legalTargets(att,a,key);if(!legal.length)return[];
  if(a?.target==='two-allies'){const others=legal.filter(x=>x.id!==att.id),first=others.find(x=>x.id===req)||[...others].sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp)[0],second=[...others].filter(x=>x.id!==first?.id).sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp)[0];return[first,second].filter(Boolean)}
  if(a?.target==='self-and-ally'){const pool=[...legal].filter(x=>x.id!==att.id),chosen=a?.canonicalAllySelector==='back-lowest'?[...pool].filter(x=>Number(x.teamIndex)>=2).sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp)[0]:pool.find(x=>x.id===req)||[...pool].sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp)[0];return[att,chosen].filter(Boolean)}
  if(a?.target==='self-and-lowest-ally'){let pool=[...legal].filter(x=>x.id!==att.id);if(a?.canonicalAllySelector==='back-lowest'){const back=pool.filter(x=>Number(x.teamIndex)>=2);if(back.length)pool=back;}const low=pool.sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp)[0];return[att,low].filter(Boolean)}
  if(a?.target==='three-allies'){const others=[...legal].filter(x=>x.id!==att.id).sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp);return others.length>=3?others.slice(0,3):[...others,att].slice(0,3);}
  if(a?.target==='front-row')return legal.filter(x=>Number(x.teamIndex)<2).slice(0,2).length?legal.filter(x=>Number(x.teamIndex)<2).slice(0,2):legal.slice(0,2);
  if(a?.target==='back-row')return legal.filter(x=>Number(x.teamIndex)>=2).slice(0,3);
  return oldTargets.call(this,att,a,key,req)};
P.start=function(){const ok=oldStart.call(this);if(!ok)return ok;for(const u of this.allUnits)init(u);for(const side of ['player','enemy']){const mus=this.living(side).filter(u=>u.combatRole==='musician').sort((a,b)=>(RARITY[b.rarity]||0)-(RARITY[a.rarity]||0));mus.forEach((u,i)=>{const power=i?0.4:1;const allies=side==='player'?this.living('player'):this.living('enemy');for(const a of allies){applyStat(a,'speed',.08*power,u,2,'buff');a.meter=Math.min(90,a.meter+5*power)}u.v9.role.opener=true;u.v9.role.beats=0;u.v9.role.allyActions=0;});}return ok};
function flarionState(u){init(u);u.v9.flarion=u.v9.flarion||{embers:0,burnTicks:0,awakened:false};return u.v9.flarion}
function flarionCap(u){const p=D.pows.find(x=>x.id===u?.powId),map=p?.coreMechanic?.ember?.maxByStar||{};return Number(map[String(Math.max(1,Number(u?.owned?.stars||1)))])||12}
function flarionMarkCap(u){const p=D.pows.find(x=>x.id===u?.powId),map=p?.coreMechanic?.fireMark?.maxByStar||{};return Number(map[String(Math.max(1,Number(u?.owned?.stars||1)))])||6}
function fireMark(target,u){return Number(target?.customStatuses?.[`Flarion Hỏa Ấn ${u.id}`]?.stacks||0)}
function addFireMark(target,u,n=1){if(!target||!u)return 0;const key=`Flarion Hỏa Ấn ${u.id}`,cur=fireMark(target,u),next=Math.min(flarionMarkCap(u),cur+Math.max(0,Number(n)||0));target.customStatuses=target.customStatuses||{};target.customStatuses[key]={turns:99,sourceId:u.id,kind:'mark',stacks:next,label:`Hỏa Ấn ×${next}`};return next-cur}
function addEmbers(u,n=1){const s=flarionState(u),before=s.embers;s.embers=Math.min(flarionCap(u),s.embers+Math.max(0,Number(n)||0));if(Number(u.owned?.stars||0)>=6&&!s.awakened&&s.embers>=12){s.awakened=true;u.customStatuses=u.customStatuses||{};u.customStatuses['Hỏa Thần Xạ']={turns:3,stat:'atk',pct:.70,sourceId:u.id,kind:'buff'};}return s.embers-before}
function flarionDirect(core,u,target,atkPct,label){if(!u||!target||target.defeated||atkPct<=0)return 0;const as=core.effective(u),ds=core.effective(target),factor=E.scaledDefenseFactor(as.atk,ds.def,.85),crit=core.rng()*100<as.critRate,raw=as.atk*(atkPct/100),amount=Math.max(1,Math.round(raw*factor*elemMult(u,target)*(crit?as.critDamage/100:1)*C.DAMAGE_PACING)),before=target.hp,dealt=E.damageTarget(target,incoming(core,target,amount));core.pushEvent('damage',{sourceId:u.id,secondary:true,label,impacts:[{targetId:target.id,damage:dealt.damage,absorbed:dealt.absorbed,crit,element:elemMult(u,target),hpBefore:before,hpAfter:target.hp,secondary:true}],total:dealt.damage});return dealt.damage}
function flarionAfterAction(core,att,key,a,res){const allies=att.side==='player'?core.living('player'):core.living('enemy'),foes=att.side==='player'?core.living('enemy'):core.living('player');if(a?.status==='Burn'||/Thiêu Đốt|Burn/i.test(desc(a))){for(const f of allies.filter(x=>x.powId==='starter_fire_flarion'))addEmbers(f,Math.min(2,(res?.targets||[]).length||1))}if(att.powId!=='starter_fire_flarion')return;const s=flarionState(att),star=Math.max(1,Number(att.owned?.stars||1)),targets=res?.targets||[],primary=targets[0];if(!primary)return;if(key==='basic'&&primary.statuses?.Burn)flarionDirect(core,att,primary,Number(a.burnBonusAtk||0)*(star>=6&&s.awakened?1.5:1),'Hỏa Tiễn · Burn Bonus');if(key==='skill1'){const marks=fireMark(primary,att);if(marks>0)flarionDirect(core,att,primary,Number(a.bonusPerFireMarkAtk||0)*marks,'Liên Tiễn · Hỏa Ấn');if(star>=3){att.v9.battleFlags.flarionSkill1Bonus=Math.min(.32,Math.floor(s.embers/3)*.08)}}if(key==='skill2'){const burned=foes.filter(x=>x.statuses?.Burn).length,need=star>=6&&s.awakened?2:3;if(burned>=need){let coeff=Number(a.followAtk||0);if(star>=6&&s.awakened)coeff*=1.7;flarionDirect(core,att,primary,coeff,'Hỏa Vũ Truy Kích');if(star>=2)addEmbers(att,2)}}if(key==='ultimate'){const live=foes.filter(x=>!x.defeated);const fin=live.sort((x,y)=>fireMark(y,att)-fireMark(x,att)||x.hp/x.maxHp-y.hp/y.maxHp)[0];if(fin){let coeff=Number(a.finisherAtk||0)+fireMark(fin,att)*Number(a.perFireMarkAtk||0)+s.embers*Number(a.perEmberAtk||0);if(star>=5&&s.embers>=flarionCap(att))coeff*=1.5;if(star>=6&&s.awakened)coeff*=1.8;flarionDirect(core,att,fin,coeff,star>=6&&s.awakened?'Diệt Thế Thiên Hỏa':'Thiên Hỏa Finisher');if(star>=6&&s.awakened){}else if(star>=5)s.embers=Math.ceil(s.embers*.5);else s.embers=0}}}
P.beginTurn=function(){const r=oldBegin.call(this);if(r?.unit){const u=r.unit;init(u);newRound(u,this.state.round);if(u.statuses?.Burn){const opposing=u.side==='player'?this.living('enemy'):this.living('player');for(const f of opposing.filter(x=>x.powId==='starter_fire_flarion')){addFireMark(u,f,1);const fs=flarionState(f);fs.burnTicks++;if(Number(f.owned?.stars||0)>=5&&fs.burnTicks>=4){fs.burnTicks=0;const candidates=u.side==='player'?this.living('player'):this.living('enemy'),target=[...candidates].sort((a,b)=>fireMark(b,f)-fireMark(a,f)||a.hp/a.maxHp-b.hp/b.maxHp)[0];if(target)flarionDirect(this,f,target,150,'Bất Tận Hỏa Vũ')}}}for(const k of Object.keys(u.v9.cooldowns))u.v9.cooldowns[k]=Math.max(0,u.v9.cooldowns[k]-1);if(hasSet(u,'Thiên Hộ',4)&&u.hp/u.maxHp<.35&&!u.v9.battleFlags.heavenShield){u.v9.battleFlags.heavenShield=true;u.shield=Math.max(u.shield||0,Math.round(u.maxHp*.30));}}
  return r};
P.finishCurrent=function(skipped=false){const u=this.state.current;if(u){for(const [k,v] of Object.entries(u.customStatuses||{})){if(v&&Number.isFinite(Number(v.turns))){v.turns--;if(v.turns<=0)delete u.customStatuses[k]}}}return oldFinish.call(this,skipped)};
P.canUse=function(u,key){if(!oldCan.call(this,u,key))return false;init(u);return !(u.v9.cooldowns[key]>0)};
P.actionInfo=function(u,key){const x=oldActionInfo.call(this,u,key);init(u);x.cooldownRemaining=Number(u.v9.cooldowns[key]||0);x.cooldown=Number(x.ability?.cooldown||0);return x};
function incoming(core,target,amount){init(target);newRound(target,core.state.round);let m=1-incomingReduction(target);const one=[];for(const [k,v] of Object.entries(target.customStatuses||{}))if(v?.oneShotDirect&&Number(v.incomingDR)>0)one.push([k,v]);if(one.length){for(const [k,v] of one){if(Number(v.afterHitMeter)>0)target.v9.battleFlags.v1850AfterHitMeter=Math.max(Number(target.v9.battleFlags.v1850AfterHitMeter||0),Number(v.afterHitMeter));if(v.debuffImmune)target.v9.battleFlags.v1850DebuffBlockSource={sourceId:core.state.current?.id||null};delete target.customStatuses[k];core.pushEvent('utility-consume',{sourceId:v.sourceId||target.id,targetId:target.id,kind:'one-shot-dr',reduction:Number(v.incomingDR)||0,utility1850:true});}}if(hasSet(target,'Bền Bỉ',4)&&target.v9.battleFlags.nextDR){m*=.75;target.v9.battleFlags.nextDR=false}return Math.max(1,Math.round(amount*Math.max(.5,m)))}
P.supportEffect=function(att,a,targets,key='skill1',effectKnow=1,damageKnow=1){init(att);key=key||a.v81Slot||(a.slotType==='ultimate'?'ultimate':a.slotType==='exclusive'?'exclusive':a.id?.endsWith('.skill1')||a.id?.endsWith('_skill1')?'skill1':a.id?.endsWith('.skill2')||a.id?.endsWith('_skill2')?'skill2':'basic');let base={damage:0,heal:0,targets};installEmergencyHeal(this,att,a,targets,effectKnow);const extra=applyMaster(this,att,a,targets,key,false,effectKnow);utilitySupport1850(this,att,a,targets,key,effectKnow);base.heal=extra.heal;base.shield=extra.shield;const allySafeStatus=new Set(['Shield','Regeneration','Defense Up','Attack Up','AP Up','Speed Up','Cleanse']);if(a.status&&allySafeStatus.has(a.status)&&!a.suppressLegacyStatus)for(const t of targets)this.applyStatus(att,t,a.status,a);const enemy=applyEnemySupportEffects(this,att,a,key,effectKnow,damageKnow);base.damage=enemy.damage||0;base.secondaryImpacts=enemy.impacts||[];
  // Correct legacy tags whose status describes a self-cost, cleanse or retaliation rather than an ally debuff.
  if(a.id==='zephyroo.skill1')applyStat(att,'speed',-.10,att,1,'debuff');
  if(a.id==='tidecrest.ultimate'){for(const t of alliesOf(this,att)){if(t.statuses?.Burn){E.removeStatus(t,'Burn');this.pushEvent('cleanse',{sourceId:att.id,targetId:t.id,status:'Burn',limited:true,label:'Dập Thiêu Đốt'});}}}

  if(a.id==='brookfin.basic'&&alliesOf(this,att).every(x=>x.hp/x.maxHp>.85)){const foes=enemiesOf(this,att),t=att.side==='enemy'?this.weightedPresenceTarget(foes):foes[0];if(t){const as=this.effective(att),ds=this.effective(t),raw=as.ap*.48*directScale(att),factor=E.scaledDefenseFactor(as.ap,ds.def,.70),amount=incoming(this,t,Math.max(1,Math.round(raw*factor*elemMult(att,t)*C.DAMAGE_PACING*damageKnow))),before=t.hp,dealt=E.damageTarget(t,amount);base.damage+=dealt.damage;this.pushEvent('damage',{sourceId:att.id,impacts:[{targetId:t.id,damage:dealt.damage,absorbed:dealt.absorbed,crit:false,element:elemMult(att,t),hpBefore:before,hpAfter:t.hp,secondary:true}],total:dealt.damage,secondary:true})}}
  return base};
P.offensiveEffect=function(att,a,key,targets,know,targetCorePayoff=null,effectKnow=1){
 init(att);newRound(att,this.state.round);let total=0,impacts=[],breakEvents=[],killEvents=[],masterTargets=[];
 for(let i=0;i<targets.length;i++){
  const target=targets[i];if(!target||target.defeated)continue;init(target);newRound(target,this.state.round);
  const noDirect=Number(a?.power||0)<=0&&!Object.keys(totalCoeff(a,i)).length;if(noDirect){if(a.status)attemptStatus(this,att,target,a,key);masterTargets.push(target);continue;}
  const hit=masterHitRoll(this,att,target,a,key);
  if(hit.evaded){impacts.push({targetId:target.id,damage:0,absorbed:0,crit:false,element:elemMult(att,target),hpBefore:target.hp,hpAfter:target.hp,shieldBefore:Math.max(0,Number(target.shield)||0),shieldAfter:Math.max(0,Number(target.shield)||0),shieldBreak:false,killed:false,evaded:true,hitChance:hit.hitChance});this.pushEvent('evade',{sourceId:att.id,targetId:target.id,key,ability:a.name,hitChance:hit.hitChance});continue;}
  const calc=coeffDamage(this,att,target,a,i)||E.calculateDamage(att,target,a,{key,area:Boolean(C.isArea?.(a)),unavoidable:true,combo:att.combo,sameElementAllies:(att.side==='player'?this.living('player'):this.living('enemy')).filter(u=>u.element===att.element).length-1,rng:this.rng});calc.hitChance=hit.hitChance;
  let bonus=gearArtifactDamage(this,att,target,a,key),amount=Math.max(1,Math.round(calc.amount*know*C.DAMAGE_PACING*(1+bonus)*(C.rankDamagePacing?.(att)||1)*(C.combatPressure?.(this.state.round)?.damage||1)));amount=incoming(this,target,amount);amount=C.roundOneSafety?.(this,att,target,key,a,amount,know,this.activeDomain?.(att.side)||null,this.activeSimple?.(att.side)||null)??amount;amount=Math.min(amount,C.pvpDirectCap?.(this,target,key,a)??Infinity);
  const split=transferProtection(this,att,target,a,amount);amount=split.remaining;if(split.transfer){impacts.push(split.transfer);total+=split.transfer.damage;if(split.transfer.killed)killEvents.push({sourceId:att.id,targetId:split.transfer.targetId,ability:a.name,crit:false,protection:true});}
  const hpBefore=target.hp,shieldBefore=Math.max(0,Number(target.shield)||0),dealt=E.damageTarget(target,amount),shieldBreak=shieldBefore>0&&Math.max(0,Number(target.shield)||0)<=0&&dealt.absorbed>0,killed=target.defeated||target.hp<=0;triggerEmergencyHeals(this,target);total+=dealt.damage;target.lastDamage=dealt.damage;target.v9.roundFlags.receivedHit=true;target.v9.receivedDirect=true;
  const area=Boolean(C.isArea?.(a));this.processMechanicEvent('DAMAGE_TAKEN',{actorId:att.id,sourceId:att.id,targetId:target.id,damage:dealt.damage,absorbed:dealt.absorbed,hpBefore,hpAfter:target.hp,area,key,round:this.state.round},[target]);if(!area&&dealt.damage>0&&target.coreRuntime)target.coreRuntime.lastSingleHitTurn=this.state.turnCount;if(calc.crit)this.processMechanicEvent('CRIT_TAKEN',{actorId:att.id,sourceId:att.id,targetId:target.id,key,ability:a.name},[target]);
  if(target.combatRole==='fighter')target.v9.role.fury=Math.min(5,Number(target.v9.role.fury||0)+1);if(hasSet(target,'Bền Bỉ',4)&&(dealt.damage+dealt.absorbed)>=target.maxHp*.15&&target.v9.roundFlags.bulwarkProc!==this.state.round){target.v9.roundFlags.bulwarkProc=this.state.round;target.v9.battleFlags.nextDR=true;}if(hasSet(target,'Thiên Hộ',4)&&target.hp/target.maxHp<.35&&!target.v9.battleFlags.heavenShield){target.v9.battleFlags.heavenShield=true;target.shield=Math.max(target.shield||0,Math.round(target.maxHp*.30));}
  impacts.push({targetId:target.id,damage:dealt.damage,absorbed:dealt.absorbed,crit:calc.crit,element:calc.element,hpBefore,hpAfter:target.hp,shieldBefore,shieldAfter:Math.max(0,Number(target.shield)||0),shieldBreak,killed,evaded:false,hitChance:hit.hitChance});
  if(shieldBreak){breakEvents.push({sourceId:att.id,targetId:target.id,kind:'shield',ability:a.name});if(hasSet(att,'Phá Giáp',4))target.customStatuses[`V9 ArmorBreak ${att.id}`]={turns:1,sourceId:att.id,kind:'debuff'};}if(killed){killEvents.push({sourceId:att.id,targetId:target.id,ability:a.name,crit:!!calc.crit});if(hasSet(att,'Truy Kích',4))att.v9.battleFlags.afterKill=true;}
  target.rage=clamp(target.rage+15,0,100);if(a.status&&!target.defeated)attemptStatus(this,att,target,a,key,Number(dealt.damage||0)+Number(dealt.absorbed||0));masterTargets.push(target);if(strong(att,target)&&hasSet(att,'Nguyên Tố Cộng Hưởng',4)){att.v9.battleFlags.advantageHits=(att.v9.battleFlags.advantageHits||0)+1;if(att.v9.battleFlags.advantageHits>=3){att.v9.battleFlags.advantageHits=0;att.v9.battleFlags.elementEffectBoost=true;}}
 }
 const masterExtra=masterTargets.length?applyMaster(this,att,a,masterTargets,key,true,effectKnow):{heal:0,shield:0};
 if(att.combatRole==='fighter')att.v9.role.fury=Math.min(5,Number(att.v9.role.fury||0)+1);if(a.masterEffects?.followup||a.masterEffects?.counter||/truy kích|phản kích/i.test(desc(a)))att.v9.battleFlags.afterReaction=true;this.pushEvent('damage',{sourceId:att.id,key,ability:a.name,impacts,total});
 const meaningful=impacts.filter(x=>!x.evaded&&Number(x.damage)>0&&!x.protection);if(meaningful.length){const targetHasSlow=meaningful.some(imp=>{const t=this.allRosterUnits.find(x=>x.id===imp.targetId);return t&&Object.keys(t.statuses||{}).concat(Object.keys(t.customStatuses||{})).some(x=>/Slow|SPEED Down|Hàn|Chậm|Frost|Ice/i.test(x));});this.processMechanicEvent('DAMAGE_DEALT',{actorId:att.id,sourceId:att.id,key,ability:a.name,damageType:a.type||null,targetHasSlow,impacts:meaningful,total:meaningful.reduce((n,x)=>n+Number(x.damage||0),0)},[att]);for(const imp of meaningful)this.processMechanicEvent('ALLY_DAMAGE',{actorId:att.id,sourceId:att.id,targetId:imp.targetId,key,ability:a.name,damage:imp.damage,impacts:[imp]},this.allUnits);}
 if(meaningful.some(x=>x.crit)){this.processMechanicEvent('CRIT',{actorId:att.id,sourceId:att.id,key,ability:a.name,bonus:1,round:this.state.round},[att]);this.processMechanicEvent('ALLY_CRIT',{actorId:att.id,sourceId:att.id,side:att.side,key,ability:a.name,round:this.state.round},this.allUnits);}
 breakEvents.forEach(e=>{this.pushEvent('break',e);this.processMechanicEvent('SHIELD_BREAK',{actorId:att.id,sourceId:att.id,targetId:e.targetId,ownerId:e.targetId,key,ability:a.name,round:this.state.round},this.allUnits);});killEvents.forEach(e=>{this.pushEvent('kill',e);this.processMechanicEvent('KILL',{actorId:att.id,sourceId:att.id,targetId:e.targetId,bonus:1},this.allUnits);window.POWDER_MECHANICS_V2?.purgeTargetStacks?.(this.allRosterUnits,e.targetId);});return{damage:total,heal:Number(masterExtra.heal)||0,shield:Number(masterExtra.shield)||0,targets,impacts};};
function afterMain(core,att,key,a,res,cost){newRound(att,core.state.round);const rf=att.v9.roundFlags,bf=att.v9.battleFlags;if(key==='basic'){rf.afterBasic=true;rf.afterSkill=false;bf.eliteBasics=(bf.eliteBasics||0)+1;if(hasSet(att,'Tinh Nhuệ',4)&&bf.eliteBasics>=2){bf.eliteBasics=0;bf.eliteSkill1=true}}else{rf.afterSkill=true}if(key==='ultimate')bf.afterUlt=true;
  const targetId=res?.targets?.[0]?.id||null;if(targetId){if(att.v9.lastMainTarget===targetId)att.v9.sameTargetCount++;else{att.v9.sameTargetCount=1;att.v9.lastMainTarget=targetId;bf.rageNext=false;bf.artChainReady=false}if(att.v9.sameTargetCount>=2){if(hasSet(att,'Cuồng Công',4))bf.rageNext=true;if(art(att)?.effect?.code==='same_target_chain')bf.artChainReady=true}}
  if(att.combatRole==='mage'&&key!=='basic'&&cost>0)att.v9.role.arcaneSeal=Math.min(3,Number(att.v9.role.arcaneSeal||0)+1);
  if(att.combatRole==='musician'){att.v9.role.allyActions=(att.v9.role.allyActions||0)+1}
  if(att.v9.mythic.breakthrough)att.v9.mythic.postBreakActions=(att.v9.mythic.postBreakActions||0)+1;
  // Musician role rhythm: allied main actions build beats for active musicians. Auxiliary actions never recurse.
  const allies=att.side==='player'?core.living('player'):core.living('enemy');for(const m of allies.filter(x=>x.combatRole==='musician')){init(m);m.v9.role.allyActions=(m.v9.role.allyActions||0)+1;if(m.v9.role.allyActions>=3){m.v9.role.allyActions=0;m.v9.role.beats=(m.v9.role.beats||0)+1;if(m.v9.role.beats>=3){m.v9.role.beats=0;if(m.v9.roundFlags.chorus!==core.state.round){m.v9.roundFlags.chorus=core.state.round;for(const x of allies)x.meter=Math.min(90,x.meter+6);core.pushEvent('v9-chorus',{sourceId:m.id,side:m.side})}}}}
}
P.executeAction=function(att,key,req,knowledge={baseWrong:0,extraCorrect:0}){init(att);newRound(att,this.state.round);const a=this.ability(att,key),cost=this.abilityCost(att,key),beforeMana=att.mana,meterBefore=new Map(this.allUnits.map(u=>[u.id,Number(u.meter)||0])),hpBeforeMap=new Map(this.allUnits.map(u=>[u.id,Number(u.hp)||0])),shieldBeforeMap=new Map(this.allUnits.map(u=>[u.id,Number(u.shield)||0])),manaBeforeMap=new Map(this.allUnits.map(u=>[u.id,Number(u.mana)||0])),eventStart=this.events.length;const pendingManaRefund=att.v9.battleFlags.manaRefundReady||att.v9.battleFlags.artManaReady;const formActionValid=this.canUse(att,key);if(formActionValid&&att.powId==='frostmaw'){if(key==='skill1'||key==='skill2')setFrostmawForm(this,att,'kun',a?.name||key);else if(key==='exclusive')setFrostmawForm(this,att,'peng',a?.name||key);}const res=oldExec.call(this,att,key,req,knowledge);const dealtTotal=Math.max(0,Number(res?.result?.damage||0));if(dealtTotal>0){const ls=Object.values(att.customStatuses||{}).reduce((n,v)=>n+Math.max(0,Number(v?.lifestealPct)||0),0);if(ls>0){const healed=E.healTarget(att,Math.round(dealtTotal*Math.min(.5,ls)));if(healed>0){this.pushEvent('heal',{sourceId:att.id,targetIds:[att.id],amount:healed,label:'Hút máu',utility1850:true});oldMechanicEvent.call(this,'HEAL',{actorId:att.id,sourceId:att.id,side:att.side,targetIds:[att.id],amount:healed,round:this.state.round},this.allUnits);}}}for(const u of this.allUnits){const before=Number(meterBefore.get(u.id));if(!Number.isFinite(before))continue;const resist=Object.values(u.customStatuses||{}).some(v=>v?.meterLossResist);if(resist&&Number(u.meter)<before){const after0=Number(u.meter)||0;u.meter=before;this.pushEvent('meter-resist',{sourceId:u.id,targetId:u.id,before:after0,after:before,delta:before-after0,utility1850:true});}}for(const u of this.allUnits){const b=u.v9?.battleFlags?.v1850DebuffBlockSource;if(b&&b.sourceId===att.id)delete u.v9.battleFlags.v1850DebuffBlockSource;}if(a?.cooldown)att.v9.cooldowns[key]=Number(a.cooldown);att.v9.totalManaSpent+=cost;if(pendingManaRefund&&key!=='basic'&&cost>0){let refund=0;if(att.v9.battleFlags.manaRefundReady){refund+=18;att.v9.battleFlags.manaRefundReady=false}if(att.v9.battleFlags.artManaReady){refund+=Number(art(att)?.effect?.value||6);att.v9.battleFlags.artManaReady=false}att.mana=clamp(att.mana+refund,0,att.maxMana);if(refund)this.pushEvent('mana',{targetId:att.id,amount:refund,source:'V9 refund'})}
  if(hasSet(att,'Điều Nhịp',4)&&att.v9.totalManaSpent>=60&&att.v9.roundFlags.set6!==this.state.round){att.v9.totalManaSpent-=60;att.v9.roundFlags.set6=this.state.round;att.v9.battleFlags.manaRefundReady=true;att.v9.battleFlags.manaTempoBoost=true}if(art(att)?.effect?.code==='mana_refund_50'&&att.v9.totalManaSpent>=50&&att.v9.roundFlags.art27!==this.state.round){att.v9.totalManaSpent-=50;att.v9.roundFlags.art27=this.state.round;att.v9.battleFlags.artManaReady=true}
  afterMain(this,att,key,a,res,cost);flarionAfterAction(this,att,key,a,res);
  if(hasSet(att,'Cổ Thần Điều Hòa',4)&&['ultimate','exclusive'].includes(key)){const allies=(att.side==='player'?this.living('player'):this.living('enemy')).filter(x=>x.id!==att.id).sort((x,y)=>x.hp/x.maxHp-y.hp/y.maxHp).slice(0,2);for(const x of allies)x.customStatuses[`V9 Harmony DR ${att.id}`]={turns:1,incomingDR:.35,sourceId:att.id,kind:'buff'}}
  const produced=this.events.slice(eventStart),healCovered=new Set(),shieldCovered=new Set();for(const ev of produced){if(ev.type==='heal')for(const id of ev.targetIds||[])healCovered.add(id);if(ev.type==='shield'&&ev.targetId)shieldCovered.add(ev.targetId);if(ev.type==='status-apply'&&ev.status==='Shield'&&ev.targetId)shieldCovered.add(ev.targetId);}
  for(const u of this.allUnits){const hp0=Number(hpBeforeMap.get(u.id))||0,hp1=Number(u.hp)||0,dh=hp1-hp0;if(dh>0&&!healCovered.has(u.id))this.pushEvent('heal',{sourceId:att.id,targetIds:[u.id],amount:dh,ability:a?.name||key,key,derived:true});const sh0=Number(shieldBeforeMap.get(u.id))||0,sh1=Number(u.shield)||0,ds=sh1-sh0;if(ds>0&&!shieldCovered.has(u.id))this.pushEvent('shield',{sourceId:att.id,targetId:u.id,amount:ds,ability:a?.name||key,key,derived:true});const m0=Number(manaBeforeMap.get(u.id))||0,m1=Number(u.mana)||0,dm=m1-m0;if(u.id!==att.id&&Math.abs(dm)>=.01)this.pushEvent('mana-change',{sourceId:att.id,targetId:u.id,before:m0,after:m1,delta:dm,ability:a?.name||key,key});const before=Number(meterBefore.get(u.id))||0,after=Number(u.meter)||0,delta=after-before;if(Math.abs(delta)>=.01)this.pushEvent('meter-change',{sourceId:att.id,targetId:u.id,before,after,delta,ability:a?.name||key,key});}
  return res};
// Expose exact V7.1 matrix and runtime diagnostics without replacing the stable V7 object used by the scene.
C.masterVersion='11.4-kunpeng-combat-forms';C.elementMatrixV9=ELEMENT_MATRIX;window.POWDER_COMBAT_V9=C;
window.POWDER_COMBAT_V9_DIAGNOSTICS={elementMultiplier:elemMult,totalCoeff,statusChance,hasSet,artifact:art};
})();
