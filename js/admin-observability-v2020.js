(()=>{'use strict';
const VERSION='20.2.0',BUILD='powder-21.0.0-official-production',URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';let st=null;
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function ses(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,p={}){const s=ses();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-observability',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,channel:'production',buildId:BUILD,...p})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function ensure(){let h=$('#obs2020');if(h)return h;const page=$('#pageLaunch')||$('#pageReliability')||document.querySelector('main');if(!page)return null;const sec=document.createElement('section');sec.className='admin-card obs2020';sec.innerHTML='<div class="card-head"><div><p class="eyebrow">OBSERVABILITY · 20.2.0</p><h2>Post-Launch Stability</h2><p class="muted">P50/P95/P99, release rings, crash/error aggregation và rollback comparison. Production mặc định OFF.</p></div><span id="obs2020Badge">OFF</span></div><div id="obs2020"></div>';page.appendChild(sec);return $('#obs2020')}
function n(v,d=0){return Number(v||0).toFixed(d)}
function render(){const h=ensure();if(!h||!st)return;const c=st.config||{},x=st.health||{},eps=x.endpoints||[],rings=x.rings||[];$('#obs2020Badge').textContent=!c.enabled?'OFF':x.healthy?'HEALTHY':'HOLD';h.innerHTML=`<div class="obs-grid"><div><small>Sampling</small><b>${c.enabled?'ON':'OFF'} · ${n(Number(c.sample_rate||0)*100,1)}%</b></div><div><small>Requests / 30m</small><b>${x.requests||0}</b></div><div><small>Error rate</small><b>${n(Number(x.errorRate||0)*100,2)}%</b></div><div><small>P50 / P95 / P99</small><b>${n(x.p50Ms,0)} / ${n(x.p95Ms,0)} / ${n(x.p99Ms,0)} ms</b></div><div><small>Devices</small><b>${x.devices||0}</b></div><div><small>Crash / Save conflict</small><b>${x.crashes||0} / ${x.saveConflicts||0}</b></div></div><div class="obs-actions"><button data-refresh>Refresh</button><button data-toggle>${c.enabled?'Disable':'Enable'} sampling</button><button data-baseline>Save baseline</button><button data-compare>Compare baseline</button></div><div class="obs-tables"><div><h3>API endpoints</h3><table><thead><tr><th>Endpoint</th><th>Req</th><th>Err</th><th>P95</th><th>P99</th></tr></thead><tbody>${eps.map(e=>`<tr><td>${esc(e.endpoint)}</td><td>${e.requests}</td><td>${n(Number(e.error_rate||0)*100,2)}%</td><td>${n(e.p95_ms,0)}</td><td>${n(e.p99_ms,0)}</td></tr>`).join('')||'<tr><td colspan="5">Chưa có sample.</td></tr>'}</tbody></table></div><div><h3>Release rings</h3><table><thead><tr><th>Ring</th><th>Req</th><th>Error</th><th>P95</th></tr></thead><tbody>${rings.map(r=>`<tr><td>${r.rollout_percent}%</td><td>${r.requests}</td><td>${n(Number(r.error_rate||0)*100,2)}%</td><td>${n(r.p95_ms,0)}</td></tr>`).join('')||'<tr><td colspan="4">Chưa có ring data.</td></tr>'}</tbody></table></div></div><pre id="obs2020Compare" hidden></pre>`;h.querySelector('[data-refresh]').onclick=load;h.querySelector('[data-toggle]').onclick=async()=>{if(!confirm(`${c.enabled?'TẮT':'BẬT'} Production Observability?`))return;await api('set_config',{config:{enabled:!c.enabled}});await load()};h.querySelector('[data-baseline]').onclick=async()=>{await api('snapshot',{kind:'baseline',label:`baseline-${new Date().toISOString()}`});await load()};h.querySelector('[data-compare]').onclick=async()=>{const d=await api('compare'),p=$('#obs2020Compare');p.hidden=false;p.textContent=JSON.stringify(d.comparison||{note:'Chưa có baseline'},null,2)}}
async function load(){try{st=await api('state');render()}catch(e){const h=ensure();if(h)h.innerHTML=`<p class="is-error">${esc(e.message)}</p>`}}
function bind(){ensure();setTimeout(load,1200)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();window.POWDER_ADMIN_OBSERVABILITY_V2020={load,state:()=>st};
})();
