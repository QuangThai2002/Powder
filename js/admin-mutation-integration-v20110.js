(()=>{'use strict';
const VERSION='20.11.0',URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));let state=null,busy=false;
function sess(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,p={}){const s=sess();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-mutation-integration',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...p})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function toast(t){const e=$('#adminToast');if(e){e.textContent=t;e.hidden=false;clearTimeout(e._t);e._t=setTimeout(()=>e.hidden=true,3200)}}
function render(){const root=$('#mutation20100Root');if(!root)return;const p=state?.adapterPosture||{},r=p.routes||{},d=p.dispatch||{},a=state?.atomicPosture||{},routes=state?.routes||[],owner=state?.role==='owner',ready=p.ready===true;root.innerHTML=`<div class="m20100-summary"><span class="${ready?'pass':'hold'}"><small>CANONICAL ADAPTERS</small><b>${ready?'READY':'HOLD'}</b></span><span class="${Number(r.covered||0)===Number(r.required||18)?'pass':'hold'}"><small>ROUTE COVERAGE</small><b>${Number(r.covered||0)}/${Number(r.required||18)}</b></span><span class="${Number(r.verified||0)===Number(r.required||18)?'pass':'hold'}"><small>SERVER VERIFIED</small><b>${Number(r.verified||0)}/${Number(r.required||18)}</b></span><span class="${Number(d.unknown||0)===0?'pass':'hold'}"><small>UNKNOWN TX</small><b>${Number(d.unknown||0)}</b></span><span class="${a.ready?'pass':'hold'}"><small>SQL ATOMIC CORE</small><b>${a.ready?'READY':'HOLD'}</b></span></div><div class="m20100-actions"><button class="admin-btn ghost" id="m20110Refresh">Làm mới</button><button class="admin-btn primary" id="m20110Reconcile" ${owner?'':'disabled'}>Reconcile Safe</button><button class="admin-btn danger" id="m20110Chaos" ${owner?'':'disabled'}>Atomic Chaos Drill</button></div><p class="m20110-warn"><b>20.11 gateway-only:</b> player write không còn fallback trực tiếp. 18 route đã được centralize, nhưng Official Production chỉ READY khi từng route có evidence mới ≤7 ngày chứng minh <b>txKey idempotency + business atomicity + direct legacy write blocked</b>. Không có nút tự đánh dấu PASS trong UI.</p><div class="m20100-grid"><section><h3>18 canonical routes</h3><div class="m20100-list">${routes.filter(x=>x.required_for_launch).map(x=>{const ok=x.idempotency_verified&&x.business_atomic_verified&&x.direct_write_blocked&&x.verified_at&&Date.now()-new Date(x.verified_at).getTime()<=7*864e5;return `<div class="${ok?'pass':'hold'}"><span><b>${esc(x.scope)}/${esc(x.action)}</b><small>${esc(x.route_kind)} → ${esc(x.target_edge||x.handler_function||'SQL')}</small><span class="m20110-route-meta"><em>txKey ${x.txkey_forwarded?'✓':'✕'}</em><em>idempotent ${x.idempotency_verified?'✓':'—'}</em><em>atomic ${x.business_atomic_verified?'✓':'—'}</em><em>direct lock ${x.direct_write_blocked?'✓':'—'}</em></span>${x.evidence_sha256?`<span class="m20110-evidence">evidence ${esc(x.evidence_sha256.slice(0,12))}… · ${x.verified_at?new Date(x.verified_at).toLocaleString('vi-VN'):'—'}</span>`:''}</span><strong>${ok?'VERIFIED':'NEEDS EVIDENCE'}</strong></div>`}).join('')}</div></section><section><h3>Dispatch / Recovery</h3><p>Unknown: <b>${Number(d.unknown||0)}</b></p><p>Stale pending: <b>${Number(d.stalePending||0)}</b></p><p>Failed: <b>${Number(d.failed||0)}</b></p><p>Open dispatch rows: <b>${(state?.openDispatch||[]).length}</b></p><p class="muted">Pending quá 90 giây chuyển UNKNOWN. 20.11 không tự retry/replay transaction mơ hồ.</p></section></div>`;$('#m20110Refresh')?.addEventListener('click',refresh);$('#m20110Reconcile')?.addEventListener('click',()=>run('reconcile','Chạy reconciliation fail-safe? Transaction mơ hồ chỉ được đánh dấu UNKNOWN, không replay tài nguyên.'));$('#m20110Chaos')?.addEventListener('click',()=>run('chaos_drill','Chạy atomic chaos drill trên sandbox? Không chạm tài nguyên người chơi.'))}
async function run(action,msg){if(busy||!confirm(msg))return;busy=true;try{const x=await api(action,{limit:200});state=x.state||state;toast(action==='reconcile'?'Reconciliation 20.11 hoàn tất.':'Atomic chaos drill hoàn tất.');render()}catch(e){toast(e.message)}finally{busy=false}}
async function refresh(){if(busy)return;busy=true;try{state=await api('state');render()}catch(e){toast(e.message)}finally{busy=false}}
function boot(){window.POWDER_ADMIN_MUTATION_V20110=Object.freeze({version:VERSION,refresh,getState:()=>state});document.querySelector('[data-page="mutation"]')?.addEventListener('click',()=>setTimeout(refresh,20));setTimeout(()=>{if(!$('#pageMutation')?.hidden)refresh()},1200)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
