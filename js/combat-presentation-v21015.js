(()=>{'use strict';
if(window.POWDER_COMBAT_PRESENTATION_V21015)return;
const VERSION='21.2.12-combat-scoped';
const state={mounts:0,handoffs:0,syncs:0,mutations:0,fxSeen:0,maxFxCluster:0,attackFlows:0,controlLocks:0,criticalUnits:0,woundedUnits:0,lastSyncAt:0,lastPressure:'calm',mounted:false,viewObserverActive:false};
let mount=null,mountObserver=null,viewObserver=null,raf=0;
const now=()=>performance?.now?.()||Date.now();
const pressure=()=>String(window.POWDER_ADAPTIVE_PRESSURE_V21011?.level?.()||document.documentElement.dataset.resourcePressure||'calm');
const activeByDom=n=>Boolean(n?.isConnected&&!n.closest?.('[hidden]'));
const inBattleView=()=>/battle|combat/i.test(String(document.body?.dataset?.activeView||''));
function fxKind(n){for(const k of ['crit','damage','heal','shield-gain','shield','blocked','evade','status-control','status-dot','status-heal','break','kill','cleanse','guard'])if(n.classList.contains(k))return k;return'other'}
function groupKey(n){const side=n.classList.contains('enemy')?'enemy':n.classList.contains('player')?'player':'center';const s=[0,1,2].find(i=>n.classList.contains(`slot-${i}`));return`${side}:${s??'x'}`}
function lanePosition(i){if(i===0)return{x:0,y:0,lane:0};const ring=Math.ceil(i/2),sign=i%2?-1:1;return{x:sign*(ring%2?64:32),y:sign*ring*54,lane:sign*ring}}
function laneFx(root){const groups=new Map();for(const n of root.querySelectorAll('.cv7-fx[data-fx-id]')){n.dataset.cpKind=fxKind(n);const k=groupKey(n);if(!groups.has(k))groups.set(k,[]);groups.get(k).push(n)}let max=0;for(const nodes of groups.values()){max=Math.max(max,nodes.length);nodes.forEach((n,i)=>{const pos=lanePosition(i);n.dataset.cpLane=String(pos.lane);n.style.setProperty('--cp-lane-x',`${pos.x}px`);n.style.setProperty('--cp-lane-y',`${pos.y}px`);n.style.setProperty('--cp-stack-index',String(i))})}state.maxFxCluster=Math.max(state.maxFxCluster,max);state.fxSeen+=root.querySelectorAll('.cv7-fx[data-fx-id]').length}
function markUnits(root){let controls=0,critical=0,wounded=0;for(const u of root.querySelectorAll('.cv7-unit')){const lock=!!u.querySelector('.cv7-cc-lock')||u.classList.contains('pulse-status-control');let kind='normal';if(lock){kind='control';controls++}else if(u.classList.contains('hp-critical')){kind='critical';critical++}else if(u.classList.contains('hp-wounded')){kind='wounded';wounded++}u.dataset.cpState=kind;u.dataset.cpStatuses=String(u.querySelectorAll('.cv7-status-chip,.cv78-status-chip').length)}state.controlLocks=controls;state.criticalUnits=critical;state.woundedUnits=wounded}
function sync(){raf=0;if(!inBattleView()){deactivate();return}if(!mount||!mount.isConnected){handoff();return}state.syncs++;state.lastSyncAt=now();const p=pressure();state.lastPressure=p;mount.classList.add('cp215-ready');mount.classList.toggle('cp215-pressure-reduced',p==='hot'||p==='critical'||mount.classList.contains('cv71-fx-low'));laneFx(mount);markUnits(mount);const flows=mount.querySelectorAll('.cv7-attack-flow').length;state.attackFlows=flows;mount.dataset.cpAttack=flows?'active':'idle';mount.dataset.cpPressure=p;try{window.dispatchEvent(new CustomEvent('powder:combat-presentation-sync',{detail:{version:VERSION,flows,pressure:p}}))}catch(_){}}
function schedule(){if(raf||document.hidden||!inBattleView())return;raf=requestAnimationFrame(sync)}
function attach(m){if(!m||m===mount){schedule();return}if(mount)state.handoffs++;detachMount();mount=m;state.mounts++;state.mounted=true;mountObserver=new MutationObserver(ms=>{state.mutations+=ms.length;schedule()});mountObserver.observe(mount,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});schedule()}
function detachMount(){mountObserver?.disconnect();mountObserver=null;if(raf){cancelAnimationFrame(raf);raf=0}if(mount)mount.classList.remove('cp215-ready','cp215-pressure-reduced');mount=null;state.mounted=false}
function pickMount(root=document){const all=[...(root.querySelectorAll?.('.combat-v7-mount')||[])];return all.find(activeByDom)||all[0]||null}
function handoff(){if(!inBattleView())return deactivate();const root=document.querySelector('#battleView')||document,next=pickMount(root);if(next&&next!==mount)attach(next);else if(!next)detachMount();else schedule()}
function ensureViewObserver(){if(viewObserver||!inBattleView())return;const root=document.querySelector('#battleView');if(!root)return;viewObserver=new MutationObserver(ms=>{state.mutations+=ms.length;if(!mount||!mount.isConnected||ms.some(m=>[...m.addedNodes].some(n=>n.nodeType===1&&(n.matches?.('.combat-v7-mount')||n.querySelector?.('.combat-v7-mount')))))handoff()});viewObserver.observe(root,{childList:true,subtree:true});state.viewObserverActive=true}
function activate(){if(!inBattleView())return false;ensureViewObserver();handoff();return true}
function deactivate(){viewObserver?.disconnect();viewObserver=null;state.viewObserverActive=false;detachMount()}
function onView(e){const v=String(e?.detail?.view||document.body?.dataset?.activeView||'');if(/battle|combat/i.test(v))activate();else deactivate()}
function audit(){if(!mount)return{mounted:false,overlaps:0,fx:0,flows:0};const fx=[...mount.querySelectorAll('.cv7-fx[data-fx-id]')].filter(n=>{const r=n.getBoundingClientRect(),s=getComputedStyle(n);return s.display!=='none'&&r.width>0&&r.height>0});let overlaps=0;for(let i=0;i<fx.length;i++)for(let j=i+1;j<fx.length;j++){if(groupKey(fx[i])!==groupKey(fx[j]))continue;const a=fx[i].getBoundingClientRect(),b=fx[j].getBoundingClientRect();if(a.left<b.right&&a.right>b.left&&a.top<b.bottom&&a.bottom>b.top)overlaps++}return{mounted:true,fx:fx.length,flows:mount.querySelectorAll('.cv7-attack-flow').length,overlaps,pressure:state.lastPressure,controlLocks:state.controlLocks,criticalUnits:state.criticalUnits,activeByDom:activeByDom(mount)}}
function snapshot(){return{version:VERSION,...state,active:!!mount?.isConnected,activeByDom:activeByDom(mount),fxNow:mount?.querySelectorAll('.cv7-fx[data-fx-id]').length||0,flowNow:mount?.querySelectorAll('.cv7-attack-flow').length||0,scopedObserver:true}}
window.addEventListener('powder:view-changed',onView,{passive:true});window.addEventListener('powder:rendered',e=>{if(/battle|combat/i.test(String(e?.detail?.view||document.body?.dataset?.activeView||'')))activate()},{passive:true});document.addEventListener('visibilitychange',()=>{if(document.hidden){if(raf){cancelAnimationFrame(raf);raf=0}}else activate()},{passive:true});window.addEventListener('powder:resource-pressure',schedule,{passive:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',activate,{once:true});else queueMicrotask(activate);
window.POWDER_COMBAT_PRESENTATION_V21015={version:VERSION,snapshot,audit,sync:activate,detach:deactivate};
})();
