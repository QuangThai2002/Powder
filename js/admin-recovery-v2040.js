(()=>{'use strict';
const VERSION='20.4.0',BUILD='powder-21.0.0-official-production',URL='https://pxejydhqfzidnheudgnn.supabase.co',KEY='sb_publishable_EeppsefEV5sMaTcHrKWNCA_-dzAEA_H',STORE='powder_admin_session_v151';let state=null;
const q=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function sess(){try{return JSON.parse(sessionStorage.getItem(STORE)||'null')}catch{return null}}
async function api(action,p={}){const s=sess();if(!s?.access_token)throw new Error('Hãy đăng nhập Admin.');const r=await fetch(URL+'/functions/v1/powder-admin-recovery',{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,channel:'production',buildId:BUILD,...p})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
function ensure(){let h=q('#recovery2040Admin');if(h)return h;const page=q('#pageReliability')||q('#pageOps')||q('#pageLaunch')||document.querySelector('main');if(!page)return null;const sec=document.createElement('section');sec.className='admin-card recovery2040-card';sec.innerHTML='<div class="card-head"><div><p class="eyebrow">DISASTER RECOVERY · 20.4.0</p><h2>Backup Integrity & Real Restore Gate</h2><p class="muted">Sandbox drill không chạm dữ liệu người chơi. Production chỉ READY khi có Real Restore Evidence đạt RPO/RTO và verify Cloud Save/Economy/Pow/Inventory/Rollback.</p></div><span id="recovery2040Badge">HOLD</span></div><div id="recovery2040Admin"><p class="muted">Đang tải Recovery Posture…</p></div>';page.appendChild(sec);return q('#recovery2040Admin')}
function checks(p){return Object.entries(p?.checks||{}).map(([k,v])=>`<div class="${v?'pass':'hold'}"><span>${esc(k)}</span><b>${v?'PASS':'HOLD'}</b></div>`).join('')}
function dt(id){const v=q(id)?.value;return v?new Date(v).toISOString():''}
function render(){const h=ensure();if(!h)return;const p=state?.posture||{},cfg=state?.config||{},real=p.realEvidence||null,role=state?.role||'support',canOwner=role==='owner';const b=q('#recovery2040Badge');if(b){b.className=p.ready?'pass':'hold';b.textContent=p.ready?'READY':'HOLD'}h.innerHTML=`<div class="recovery2040-summary"><span class="${p.ready?'pass':'hold'}"><small>RECOVERY POSTURE</small><b>${p.ready?'READY':'HOLD'}</b></span><span><small>BACKUPS</small><b>${p.counts?.backupRows??0}</b><em>${p.counts?.usersWithBackup??0}/${p.counts?.playerSaves??0} users</em></span><span class="${p.checks?.sandboxRestore?'pass':'hold'}"><small>SANDBOX</small><b>${p.checks?.sandboxRestore?'PASS':'HOLD'}</b></span><span class="${p.checks?.realRestoreEvidence?'pass':'hold'}"><small>REAL RESTORE</small><b>${p.checks?.realRestoreEvidence?'PASS':'MISSING'}</b></span></div><div class="recovery2040-grid"><section><h3>Technical posture</h3><div class="recovery2040-checks">${checks(p)}</div><p class="muted">Schema fingerprint: <code>${esc(p.schema?.current||'—')}</code></p><p class="muted">Sandbox: ${p.sandbox?`${p.sandbox.elapsedMs||0}ms · hash ${p.sandbox.hashMatch?'match':'mismatch'} · cleanup ${p.sandbox.cleaned?'yes':'no'}`:'chưa có evidence'}</p><div class="recovery2040-actions"><button class="admin-btn primary" id="r2040Sandbox" ${role==='support'?'disabled':''}>Run Sandbox Restore</button><button class="admin-btn ghost" id="r2040Snapshot" ${role==='support'?'disabled':''}>Save Posture Snapshot</button><button class="admin-btn ghost" id="r2040Refresh">Refresh</button><button class="admin-btn ghost" id="r2040Local">Mở Local Recovery</button></div></section><section><h3>Recovery targets</h3><div class="recovery2040-metrics"><span>RPO target <b>${cfg.target_rpo_minutes??360} phút</b></span><span>RTO target <b>${cfg.target_rto_minutes??60} phút</b></span><span>Evidence tuổi tối đa <b>${cfg.max_real_restore_age_days??7} ngày</b></span><span>Require real restore <b>${cfg.require_real_restore?'YES':'NO'}</b></span></div>${real?`<p class="pass">Latest: ${esc(real.backupProvider)} · RPO ${real.rpoMinutes}m · RTO ${real.rtoMinutes}m · ${esc(real.status)}</p>`:'<p class="hold">Chưa có Real Restore Evidence cho build 20.4.</p>'}</section><section class="recovery2040-real"><h3>Record Real Restore Evidence · Owner</h3><p class="muted">Form này chỉ ghi bằng chứng. Không có hành động restore production.</p><div class="recovery2040-form"><label>Backup provider<input id="r2040Provider" placeholder="Supabase / PITR / external"></label><label>Backup ID<input id="r2040BackupId" placeholder="backup identifier"></label><label>Backup SHA-256<input id="r2040BackupSha" maxlength="64"></label><label>Evidence SHA-256<input id="r2040EvidenceSha" maxlength="64"></label><label>Backup created<input id="r2040BackupAt" type="datetime-local"></label><label>Incident cutoff<input id="r2040IncidentAt" type="datetime-local"></label><label>Restore started<input id="r2040StartAt" type="datetime-local"></label><label>Restore completed<input id="r2040EndAt" type="datetime-local"></label><label>Restore environment<input id="r2040Env" placeholder="isolated-restore / staging"></label></div><div class="recovery2040-flags">${[['Schema compatible','r2040Schema'],['Cloud Save verified','r2040Cloud'],['Economy verified','r2040Economy'],['Pow verified','r2040Pows'],['Inventory verified','r2040Inventory'],['Rollback verified','r2040Rollback']].map(([t,id])=>`<label><input id="${id}" type="checkbox"> ${t}</label>`).join('')}</div><textarea id="r2040Notes" placeholder="Notes / restore drill reference"></textarea><button class="admin-btn danger" id="r2040Record" ${canOwner?'':'disabled'}>Record Real Restore Evidence</button></section></div>`;bind()}
function flag(id){return !!q(id)?.checked}function bind(){q('#r2040Refresh').onclick=refresh;q('#r2040Sandbox').onclick=()=>act('run_sandbox');q('#r2040Snapshot').onclick=()=>act('snapshot');q('#r2040Local').onclick=()=>window.open('index.html?recovery=1','_blank','noopener');const rec=q('#r2040Record');if(rec)rec.onclick=async()=>{if(!confirm('Xác nhận đây là bằng chứng từ một restore drill thật trên môi trường cô lập, không phải số liệu giả?'))return;await act('record_real_restore',{backupProvider:q('#r2040Provider').value,backupId:q('#r2040BackupId').value,backupSha256:q('#r2040BackupSha').value,evidenceSha256:q('#r2040EvidenceSha').value,backupCreatedAt:dt('#r2040BackupAt'),incidentAt:dt('#r2040IncidentAt'),restoreStartedAt:dt('#r2040StartAt'),restoreCompletedAt:dt('#r2040EndAt'),restoreEnvironment:q('#r2040Env').value,schemaCompatible:flag('#r2040Schema'),cloudSaveVerified:flag('#r2040Cloud'),economyVerified:flag('#r2040Economy'),powsVerified:flag('#r2040Pows'),inventoryVerified:flag('#r2040Inventory'),rollbackVerified:flag('#r2040Rollback'),notes:q('#r2040Notes').value})}}
async function act(action,p={}){try{state=await api(action,p);render()}catch(e){alert(e.message)}}async function refresh(){try{state=await api('state');render()}catch(e){const h=ensure();if(h)h.innerHTML=`<p class="hold">${esc(e.message)}</p>`}}
function legacyEvidence(){const p=state?.posture||{};return{pass:!!(p.checks?.backupCoverage&&p.checks?.backupChecksum&&p.checks?.restoreRpcServiceOnly&&p.checks?.schemaFingerprint&&p.checks?.sandboxRestore),details:{version:VERSION,posture:p}}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',refresh,{once:true});else refresh();window.POWDER_ADMIN_RECOVERY_V2040={version:VERSION,refresh,state:()=>state,legacyEvidence};
})();
