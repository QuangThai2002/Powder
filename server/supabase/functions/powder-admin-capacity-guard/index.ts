import { createClient } from "npm:@supabase/supabase-js@2";
import { enforceAdminAccessV20160 } from "../_shared/admin-security-v20160.ts";
const VERSION='21.0.4',BUILD='powder-21.0.4-capacity-guard';
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"};
const out=(d:unknown,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{...cors,'X-Powder-Capacity-Version':VERSION}});
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return out({error:'Method not allowed'},405);const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,{auth:{persistSession:false,autoRefreshToken:false}});try{const jwt=(req.headers.get('Authorization')||'').replace(/^Bearer\s+/i,'');const {data:{user},error:ue}=await db.auth.getUser(jwt);if(ue||!user?.email)return out({error:'Phiên Admin không hợp lệ.'},401);const email=user.email.toLowerCase();const {data:allow}=await db.from('admin_allowlist').select('role,active').eq('email',email).maybeSingle();if(!allow?.active)return out({error:'Không đủ quyền.'},403);const role=String(allow.role||'');const body=await req.json().catch(()=>({}));const action=String(body.action||'state'),channel=String(body.channel)==='staging'?'staging':'production',buildId=String(body.buildId||BUILD);if(buildId!==BUILD)return out({error:'Build ID 21.0.4 không hợp lệ.'},400);await enforceAdminAccessV20160({req,db,jwt,userId:user.id,email,role,functionName:'powder-admin-capacity-guard',action});const state=async()=>{const [{data:posture,error:pe},{data:config,error:ce},{data:snap,error:se},{data:events,error:ee}]=await Promise.all([db.rpc('powder_capacity_posture_v21004',{p_channel:channel,p_build:buildId}),db.from('capacity_guard_config_v21004').select('*').eq('channel',channel).maybeSingle(),db.from('capacity_guard_snapshots_v21004').select('*').eq('channel',channel).eq('build_id',buildId).order('checked_at',{ascending:false}).limit(1),db.from('capacity_guard_events_v21004').select('*').eq('channel',channel).eq('build_id',buildId).order('created_at',{ascending:false}).limit(20)]);if(pe||ce||se||ee)throw pe||ce||se||ee;return{ok:true,version:VERSION,buildId,role,channel,posture,config,latestSnapshot:snap?.[0]||null,events:events||[],serverTime:new Date().toISOString()}};if(action==='state')return out(await state());if(role!=='owner')return out({error:'Chỉ Owner được thay đổi Capacity Guard.'},403);if(action==='arm'){const mh=String(body.manifestHash||'').toLowerCase(),sha=String(body.artifactSha256||'').toLowerCase();if(!/^[0-9a-f]{16,64}$/.test(mh)||!/^[0-9a-f]{64}$/.test(sha))return out({error:'ARM cần manifestHash + artifactSha256 hợp lệ.'},400);const {error}=await db.rpc('powder_capacity_arm_v21004',{p_channel:channel,p_build:buildId,p_manifest_hash:mh,p_artifact_sha256:sha,p_actor:email});if(error)throw error;return out(await state())}if(action==='disarm'){const {error}=await db.rpc('powder_capacity_disarm_v21004',{p_channel:channel,p_build:buildId,p_actor:email});if(error)throw error;return out(await state())}if(action==='emergency_hold'){await db.from('release_rollout_v1950').update({emergency_mode:'halt',emergency_message:'21.0.4 Capacity Owner HOLD',updated_at:new Date().toISOString(),updated_by:email}).eq('channel',channel);return out(await state())}return out({error:'Action không hợp lệ.'},400)}catch(e){const x=e as any;return out({error:String(x?.message||e),code:x?.code||''},Number(x?.status||500))}});
